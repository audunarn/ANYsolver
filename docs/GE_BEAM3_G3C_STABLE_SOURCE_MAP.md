# G3c private stable-numerics source map

This is a design-only freeze, not graph qualification or permission to execute
mechanics. The accepted isolated kernel is preserved at 8aa4a246; its acceptance
does not cure the failed mixed-owner directional result (2.767173042642239e-7).
The unchanged directional gate is 1e-7 at every h=1e-4,1e-5,1e-6.

The canonical source map binds eleven original source modules (seven whole
modules and four exact helper extractions), two unchanged test bodies, original
Git blob IDs, LF-normalized source hashes and expected generated hashes. Preview
uses AST import spans and individually enumerated literal replacements only.
It never imports mechanics, writes destinations, or dynamically clones code.

## Dependency boundary

| Private path | Required derivative route |
| --- | --- |
| owner | Support Exp/Log and graph dispatch |
| element -> boundary -> chart | Native chart pullback |
| operator | Native strain Exp/Log |
| operator -> line_work | Nonzero line-load work |
| beam / shell -> joint | Local strain and spatial/chart derivatives |
| shell -> fit | Unchanged positional Procrustes helper |
| operator / line_work -> compensated | Unchanged compensated-strain helpers |

Only Exp/Log import symbols route to the accepted private kernel. Original Jet2,
state/context/reference classes remain authentic. Native ElasticElement and
ElasticOperator are copied together and get distinct private formulation/policy
IDs; their schemas, exact-type checks, issued state ownership, 42-variable
response, Schur operations and origin checks do not change.

Authoritative native rotation_exponential, internal value-only cell rotation,
predictor/guard rotation_log and reference geometry frame helpers stay original.
They are not derivative fallbacks. Original recovery retains only its pure
owned-array utility dependency; it does not evaluate Exp/Log. Existing recovery
admission limits, including B2 clamp-domain exclusions, remain unchanged.

## Required authority wrapper (implementation not yet authorized)

The new inert package initializer and fixed authority wrapper are the only new
non-copied source modules. The wrapper must bind this map and accepted independent
review in addition to original fixture/contract/review authority. Original
expanded graphs and programs remain unchanged. The existing definition shape is
retained, with policy_sha256 set to the new map hash; source/state provenance
must distinguish predecessor and successor. No runtime digest alone is authority.

Keep effective bound, expand, command, canonical, U and SHIFT entry points. The
bound hook must remain on the actual authority-read path, preserving the existing
authority-exception poisoning regression. Validate the complete frozen input
tree, kernel, original blobs and generated destinations before importing NumPy
or evaluating mechanics. A separate reviewed implementation lease binds new
wrapper/runner/tests; this design cannot self-authorize those unknown bytes.

runtime_modules must include fixed authority, boundary, chart, compensated,
line_work, fit, kernel, shared AD and recovery modules in addition to existing
dispatch coverage. Capture imported/inherited methods and code identities.
runtime_constants must validate the kernel SINC/COSC/LOG coefficient values
against their independently fixed recurrence, require exact immutable tuple
types, and capture tuple identity and values. Preserve owned locks, poisoning,
epochs, nonces, sandbox commits and single immutable generation publication.
Do not mutate shared modules, spoof classes or weaken foreign-state rejection.

## Review and execution sequence

1. Audit this thirteen-copy map and independently review the design.
2. Implement exact static copies plus wrapper and regressions for authority,
   kernel/constant mutation, transitive dispatch, foreign state and source maps.
3. Independently review/freeze implementation and a bounded runner.
4. Execute the unchanged one-node component diagnostic and eighteen-node bridge
   inventory separately. A failed gate remains failed; do not relax h or tolerance.
5. Only after those pass, complete all 25 graph variants, histories and genuine
   restart/common-motion prefixes, rehearsal and formal G3c confirmation.

Children: 600 seconds, 24 GiB, one numerical thread, maximum three concurrently;
wave: 1800 seconds; inactivity: 120 seconds without CPU/output progress. Terminate
and drain complete process trees. Preserve partial external logs, retry nothing
automatically and publish no partial canonical mechanics evidence.

The four-path design extent is this note, the JSON source map, inert audit script
and standard-library static test. Independent review is a separate closeout path.
No source destinations exist at design freeze. No public routes, defaults, Q4/S3
mechanics, old qualification records, main branch, release or ecosystem changes.

Static validation (no mechanics):

    C:\Python\Python314\python.exe -I -S -B tests/test_ge_beam3_g3c_stable_source_map.py

This freezes only numerical plumbing. G3c full qualification, conventional Q4
finite recovery, G4 history sections, G5 and complete legacy parity remain open.
