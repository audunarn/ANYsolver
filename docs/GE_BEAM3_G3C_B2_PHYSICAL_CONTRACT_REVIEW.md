# Independent concrete B2 physical-successor contract review

Subject `ba97556c92e8e9359f63323d67f40b5173ca62f6`, tree `66bdc692cce4250e1bf30cb70684d915a590c476`. Reviewer `/root/g3b_independent_review`, separate reviewer. Accepted for private implementation design only, with no unresolved design findings. User approval now specifically covers replacing the private exact scalar B2 operator constraint; public legacy and other-family mechanics remain unchanged.

Read the complete prose, canonical JSON and seven-test file. Independently checked clean frozen commit/parent, exact three additive paths, whitespace check, and every normalized file hash against immutable Git blobs. Proposal/source hashes match the previously reviewed equations. Independent `audit_g3c_b2_physical_contract.py` passed in 0.362 seconds and confirms exact rational matrix/basic-transform arithmetic remains Fraction, all six fixtures and the exact seven-method inventory match the contract, and no numerical/production modules are imported.

Ran `C:/Python/Python314/python.exe -I -S -B tests/test_ge_beam3_g3c_b2_physical_contract.py`: seven standard-library tests passed in 0.008 seconds, exit zero. These are exact-equation/design tests, not the eight numerical-core, three adapter or three graph tests, which have not been implemented or executed under this contract.

The two physical flexibility eigenvalues and inverse remain correct. The integrated shear/moment source signs agree with the actual local-z/web convention. The source basic transform has rank six and annihilates the six analytical rigid columns; with positive six-dimensional basic stiffness this establishes the intended reference rank/nullity argument. It does not assert positivity of every finite prestressed tangent. Axial strain/work derivatives are retained separately and will require actual numerical verification against the new core.

The six concrete fixtures distinguish clamp-active, threshold, unclamped, unequal section/shear factors, finite local recipe and A=2^-80 shear-soft behavior. The last fixture has representable positive modal stiffness even when a rounded dense block loses its tiny symmetric component. The contract correctly requires retention of modal factors through force, energy and recovery, not merely accurate eigenvalue construction. Implementation review must verify those actual paths do not reconstruct a rounded dense matrix and apply it to recover the same lost mode. Dense graph conditioning remains explicitly unqualified.

Units/range tests specify length and force transforms, area/inertia/modulus scaling, rigidity power-of-two sweeps and component-dependent outputs. Force, moment, energy and tangent blocks need their distinct factors; a single global scale or max-one-only norm cannot validate the tiny physical work. The relative nonzero-energy requirement is an intentional stricter observation of the preserved physical obstruction, not permission to relax the inherited tolerances. Overflow and positive-to-zero underflow fixtures fail explicitly rather than creating a new material floor.

State/transport recipes bind the physical anchor, frame direction, noncommuting accepted/trial rotations, common motions, passive rotation, reversal and same-pose rebase. A subsequent numerical runner must bind their exact tests and every imported chart/helper through its immutable input graph. This design does not authorize borrowing accepted graph tokens, interpreting old packets as the new operator, or routing the frozen owner to the successor. New graph/restart identities and affected B2/multifamily coverage require their own freeze before propagation.

The existing code's public B2 route, B3, native GE-B3 and qualified Q4/S3 are not changed by this additive contract. Q4 finite physical recovery, remaining MO gates, full history/formal cycles and G4/G5 remain open. Next gate: implement and independently review the private local core and bounded runner; numerical execution only after that review. No defaults, main integration, release or production qualification follows.
