# G1 state-safety correction and scoped formal confirmation

Successor of blocked independent review commit `9ab7889`. Preserve the original
candidate `635888a`, both P1 findings, and all previous development evidence.
Only the private G1 analysis owner and its native material-validation hook change:
immutable authenticated free DOFs, post-callback graph validation, and validation
at the end of store material preparation. No element
equations, tolerances, old runtime, default, public API or qualification edits.

The frozen general-static/G1a contracts still govern S01-S08. New regressions
exercise virgin and accepted owners, immutable aliases, private replacement,
size tampering, final-callback graph/support/section mutations, and preparation
mutation. Rejection must preserve committed bytes, both generations and history.

## Confirmation protocol

The separate standard-library coordinator binds a clean candidate commit/tree,
every tracked input's Git identity (tree), runtime/test LF-content hashes,
Python executable bytes, and installed distribution versions. The exact ordered
49-node mechanics inventory is `ge_beam3_g1_confirmation_inventory.json`.
Infrastructure tests are a separate inventory and are not mechanics passes.
Run regression smoke and a complete rehearsal before freezing. An independent
five-key review of the frozen commit must have an accepted decision and no
findings before formal execution. The user's correction/re-review/confirmation
request authorizes this scoped protocol, not G2 or public integration.

Run two fresh exclusive external cycles, serially, with one numerical thread,
24 GiB process-tree memory, 600 seconds per child, 120 seconds of CPU/output
inactivity, and 1800 seconds per coordinator wave. There is no automatic retry.
Use all tests, reject missing/duplicate/skipped/failed setup/call/teardown records,
and preserve raw output and process records. Capture a fresh deterministic test
packet in each cycle; its historical DEVELOPMENT_PACKET schema remains unchanged
and explicitly is not itself a formal certificate. Formal acceptance is a separate
scoped record binding every test outcome and the packet hash after both agree.

Revalidate frozen inputs after each child and before exclusive aggregate creation.
Runtime metrics and absolute paths remain external diagnostics. A failure never
creates a partial acceptance aggregate. Terminal precedence: authority failure,
process/evidence failure, state/restart failure, constraint/work failure,
section/internal-algebra failure, assembly failure, scoped G1 confirmation.
Test failure blocks confirmation; detailed adjudication follows independent review,
without guessing a scientific terminal from an exception. No failed run is retried.

Successful result: `PROVISIONAL_GO_GE_BEAM3_G1_ELASTIC_STATIC_ONLY`.
This means S01-S08 on the one/two-element private elastic owner only, not general
static parity, full beam qualification, a new alias, a release, or G2 execution.
After confirmation, obtain independent evidence inspection and recommend G2.
