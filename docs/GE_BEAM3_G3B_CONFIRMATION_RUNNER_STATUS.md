# G3b bounded confirmation runner implementation

Parent: `47665b120f398618df71960aafd49c0054a9ec14`. This is an unreviewed
runner implementation checkpoint, not formal G3b acceptance.

The coordinator consumes a clean exact commit, isolated dependency manifest,
ordered eight-lane inventory and (formal mode only) an externally hash-bound
accepted independent implementation/runner review. Rehearsal is explicitly
nonqualifying. Both modes run two fresh cycles with at most three bounded child
trees in a batch: 600 seconds, 24 GiB, one numerical-library thread, 120-second
CPU/output inactivity, 1800-second wave deadline. No lane/cycle is retried.

Each child starts with -I -S -B and an absent private pycache prefix, validates
its exclusive lease and frozen input/environment graphs before importing pytest
or mechanics, rejects unbound/changed import origins, and records actual
collection and all test phases. Observation wraps owner solve before dispatch
capture and reports acceptance only after a successful return; no computation,
result or guard call is replaced. All existing progress checkpoints are read
from actual completed helper operations. Both input graphs are checked again
after execution. Only complete deterministic evidence can be exclusively linked
from a validated same-volume pending file to a canonical aggregate.

The newest 170-case correction test lane now optionally emits explicit
case-level records after its assertions: accepted/replayed journals, cache
identity observations and rejected envelopes. Its mutations, assertions and
ordered node inventory are unchanged. This data-only instrumentation is a
successor test change requiring review; it does not alter older qualification
tests or element mechanics. All seven pre-existing scientific packets must
match their preserved development byte counts and hashes, not just each other.

## Isolated dependency capsule

The builder copies installed numerical/test dependency files and binds their
actual bytes, including extensions/DLLs, without installing packages. It reads
committed source archives (not dirty working files) for ANYmaterial d8a233ef,
ANYfileIO b48ba51c, ANYgeometry 7edbb8b6 and ANYmesh 8c66f5ac. All originals,
including concurrent ANYmesh work and untracked reports, remain untouched.
Python executable/DLL/stdlib files and Git launcher/engine are also hash-bound.

Development capsule manifest:
`C:/Users/AUDUNA~1/AppData/Local/Temp/anysolver-g3b-capsule-ba64d415bdf247d59ce765af0dc45f41/environment.json`.
803,247 bytes; SHA-256
`2ce154b866565e1d03019651b1ae7fdd070e5554f38d72adf21d8080558b6756`.
Isolated import smoke passed, importing ANYsolver from this candidate and pytest
from the capsule. This capsule remains subject to independent environment review
and archival preservation before formal use; no installed package is changed.

## Infrastructure checks so far

An initial disposable unit-test run failed because its test replaced sys.flags
with an incomplete fake, disrupting Python's own traceback formatting. Raw logs
are preserved in `anysolver-g3b-development-guoglxe3` (normal temporary root).
The test now mocks a dedicated startup predicate rather than interpreter globals.
A new explicit validation run passed 16 tests in 0.32 seconds; raw directory
`anysolver-g3b-development-3dbldemy`. Neither is a scientific or formal run.

Next: complete current unit checks and bounded rehearsal against the clean runner
freeze. Resolve any genuine findings in a successor; retain failed attempts and
never resume/retry their directories. Independent review and formal confirmation
remain outstanding. Main, defaults, Q4/S3/native/legacy mechanics are unchanged.

Before the first rehearsal freeze: current runner units passed 16 tests in
0.31 seconds (raw `anysolver-g3b-development-hbqbsz3h`); the separate authority
and real-collection infrastructure lane passed 62 tests in an 18.342-second
bounded process (raw `anysolver-g3b-development-txpxeqkv`). Zero children remained.
Git whitespace checks pass, and there is no src/, package or workflow delta.

## V1 rehearsal incident and V2 correction

V1 clean candidate `c2c8d3da4cd48056d391ffbac950e593ee86344c` ran in the
fresh external directory
`C:/Users/AUDUNA~1/AppData/Local/Temp/anysolver-g3b-rehearsal-3e46d524fdc74f9c81244a255f6813f8`.
All eight Cycle 1 test lanes passed; all children exited and were reaped.
The 170 correction records were completed. The coordinator then correctly
withheld its aggregate on an infrastructure schema failure: it required a
`pytest` diagnostic directory even when no fixture requested a temporary path.
Cycle 2 did not start. This failed rehearsal is preserved and never resumed.

V2 changes only the coordinator output-inventory check to admit the absence of
that optional diagnostic directory (while rejecting a file at that name or any
unregistered output), adds four targeted regression cases, and records this
incident. All required scientific files, test counts, case records, packet
hashes, limits, input checks, element mechanics and tolerances stay unchanged.
Read-only validation of all preserved case records also exposed a validator
comparison fault: Python considers False equal to 0, but the qualification-field
mutation is intentionally type-distinct JSON. V2 uses canonical byte comparison
for journal equality/difference, and includes a fifth targeted regression. No
case is dropped or reclassified. The next rehearsal must use a new clean commit
and new external directory.
