# Improved S4 qualification final status

## Result

The registered improved-S4 mechanics candidates are closed without a
production selection:

- Candidate A: `NO_GO_CANDIDATE_A_DISCRETE_PAIR`;
- Candidate B: `NO_GO_CANDIDATE_B`;
- Candidate C: `NO_GO_CANDIDATE_C_QUOTIENT_INF_SUP`;
- published rank-four constraint: `NO_GO_PRODUCTION_RESTRICTION_UNCHANGED`.

The release terminal is therefore
`NO_GO_PRODUCTION_RESTRICTION_UNCHANGED`.  The legacy `ShellElement` remains
the production default, with no selector, serialized formulation token, root
export, or activation path.

## Evidence boundary

This closeout binds the exact canonical outputs and independent reviews at
Candidate-C commit `bdc040adb1e87ccd58443d72cbb224eb8d1fb8d6`, tree
`0842801fa32d62cf276f2e0f3cda4d6f3f4bfe2d`.  The machine-readable status is
`docs/reference_cases/s4_improved_qualification_final_status.json`: 3,943
bytes, SHA-256
`E3E8F3AA2DD6BA4193358AEDFC7F01889A80544313041531A1840218C09C29C1`.

The result closes only the formulations registered and evaluated by the
accepted A, B, and C contracts.  It does not assert that every possible
coefficient-free shell formulation has been exhausted, nor does it reinterpret
Candidate B's historical `BLOCKED_PRIMARY_SOURCE_UNAVAILABLE` stage status.

## Qualification conclusions

- A1 removes two energetic directions and fails its exact flat-rank screen.
- A2 has a nonzero multiplier left-kernel on the registered clamped patch.
- Candidate B fails five registered energetic/rigid/exact-gauge gates.
- Candidate C removes exact multiplier nonuniqueness but retains positive
  quotient near-kernels with exact `O(n^-2)` inf-sup upper bounds.

No additional precision, nonlinear, performance, or production execution can
change those registered terminals.  Any further improved-S4 attempt requires a
new, preregistered program based on either new physical material parameters or
a different discretization with a mesh-uniform stability proof.

## Verification

The accepted 84-test qualification matrix plus the single fail-closed closeout
test passed together: `85 passed`.  The closeout test validates the
canonical outputs, terminal pointers, production source identities, legacy
constructor defaults, shell aliases, root exports, and dormant release policy
without importing the production package.

## Preserved restrictions

No production source, public API, selector, serialization, dispatch, package,
workflow, penalty, stabilization, `C^T C`, finite-rotation, nonlinear,
Candidate-A/B rerun, remote push, publication, or cleanup action is part of
this closeout.  Existing qualification evidence remains preserved.
