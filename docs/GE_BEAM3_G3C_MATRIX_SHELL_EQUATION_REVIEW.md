# Independent frozen matrix-shell equation review

Subject f99f1fb2c59780791f0907defabffe56d7e7da65, tree
23d21454a31d9020ab976d139f8454a78b59066a. Reviewer
`/root/g3b_independent_review`, independent separate reviewer agent explicitly
approved by the user. No candidate files changed and no mechanics executed.

Accepted for private local implementation/testing preparation only, with no
new correctness finding in that equation scope. The accompanying canonical
five-key review defines the narrow acceptance boundary. Its empty findings do
not close the explicitly retained conventional finite Q4 PHYSICAL_RECOVERY
obligation or the B2 clamp-domain gap.

## Scope and mathematical review

I read the complete contract, its canonical JSON, inert auditor and eight-test
suite. I verified the copied Q4 assessment against my original byte-for-byte.
The complete matrix chart preserves the actual positional Procrustes extraction
and source Exp/Log derivatives while substituting owned matrix trial rotations.
Common-motion left action, passive conjugation, same-pose rebase, fixed physical
normal/port authority and the distinction between chart Hessian and spatial-row
tangent are correctly stated. Only the simple top Davenport eigenpair matters;
repeated lower eigenvalues are not spuriously excluded. The inherited fit-to-node
Log and increment domains are explicit and no shell all-pair restriction was
introduced. The force-weighted Hessian and chart-connection terms are mandatory.

The five Q4 channels match the source potential previously independently
derived: actual qualified physical, PL and hourglass baselines, added compatible
von-Karman membrane, and subtracted compatible linear membrane. Signed channel
work and Hessians must be checked against actual source responses. Different
nonlinear-center and mixed Equation-7 frames, engineering/tensor shear, director
transport and station coordinates are explicitly accounted for. The invalid
single-resultant interpretation is forbidden and the original recovery
obligation is expressly not waived. A negative-signed channel is not falsely
required to have positive contribution to the total energy.

I additionally inspected S3 V2D `_native_trial_response` (lines 1927–2099),
`compute_nonlinear_response` (2101–2137), stiffness/recovery selection and actual
station maps. With zero initial fields, virgin state and the specified scalar
elastic material, strains are linear station-operator images of local d;
the actual layer integration is elastic, and only the complete common-pose
pullback supplies geometric terms. Actual source PL remains separate. This
supports the contract's scoped S3 statement; it does not permit borrowing Q4
von-Karman operators or bypassing model-bound origin/candidate validation.

The immutable scalar facade and fresh family-specific virgin origins avoid
foreign origin/material history. Returned state stays in its actual local-d
convention and is detached, not a graph commit. Later prepare-all, atomic
publication, cancellation protection and authenticated restart are not
pretended to follow from these local checks.

## Independent validation

Executed exactly the requested standard-library-only command:

    C:/Python/Python314/python.exe -I -S -B tests/test_ge_beam3_g3c_matrix_shell_contract.py

Eight tests passed in 0.648 seconds (process 0.880 seconds). They cover authority,
canonical parsing, duplicate/nonfinite rejection, exact typed frozen scope and
channels, hash mutation, rational polynomial first/second variations, the
single-field algebraic non-equivalence and cancellation of linear terms.
These are inert source/design checks, not numerical shell qualification.

My separate `audit_g3c_matrix_shell_equations.py` passed in 0.372 seconds.
It uses one batched Git blob read for the 19 source and four payload bindings,
checks their actual immutable blob IDs/bytes/SHA-256, normalized working bytes,
exact five-path extent, clean tree and diff check. The contract is 5912 bytes,
SHA-256 9bd9cb74453a52af987a0256759402bb75041db363837b2d7dfcb6706deb8517.
The existing assessment remains byte-identical at SHA-256
b11496d49a348e077187d100574f9c90cbd835cf099e27739433e9febe5d057c.

## Next gate

Implement the private local adapter in a successor, freeze the complete source,
runner and test inventory before bounded mechanics, and independently check the
actual chart/work/recovery channels at all registered local cases. Do not use a
local pass to skip any of the five graphs, 25 variants, state/restart or formal
confirmation obligations. Conventional finite Q4 recovery remains on the parity
register; full G3c, G4/G5, defaults and release are not accepted here.
