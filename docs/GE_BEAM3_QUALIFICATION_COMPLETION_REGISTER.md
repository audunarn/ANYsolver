# Beam qualification completion register

Controlling objective: complete the original beam qualification and full
P01-P32/U01-U10 legacy-parity matrix. Local success never substitutes for a
required graph, material, application or installed-artifact gate.

User-approved completion plan: implement/review/qualify private B2 and, if
necessary after source adjudication, a separately identified private Q4
successor. Preserve public legacy B2/B3, qualified Q4/S3, defaults and evidence.
Normal protected integration is authorized after acceptance; no version, tag,
publication or default change. No repeated approval needed inside this boundary.

## Gate register

| Gate | Current authoritative state | Required next evidence |
| --- | --- | --- |
| G1/G2/G3a | Prior scoped confirmations preserved | Integration impact regressions, not rewritten old acceptance |
| G3b | Prior scoped confirmation preserved | No substitution for finite mixed G3c |
| G3c original rehearsal | Accepted at 58ad1f0: ten histories, two resumes,142 negatives | Original packets remain original-operator evidence only |
| MO16 old B2 | Exact obstruction reviewed at62231d6 | New physical operator and graph identity |
| B2 successor equations | Contract ba97556, independent review ba784eb4...c204a4,7 exact tests passed | No more equation changes without review |
| B2 successor local core | Corrected implementation, numerical execution pending | Independent freeze review, smoke, two matching full8-node core results |
| B2 matrix adapter | Not implemented | Chart/transport/work/state tests under new identity |
| Q4 physical recovery | Open; naive single-field formula disproved, general impossibility unproved | Source derivation or explicitly reviewed private successor |
| Successor mixed graph | Not implemented | Every original MO01-MO18 obligation with new operator/restart identities |
| Full G3c | Not executed | Two complete375-history/3075-stage/3450-prefix cycles after prerequisites |
| G4 | Open | S19-S24 material/state/recovery/control/restart closure |
| G5 | Open | S25-S26 actual-route/installed-wheel integration |
| Additional domain parity | Open | Loads/inertia, spectral/transient, contact/activity, scale, consumers |
| Final integration | Not authorized by results yet | All rows accepted/verified unsupported, final wheel/reviews/protected PR |

Detailed P01-P32/U01-U10 authority remains GE_BEAM3_LEGACY_PARITY_MATRIX.md
and its canonical inventory. Detailed S01-S26 and MO01-MO18 remain their frozen
contracts. Unsupported requires demonstrated actual-route evidence, never merely
no test. Full finite-velocity dynamics and stable postbuckling are baseline-audit
questions, not automatically inherited legacy capability.

## B2 core correction record

Static independent reviewer /root/beam_core_review found three issues before
any numerical execution of6a612bd: representable axial energy/tangent lost by
premature squares at L=1e308; missing independent physical field/differential
oracle; and a relative comparator whose norm could underflow. The reviewed
candidate remains preserved. Corrections use exact intermediate ratios, an
independent full flexibility/Hq oracle and scale-safe comparison; regressions
also cover recovery units, actual rank/rigid modes and malformed/caller inputs.

Final dense binary64 entries below the subnormal range still round to zero;
the implementation does not claim arbitrary dense graph conditioning. Required
positive physical rigidities and nonzero energy that cannot be represented fail
explicitly. No floor or physics change was used to conceal the limitation.
Exact intermediate arithmetic is correctness-first; performance is unmeasured.

## One shared harness

run_ge_beam3_qualification.py registers b2-core initially. Gates are added only
with reviewed inventories, not arbitrary import/command input. It binds clean
commit/tree, all tracked source hashes, contract/reviews and capsule before
mechanics imports and after execution. Test checkpoints provide inexpensive
progress; no full manifest rebuilt inside mechanical loops.

Separate current inventories:7 exact design tests;9 inert runner guard tests;
2 selected smoke nodes;8 complete numerical-core nodes. Only the first two
inventories have run at this checkpoint. Fake-job guard tests prove timeout,
inactivity/memory drain, parent-exit/live-child handling, exception cleanup,
exclusive attempts and authority rejection. These are not physical process-tree
stress experiments or mechanics qualification.

Each invocation has a fresh UUID/exclusive external directory and single-use
worker marker. Scientific records contain actual inputs/outputs without times
or attempt IDs and are promoted only after complete passing inventory, unchanged
authority and terminal-zero tree. Failure retains diagnostics, never a canonical
partial. All8 core nodes must produce byte-identical full scientific results in
two fresh reviewed runs before scoped local acceptance. Full G3c remains false.

Bounds: one numerical thread,24 GiB,600 seconds including15-second drain reserve,
120-second inactivity, at most3 workers,1800-second wave. Current local lane uses
one child, not an unbounded multi-wave scheduler. No automatic retry. Corrected
candidates need independent review and fresh attempts. Freeze/main/public source
boundaries are checked before execution; old historical runners remain unchanged.
