# Independent G3b implementation and runner review

Reviewer: `/root/g3b_independent_review`, separately delegated after the user's
explicit approval on 2026-09-12. This reviewer did not implement the candidate,
make its prior corrections, author its tests, or execute its rehearsals. The
reviewer inspected source and records directly, rather than adopting the
implementation author's status as acceptance. No sub-reviewer was used.

Decision: accept the exact candidate and runner for formal confirmation of
`G3B_S16_MIXED_S18_REFERENCE_LINEAR_ONLY`; no unresolved findings identified.
The exact bindings and five-key attestation are in `review.json`.

## Inspection performed

- Read the workspace resource policy, complete review request, G3 graph/junction
  scope, mixed-owner contract, formal preparation contract and successor status.
- Inspected the entire mixed snapshot owner, all five concrete adapter sources
  (B2, B3, Q4, S3 V2D and weighted Q4), and the entire inert result schema.
  Checked reference-only admission, independent rotations, exact affine tie
  construction, Schur lift, complete internal equilibrium, reaction signs,
  work and native/nonnative physical recovery. The independent KKT test assembly
  uses fresh unchanged family operators, as explicitly permitted by the contract;
  it is not an independent requalification of those element equations.
- Inspected immutable byte publication, private ownership, lexical-lock cleanup,
  cached-factor identity, graph/operator/dispatch closure and callback boundaries.
  The entry correction reads rollback authority from the registry within the
  protected region and retains the original lock for cleanup. Captured method and
  property identities close the prior recovery-dispatch defect. Externally
  mutated graphs remain rejected rather than being silently resealed.
- Inspected complete restart preflight before owner construction, typed numeric
  and provenance schemas, canonical/duplicate/nonfinite rejection, all-entry
  shape checks, external digest binding, full result replay and failed-replay
  ownership cleanup. The scope contains reference snapshots, not finite pose
  or constitutive history.
- Inspected correction, owner, family/KKT and transport regressions. Confirmed
  coverage of real failed preparation, cancellation, recovery closure, entry
  access failures, forged publication views, early/late malformed journals,
  replay continuation, independent histories, translation-only work, reordered
  IDs/insertion, reference reversal and proper global transport. Negative tests
  do not confer positive rotational-adapter admission.
- Inspected authority, isolated dependency capture/verification, coordinator,
  worker lease, import-origin guard, scientific recorder and publication code.
  Checked exact clean candidate/input/environment/review binding before mechanics,
  isolated startup, disabled ambient pytest plugins, hash-bound committed sibling
  sources, numerical-thread settings and full input revalidation.
- Inspected the Windows process-job implementation used by the runner: suspended
  launch, assignment before resume, job-memory limit, kill-on-close, descendant
  CPU/memory accounting, tree termination/reaping. Coordinator limits are three
  children, 600 seconds each, 120 seconds of CPU/output inactivity, and 1800
  seconds for the wave. No automatic retry or canonical partial GO is admitted.
- Inspected strict separate inventory and test-phase validators, seven frozen
  packet hashes, all 180 correction-case record checks, cycle byte equality and
  exclusive same-volume hardlink publication. Formal mode requires the real
  review; rehearsal is explicitly nonqualifying.

## Independently checked evidence

The reviewer-authored read-only `audit_rehearsal.py` recomputed all 506 archive
file bindings and audited both raw cycles: leases/attempts, exact collected
node IDs and setup/call/teardown phases, process bounds and log hashes, actual
checkpoint text, imported-file membership, packet/case hashes and canonical
aggregate correspondence. It completed successfully in 2.480 seconds without
importing mechanics or running tests. Separate inventories were mb2 25, mb3 40,
mq4 42, ms3 45, weighted 54, transport 39, owner 60 and owner_corrections 180;
each lane's scientific bytes matched between cycles. No combined count is used.

The inspected rehearsal is 266617 bytes, SHA-256
`e435339dd641415786bb782895232951afa045aeeb297770e074b6d8f794c4c8`.
Its terminal remains `REHEARSAL_ONLY`; no formal aggregate exists in that archive.
Authority 62 and runner 21 are separate infrastructure inventories reported in
the preserved candidate status; they are not added to the mechanical inventories.

Read-only Git inspection confirmed HEAD and tree match the attestation, the
candidate is clean and `git diff --check` passes. Relative to the pre-G3b base,
the production-source extent consists solely of six added private G3b modules;
existing element mechanics, package metadata and workflows are unchanged.

## Acceptance boundary and next gate

This is implementation/runner acceptance, not formal confirmation. Two formal
cycles against these exact bindings and a subsequent evidence adjudication and
static closeout remain required. No finite mixed response, G3c shared-rotation
adapter, G4 material history, G5 public integration, general static/full legacy
parity, default activation, release or performance claim is accepted here.
Qualified Q4/S3 mechanics and existing defaults remain unchanged.
