# Independent G3b formal evidence adjudication

Reviewer: `/root/g3b_independent_review`, the separately delegated reviewer whose
implementation/runner review preceded formal execution. I did not implement,
execute, archive or author-adjudicate the formal campaign. The user explicitly
approved this independent delegation. No additional agent was used.

Decision: `ACCEPTED_G3B_SCOPED_CONFIRMATION`, with no unresolved findings.
This decision binds candidate `0fe7f8f0d740f4aebd926227e49fa40bedc2e32c`, tree
`e06b1374260769f2af23f362cccd8386479d33af`. Exact aggregate, raw-manifest and
prior implementation-review hashes are recorded in `confirmation-review.json`.

## Independent evidence work

I inspected the complete terminal evidence only after both formal cycles finished.
The reviewer-authored `audit_formal_independent.py` uses the Python standard
library only; it does not import candidate validators, project modules, pytest
or mechanics, and does not run a mechanical case. It independently checked:

- All 509 archived file byte counts and SHA-256 hashes, including the exact
  aggregate, original implementation review and author audit (the latter remains
  explicitly non-independent and was not substituted for this audit).
- The complete current tracked-input graph against the formal captured graph,
  exact clean candidate commit/tree and tracked-file inventory, isolated capsule
  file set, all dependency/runtime file hashes, and Git launcher/engine hashes.
- Every formal lease and exclusive attempt against the exact candidate,
  input graph, environment, inventory and accepted implementation review.
- Every ordered collected test ID and every setup/call/teardown report in each
  lane, rejecting missing, failed or skipped phases and nonzero process results.
- Every stdout/stderr hash, actual checkpoint text, process terminal state,
  resource result, and all 1342 distinct recorded import paths against frozen
  file content; no ambient source substitute was accepted.
- All seven preserved packet byte/hash expectations, complete 180-case correction
  inventories per cycle, accepted/rejected-envelope distinction, accepted-prefix
  preservation, continuation/replay equality, reference-only journal provenance,
  and correspondence of raw scientific records to the canonical aggregate.
- Byte-identical scientific results separately for all eight lanes between the
  two formal cycles. No combined test count was used.
- No failed/rehearsal canonical result in the formal output, and original
  `aggregate.json`/`aggregate.json.pending` equality and actual same-file hardlink
  identity before considering the independently copied durable archive.

The audit passed in 13.689 seconds. All 16 child process records were terminal
with zero remaining processes. Longest child: 167.0654594 seconds. Largest tree:
259784704 bytes, below 24 GiB. The original input-file creation and aggregate
write timestamps show a 574.318-second run span, consistent with the runner's
1800-second enforced wave limit; timing remains a diagnostic, not a speed claim.

Separate scientific inventories per cycle:

- mb2: 25
- mb3: 40
- mq4: 42
- ms3: 45
- weighted: 54
- transport: 39
- owner: 60
- owner_corrections: 180

Accepted aggregate: 266728 bytes, SHA-256
`9952c38878e6732cfa5011bcdc13d6e88539164536d0c4816c85d7c25e97927b`.
Raw archive manifest: SHA-256
`186f9c3d94ecbe5153fda4a1d733e07c59104815c3e254a2cbaa0b536990389c`.
Terminal: `PROVISIONAL_GO_GE_BEAM3_G3B_REFERENCE_MIXED_ELASTIC_STATIC_ONLY`.

## Static closeout review

I reviewed the separate closeout-test draft. Its initially incomplete nested
review-schema and manifest-link checks were reported to the author, corrected,
and re-inspected. The current draft checks exact review and scope schemas,
independent provenance, implementation/aggregate/raw-manifest cross-links,
reference-only boundary, manifest row schemas and aggregate binding. Those
draft issues did not affect the frozen candidate or formal execution inputs.
The author must still assemble the closeout artifacts and pass the single
static closeout node; this review does not claim an unexecuted test passed.

## Strict acceptance boundary

Reference-linear G3b S16/mixed S18 only; no finite mixed response, G3c, G4, G5,
full parity, defaults or release. Positive shared-rotation adapter admission,
history-bearing sections and public general-static integration remain separate
gates. Qualified Q4/S3/native/legacy element mechanics and defaults are unchanged.
The passing reference orchestration evidence must not be presented as complete
beam-domain parity or finite mixed-family qualification.
