# G3c mixed-owner design accepted; implementation next

Frozen design: 5385ebd21db406621627e632ecb3cc231cf02384, tree
20742d5ea1638e3723c6b9b23cd1c2abe6934c2b. Its isolated four-path contract
worktree remains clean and unchanged. The complete canonical contract has
11446 bytes, SHA-256
471d05d2db6c604c39e30320e279e3a8ccd47186cb549969a596a00c62d967e5.

Independent review accepts private implementation and bounded testing ONLY:
ge_beam3_g3c_mixed_owner_design_review_v1.json, 737 bytes, SHA-256
c7efca597db0739b83eeeda98f55dd714cea1c4a05e35dbfc6a7f269cc8c91e3.
The original canonical review and its 632-byte test receipt are copied unchanged.
The rationale is also preserved. Empty findings do not establish a graph result.
This accepted review is the successor authority for private owner implementation;
the frozen pre-review contract is deliberately not edited to toggle its flag.

Author static suite: seven passed in 0.570 seconds. Independent static suite:
the same seven passed in 0.551 seconds. Independent immutable-blob audit passed
for 28 source bindings, three payloads, exact four new paths and clean tree.
These are two executions of one seven-node static inventory, not fourteen tests
or formal mechanics cycles. No mechanics, historical rerun or resource request
was executed. No production/source/default change occurred.

## Implement next

Continue in the dedicated ge-beam3-g3c-mixed-owner-v1 worktree. Keep the
ge-beam3-g3c-mixed-owner-contract-v1 worktree at its frozen design commit so the
exact-extent static suite remains directly reproducible there. This handoff
contains review/status only and does not alter the tested contract files.

Begin the actual native/B2 pair bridge and one immutable-generation owner.
Use actual issued native contexts in disposable sandboxes, not a synthetic
material state. Use the accepted pure local adapter exactly once per chart.
Bind one authoritative binary64 pose and check the native subset exactly.
Assemble each joint's complete multiplier blocks once. Prepare every family,
state and journal before the sole publication swap. Start with J_B2_PAIR smoke
and injected late-failure rollback; then extend to B3/Q4/S3 and the loop.
Freeze the actual implementation/test inventory before any bounded mechanics.

The design now specifies genuine four-step zero-load preparation for the frozen
large common-motion diagnostics. Every step uses real equations and acceptance;
none manufactures or imports an accepted rotated state. Restart includes those
preparation prefixes and the unchanged load programs.

Complete all 18 named implementation categories, all 25 ordinary case records
and their required diagnostic probes, independent implementation review,
full rehearsal and two deterministic formal cycles before any G3c claim.
Conventional finite Q4 physical recovery and the B2 clamp-domain recovery gap
remain OPEN, not waived by signed work channels or this design acceptance.
G4 history-bearing sections, G5 integration, full-domain parity and public
activation remain unfinished and are not authorized by this private gate.

Each mechanics child remains limited to 600 seconds, 24 GiB per process tree,
one numerical thread and 120 seconds without CPU/output progress; at most three
children, 1800 seconds per wave, no automatic retry or partial canonical result.

Root main remains 74703a3202251edc0beafb21cd31f52c9304ceb8. The concurrent
ANYMESHER_05_COMPATIBILITY_CANDIDATE_PLAN.md remains untouched at SHA-256
645c92f567b542867b6c4f103ac0f4e58b6f06e9f96ba2950c7187d27a0ebbcf.
No push, merge, release, cleanup or qualification reclassification occurred.
