# Actual displacement-controlled lateral-onset search

Parentfc75f48ae7347487df4a315526b50b21ad7b6243. No element/controller/section,
mass, tolerance, geometry or default change. This uses the new native
translation-owned factor bridge on genuine controlled checkpoints.

Freeze meshes4/8/12 of the existing clamped span2, height.1 parabola and section
diag(1000,400,400,.02,.01,.02). For each search point start a NEW controller from
virgin state with preload targets(.01,.02,.03), followed by the requested drop;
omit the duplicate last target when the requested drop is .03. Do not transfer,
cast, reseal or extend another Programme's checkpoint. Each point owns its exact
complete programme and native accepted prefix. This bounded cold-start policy
avoids changing the controller's sixteen-target or120secondContext limits.

First point is a branch anchor at that mesh's preserved arc step7 crown drop.
Require native load, all nodal positions/frames, internal cell rotations and
retained resultants to agree with that accepted arc state within1e-11 normalized
error. The old checkpoint is comparison data only, never a controlled start.
Then evaluate actual drop .03 and .055; require lateral counts0 and1. Perform
exactly eight deterministic midpoint queries based on those accepted counts,
requiring each to be0or1 and retaining planar counts as well. There are eleven
points per mesh including the anchor. Each point is an actual native nonlinear
solve, not interpolation of saved equilibria or scaled reference field.

Bound the separate knot-resolved continuum neutral reference files by exact
hash, byte count, geometry/material identity and preserved endpoint bracket.
Do not recompute their base/root solves or import old discrete P5 outcomes.
Report native endpoint drop/load and conservative endpoint discrepancies against
both reference endpoints. Require the finest twelve-macro displacement and load
endpoint comparisons to be below2% before declaring this engineering comparison
passed. Coarse-mesh errors remain visible. A failed numerical/evidence check
blocks the wave; a failed engineering comparison is preserved scientific NO-GO,
not a process success relabeled as qualification. This observed crossing does
not prove first-root status, uniqueness or absence of unsampled crossings.

Producer publishes a complete checkpoint and physical factor packet only after
native/source guards. Independent inspection starts after successful full-tree
producer exit, authenticates its assignment/ready/receipt, verifies canonical
TranslationPencil fields and original-factor planar/lateral decoupling, then
uses Decimal80/100 zero-inertia audits. Require unchanged committed elastic
history, positive algebraic trace stiffness and physical mass; the controller
row is never a physical support/stiffness. Preserve all raw logs and partial
diagnostic checkpoints, no partial canonical aggregate and no automatic retry.

Run authority/search unit tests and the four-macro anchor smoke first. Rehearse
the remaining registered searches before two fresh deterministic full cycles.
Both complete science aggregates must match byte-for-byte; every launched worker
must be terminal before aggregate publication. A failed rehearsal is preserved
and never automatically rerun. One thread,24GiB/tree,600s/child,1800s/wave,
120sCPU-idle/nativeContext; at mostthreechildren concurrently, one permesh.
Measure progress/cost before finer work. No elapsed-time or tolerance expansion.
Full spatial postbuckling, material/state/solver parity, production opt-in and
objective beam-shell joints remain separate unresolved parts of the full goal.
