# S3 E4-PL V6U performance-only Stage 4B successor

V6U re-executes only the V6S performance classification after the accepted
V6T exact global-cache repair.  V6S modal and buckling passes remain immutable;
V6S itself remains a genuine NO-GO and is not reclassified.

The frozen workers are all-Q4, 10-percent mixed S3, and 25-percent mixed S3.
Each uses the unchanged N20 dispersed/alternating topology, one warm-up, eleven
assembly and production-solve measurements, and an independent RSS observation.
The unchanged acceptance limit is 1.10 times the all-Q4 median/RSS for every
mixed route.

Run two fresh cycles.  Producer concurrency is three and duplicate independent
checker concurrency is four.  Each child has one numerical-library thread, a
600-second wall limit, and a 24-GiB process-tree memory limit.  The complete
wave has an 1,800-second bound.  Outputs are exclusive and no automatic retry
is permitted.

Terminal precedence:

1. `BLOCKED_E4_PL_S3_V6U_PERFORMANCE_PROCESS_OR_EVIDENCE`
2. `NO_GO_E4_PL_S3_V6U_MIXED_PERFORMANCE`
3. `PROVISIONAL_GO_E4_PL_S3_V6U_STAGE4B_CLOSED_ONLY`

A pass composes with the accepted V6S modal/buckling records to close Stage 4B
for V2D and authorizes V6V packaging/restart/batching and activation-gap audit
preparation only.  It does not activate S3 or alter any default.  Q4 mechanics,
V2D mechanics, thresholds, and ANYmesh remain unchanged.
