# Same-state native/continuum instability comparison

Parent c34febc1b49ad563fa4720eaa2f169c53183b291.
Only this contract, the new research worker and its targeted test are added.
No production mechanics, section, recovery, state-law, default or version changes.

Bind the accepted precise-policy seed archive and the actual +/-0.0065 completed
continuation checkpoints; use the complete original one-target programme with
control node13, spatial z direction and crown node25 dead force (0,-1,0).
Bind the separately accepted full-spatial continuum trial-work archive at those
same displacement-controlled endpoint values. Exact hashes live in the worker;
no approximate hash prefixes or old +/-0.006 packets may substitute.

Capture with existing _ge_beam3_elastic_seed_modal.prepare and exact explicit
GE_BEAM3_RETAINED_GEOMETRIC_WORK_DECIMAL80_V1 model policy. The actual owner must
mechanically restore the original chain and reproduce its bytes before capturing
the unmodified element response factors. Do not advance any target, run Newton
continuation, enrol another seed, relabel records or change completed history.
The existing owner guard remains active throughout capture. Capture uses the
original 120-second owner safety boundary plus external process controls.

Capture all 24 elements, 49 nodes, 438 physical-plus-internal rotation coordinates
and 432 stationary resultant coordinates. Only 12 physical end clamps are removed:
426 free coordinates, including numerical control DOF74. Keep 144 internal spatial
rotations; retain the 141 massless nodal rotations explicitly in stiffness. The
remaining 285 coordinates have physical mass. Spatial dead-load Hessian is zero;
neither the numerical control row nor its auxiliary load column enters K.

Freeze full packet shape, ordered free/algebraic sets, internal layout, exact
seed/checkpoint identity, one completed target, physical boundary and packet hash.
Unit tests include resealed mutations, target/policy/hash changes, strict canonical
input bindings, and an actual small precise-policy endpoint capture versus the
full stationary Schur complement. Include the established scaled-inertia tests.

After positive capture smoke succeeds, run negative capture and both capture
replicas in three fresh processes. Require exact same-sign packet bytes.
Then check each signed packet at 80 and 100 decimal digits, twice each in fresh
processes (eight audits total, max3 concurrent). Existing scaled energy audit
constructs (L R)^T(L R)+(H+H^T)/2 without dropping entries or changing quadratic
work. It guards raw geometric skew at normalized1e-11; exact raw tangent symmetry
is not claimed. Positive diagonal congruence and front-aware signed factorization
must meet their original-coordinate 1e-60 reconstruction bound. Do not modify
pivots, remove negative modes or discard unstable algebraic coordinates.

Require byte-identical audit replicas at each sign/precision and equal negative
counts/pivot paths across 80/100 digits. Record the computed inertia rather than
assuming a desired count. Report whether a native negative direction exists at
each endpoint where the separate continuum trial found one. This establishes
same-state negative-sign agreement only: it does not establish equal continuum
inertia, eigenvalue/mode-shape equality, stable postbuckling, or a physical loading
path from rest. The continuum Ritz Gram is not physical mass, so its normalized
values cannot be compared as physical frequencies.

Guard clean frozen commit, Python hash and registered runtime versions before
scientific imports and at finalization; this is not a full environment graph
certificate. Every output exclusive and external. Positive smoke must pass before
launching the remainder. No consumed-root rerun or automatic retry. Child600s and
24GiB per tree; max3 concurrent; one numerical thread;1800s wave;120s CPU inactivity.
Eight audit outputs and four capture outputs must all be terminal before aggregate
publication. On failure, preserve raw diagnostics and completed packets but issue
no successful aggregate. Scientific sign disagreement is reported honestly and
does not authorize production or a silent tolerance/formulation change.

Independent author review, broader nonlinear material/state/solver/geometry gates,
installed public opt-in selection and objective beam-shell connections remain open.
NO_GO_PRODUCTION_RESTRICTION_UNCHANGED.
