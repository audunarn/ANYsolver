# Retained generalized native state: development repetitions passed

Tested freeze `c8c14870f1662202d42a8da2a6cfc9e099d31165`. The two new
private runtime modules were introduced at0b778e8; the successor only corrected
test serialization. Existing beam/shell mechanics and public defaults did not
change. Runtime files do not import research modules or legacy beam operators.

The new standalone controller owns immutable mechanical state, paired
generalized section origins/histories and ordered distributed-load targets.
It retains24 internal variables per element during global Newton and stages
the complete canonical accepted capsule before publication. Restart verifies
the externally supplied exact hash and replays accepted history from its saved
origins, without redoing load increments or advancing plastic state.
It returns a separate private accepted-state object; it does not mutate FEModel
state or register a new public element. This is not complete native solver parity.

## Separate inventories

The smoke was one passing port/restart node. The complete rehearsal and each
of the two fresh-directory cycles then passed these separate lanes:

| Lane | Tests | Canonical scientific files |
| --- | ---: | ---: |
| Straight/curved port and restart at ratios100,10,000,1,000,000 | 6 | 18 |
| Checkpoint corruption, cancellation, failure and input guards | 22 | 5 |
| Curved and connected plastic histories | 2 | 8 |

No skips. All scientific files in both cycles are byte-identical to the
rehearsal. Port geometry, frames and retained resultants equal the frozen
research outputs exactly. Paused/resumed and uninterrupted checkpoints agree
byte-for-byte. The plastic cases complete seven loading/unloading/reversal
targets with positive accumulated history, authenticated continuation and
unchanged recovery replay. This is a small generalized-ellipsoid material
test set, not full fibre/nonlinear-section qualification.

The guard lane tests rehashed changes to positions, rotations, resultants,
origins, histories, work, reactions, targets, schema and extra fields; malformed
JSON, boolean arrays and foreign models; interruption around publication;
failed iterations and restart rewind. It is not proof that every possible
direct-object misuse is rejected. Direct State-object ownership/recovery
boundaries require the next explicit adversarial review before public exposure.

## Runtime and evidence

The two-cycle wave finished in226.174seconds. Longest child was212.256seconds;
the highest recorded child peak was244,215,808bytes. These are diagnostics,
not benchmark claims. Three workers overlapped, with no fourth, all under
600seconds/24GiB/one-thread limits. The complete wave remained under1,800seconds.
All process trees are empty, output directories distinct, final frozen guards
passed and no worker was automatically retried.

Archive:
`C:/Users/AudunArnesenNyhus/AppData/Local/ANYrelease/ge-beam3-retained-state-c8c1487-20260908`.
Its20,724-byte manifest hash is
`A1D74083115373BA8345B6378534274545D8DE1198020B1DE9C88E28FA5ECD7D`.
The4,193-byte agreement hash is
`EFE694BEAE40F90AE25D4F908C0E01F11B3E433D70004CFA17FF88C6F7AE4EEC`.
The archive retains all original-copy logs, receipts, JUnit inventories,
checkpoint/recovery outputs, coordinator scripts and frozen source snapshots.
Every copy was verified against its original. The read-only audit independently
checked serialization, hashes, inventories, exact repetition, checkpoint pairs,
positive plastic history and process bounds; it is not independent mechanics
review. All original temporary outputs remain preserved.

The original test-writer failure remains archived under
`ge-beam3-retained-state-harness-0b778e8-20260908`; its806-byte manifest is
`FB2094741A594F499A25DB4DEA9BE28430D32D7CD7BDF75AAA13C50E60652606`.
Absent original checkpoint/result outputs were not fabricated. The failure
is preserved, not reclassified as a passing smoke.

## Remaining programme

First audit and harden direct State-object ownership and recovery misuse.
Then connect the accepted retained state to the existing precise factor-modal
path with explicit current-material interpretation and conservative-load
admission: spatial couples must not acquire conservative spectral authority
by accident. No history advance or implicit state-schema translation is allowed.

Broader independent nonlinear/curved/slender/postbuckling reference campaigns,
general fibre sections, objective finite-rotation shell connections, full
environment attestation, package isolation, performance and independent review
are still required. The earlier modal and force evidence remains immutable.
The complete goal is ACTIVE_INCOMPLETE, not narrowed to this controller.
Production qualification false; public API inactive; independent review PENDING.
NO_GO_PRODUCTION_RESTRICTION_UNCHANGED.
