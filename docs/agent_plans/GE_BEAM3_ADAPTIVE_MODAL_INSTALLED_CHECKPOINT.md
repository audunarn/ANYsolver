# GE-B3 installed adaptive control and loaded modes

This research/test-only checkpoint binds source candidate
`d73b86bb3a1cc688c1776dd3ebae7bf74ff1d260`, tree
`fbf653d3b10dd89ad010bc85b44da2b0c299c229`. It changes no production source,
physical operator, state law, tolerance, selector, default or old evidence.

## Isolated verification

One private wheel was built from the unchanged source snapshot. The harness
verified 60 source bindings composed from the preserved native, control,
adaptive and loaded-modal records. Dependency wheels were checked against the
existing offline artifact identities; no old resource request or execution
authority was reused. Build and installation used no network or production
environment installation.

The wheel was installed in a fresh external virtual environment. Two isolated
processes ran in distinct fresh directories, with repository search paths
removed and research/test imports prohibited. All loaded ANYsolver modules
originated under that installed target. Canonical UTF8/LF source hashes and
actual installed byte hashes were separately verified. The standalone small
fixture is a package correctness fixture, not an independent mechanics oracle.

Each process completed a real one-update Newton-budget diagnostic with seven
cutbacks, fifteen attempts and eight accepted substeps. Restart after the first
three consumed attempts reproduced the uninterrupted capsule. A resealed
attempt-size mutation was rejected. The loaded-modal check then replayed a
separate elastic loaded state, preserved its material history, and produced
the same pencil and modes after native checkpoint restart. An incorrect load
parameter was rejected. The pencil retained 24 element coordinates and a
12-column physical dynamic map, with six requested eigenpairs.

Both result records, both kinds of accepted checkpoints and the full modal
diagnostics were byte-identical between processes. This does not assert broad
source-runtime/installed-runtime equality: the installed environment used
Python 3.13.9, NumPy 2.5.2 and SciPy 1.18.1. Private controls and modes remain
unexported. No buckling factor or inelastic vibration branch is qualified here.

## Results and preservation

The installed test passed in **111.15 seconds**. The following static suite
passed **22 tests in 0.48 seconds**, with separate inventories: five new package
checks, five loaded-modal checks, five adaptive checks and seven prior
installed-control checks. Static checks did not rerun mechanics or rebuild
the wheel. All subprocesses used one numerical-library thread and a 180-second
timeout with child-tree termination on timeout, streamed external logs and no
automatic retry. These are small correctness checks, not performance gates.

The exact private wheel is 1,362,936 bytes, SHA-256
`e5a1c1780befe1ab641c02f4dd83f0e914a8161945e87e326fc7ab9a9fb82e43`.
Its unchanged `0.4.2` metadata is development metadata; the artifact must not
replace a published wheel. All 22 selected files (1,722,331 bytes) are preserved
under:

`C:/Users/AudunArnesenNyhus/AppData/Local/ANYrelease/ge-beam3-adaptive-modal-installed-20260906-e5a1c1780bef`

Every archived byte count and SHA-256 was verified from the normal workspace
context. Only verified relay duplicates and empty relay directories were
removed; original temporary outputs and earlier archives remain intact.
The canonical per-file inventory is in
`docs/reference_cases/ge_beam3_adaptive_modal_installed_evidence.json`.

## Remaining programme

Next are engineering modal/buckling references and nonlinear branch studies.
General section/load/constraint parity, straight/curved engineering
qualification, independent review, public integration and the objective
beam-shell connection remain required. This checkpoint grants no production,
release, default-activation or independent-review authority.
