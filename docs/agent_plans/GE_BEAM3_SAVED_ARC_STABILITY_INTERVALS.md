# Saved-arc stability intervals: bounded successor inspection

Parent closeout: `7a019d7` (saved-arc spatial inertia refinement). This successor
changes research capture/inspection only. Native mechanics, arc controls,
existing evidence, aliases and defaults remain unchanged.

Freeze meshes 4, 8 and 12 and intermediate saved steps 6, 7 and 9. These sample
the vicinity of the planar load turning point and the later portion of the
already accepted path. Reuse previously completed step-1/12 inertia evidence
through its exact archive manifest; never rerun those endpoint calculations.
Every intermediate checkpoint is bound by the immutable arch-repeat manifest.
No new continuation, interpolated equilibrium, root bisection or branch switch.

Capture performs native full-prefix replay under the unchanged Context limit.
Only after successful bounded process-tree termination may the separate
inspection child consume its hash-bound factor packet. The consumer validates
the complete ArcPencil schema and claims; it does not fabricate owned states.
Require exact original-factor planar/lateral decoupling. For each family, use
the existing independent Decimal original-factor inertia audit at zero shift
with both 80 and 100 digits. Require equal counts and positive algebraic trace
stiffness/physical mass. Do not recompute six-root spectra at these new samples.

An interval result reports adjacent sampled equilibria with different negative
counts, their actual crown displacement/load and checkpoint identities.
Unchanged counts do not exclude crossings and changed counts do not establish
uniqueness. Do not infer matched-displacement convergence, certified root
brackets, a qualified continuum comparison or stable postbuckling from this
sampling. Report decreases as well as increases; do not force monotonicity.
Unresolved pivots or other arithmetic/process failures stop without a partial
canonical aggregate. This is a diagnostic refinement, not new production authority.

Unit-test exact extent, hash/claim/process mutation rejection, strict evidence
ordering, changed-count interval extraction and no invented interpolated state.
Smoke one four-macro sample, then rehearse the remaining eight samples. After
rehearsal, perform two complete fresh-directory cycles with byte-identical
canonical packets, science and aggregate; compare to rehearsal too. All launched
workers must terminate before publishing any aggregate. No automatic retry.

At most three child processes, one numerical thread each, 24 GiB/tree, 600 s per
child, 1,800 s/wave, unchanged 120 s CPU inactivity guard. Source freeze precedes
workers. Capture and inspection have separate deadlines/receipts. Raw logs,
receipts and timing diagnostics remain external and immutable. Independent
review and the complete GE-B3 goal remain pending; no full qualification claim.
