# Active-plastic curved arc state-lifecycle gate

Base655892458fbc1d0ecdbabba1e3e8fca1802aac80 closes the actual one-macro smoke.
Bind its exact status/archive/checkpoint through the new standard-library
protocol. Preserve all prior smoke, onset, failed/coarse and source evidence.
No native mechanics, section law, quadrature, load, tolerance, state-owner,
public interface, existing beam/shell or default changes are permitted here.

## Cases and actual operations

Use the existing immutable GE_BEAM3_ACTIVE_PLASTIC_ARC_SMOKE_V1 fixture for
one macrocell and its two-connected-macrocell subdivision. Span2, parabola
y=.2(1-(x-1)^2), physical second director+z, clamped first node, same coupled
C/M tensors, yield.025, hardening.6, end spatial dead-force pattern(1,.2,.1),
steps(.05,.05,.05), length scale2, parameter scale1, sign+1, order4 per subcell.
Require one-macro full checkpoint to equal the archived smoke exactly; it is
comparison evidence only, never a starting state. All full/prefix solves start
virgin under their complete immutable programme.

For EACH mesh, execute exactly nine distinct fresh workers, in this order:

1. Full three-increment native solve; all increments actually plastic.
2. Virgin solve paused after the first plastic increment. The full checkpoint
   is bound for comparison only and is not passed into this solve.
3. Resume that exact prefix; require byte-identical full checkpoint.
4. Owned complete/prefix replay and all-station material/recovery capture.
5. Independent 96-digit material audit: 8 or16stations at each of three steps,
   always from ORIGINAL increment origins. Same explicit binary64 rounding and
   normalized1e-11 comparison as the accepted smoke. No ANYsolver/NumPy/SciPy
   imports in this fresh independent-check process.
6. Cancel at step2 before assembly, preserving the plastic step1 checkpoint;
   resume the returned checkpoint and require equality with the full solve.
7. Repeat the distinct cancellation case at step2 before a trial evaluation.
8. Repeat the distinct cancellation case at step2 before committing the step.
   Each injection MUST actually be reached. Missing callbacks cannot pass.
9. Reject thirteen actual checkpoint mutations: origins, plastic histories,
   accumulated history, predictor, programme, work, recovery hash, cursor,
   predecessor, record hash, external hash, duplicate keys and nonfinite data.
   For scientific mutations regenerate ALL descendant record/self hashes, so
   rejection cannot rely on accidentally stale outer hashes. Preserve the
   separate original owned accepted state/chain through every rejection.

All native accepts retain full physical residual, frame-chord gap and complete
Newton correction <=1e-11. The admitted cancellation cases return the exact
previous prefix. No manual truncation/resealing creates an initial state;
only the native owner's checkpoint method may emit an issued-record prefix.
Independent material auditing alone does not constitute a whole-beam tangent
or load-reversal proof. Positive arc steps plus initial sign do not implement
an explicit mid-history reversal schedule.

## Authority, bounds and deterministic cycles

Freeze these six research-only plan/case/protocol/worker/wave/test paths after
pure authority tests. Run a complete rehearsal first. Only its accepted full
evidence may authorize formal-A, and accepted formal-A may authorize formal-B.
Each phase has exactly19 workers: one lifecycle unit inventory and two
independent mesh chains of nine. At most two workers concurrently, fresh
exclusive external directories, one numerical-library thread per child,
24GiB/600seconds per child,1800seconds per wave, unchanged native120second and
CPU-inactivity120second limits. Native bounds include replay/capture time.
No automatic retry, changed limit or consumed-directory reuse.

Validate original request/input bindings, every successful empty-process-tree
receipt, exact report fields, captured material coverage and the complete
closed inventory. A process/evidence failure blocks with external diagnostics;
no partial aggregate. Formal-B's entire canonical aggregate must match formal-A
bytes before publication. Timings, absolute paths and receipts remain external
administration; the common scientific aggregate excludes them.

Successful disposition: PRIVATE_ACTIVE_PLASTIC_ARC_LIFECYCLE_PASS, with
production_qualified=false and independent_review=PENDING. It proves only
these two specified active-plastic state paths and their interruption/replay/
cancellation/rejection behavior, not full production qualification.

## Full remaining programme

Keep the full goal active: spatial postbuckled branches; unloading/reversal
policy where required; broad section/fibre/load/mass/recovery/state/solver/
restart/linear-transient parity; current retained curved native18external-DOF
integration; full environment attestation; independent review; installed
explicit selection; objective eccentric/curved beam-shell connections.
The earlier straight facade is not this retained curved/generalized core.
NO_GO_PRODUCTION_RESTRICTION_UNCHANGED. No main/push/merge/version/tag/release
or default activation follows from this private gate.
