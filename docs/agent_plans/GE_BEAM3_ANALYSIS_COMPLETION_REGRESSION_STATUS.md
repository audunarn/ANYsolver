# Fibre/accepted-state modal integration: verified progress

Frozen implementation: 20528dbfaa6b4138f0759e4e747ff18d7216b6c3,
tree b2797869bfd608f8130138589e5c838c144cd231. Six research/candidate integration
paths; no changes to existing B2/B3/Q4/S3 mechanics, public aliases or defaults.

## Results

The frozen definition, native analysis, generalized translation, physical-fibre
translation, model-owned modal and original translation-modal regression suite
passed **166 tests**, with zero failures/errors/skips, in 318.49 seconds.
The bounded supervisor completed in 320.557806 seconds, peak280326144 bytes;
session9049 exited0 and the entire child tree is empty. Earlier development
inventories remain separate: initial fibre-only27pass/1fixture failure, followed
by corrected integration65pass. No test count is combined across invocations.

One wheel was built from the frozen raw Git export, without build isolation.
It remains an unpublished private candidate with unchanged version0.4.2:

- File: anysolver-0.4.2-py3-none-any.whl
- Bytes: 1732023
- SHA-256: fb56f838eaf463cc61bcdc29490c73bbd724e1837371ad0b49078714e90e8a81

All300 installed Python source files exactly match the frozen export. Source
control and two fresh isolated installed executions produce byte-identical
checkpoints, prefixes, recovery, modes and completion records (eight files per
execution). These execute actual physical-fibre loading/unloading/reversal and
generalized accepted-state modes through NativeBeamAnalysis, including native
prefix restart; they do not merely import or inspect the package.

The passing package process used Python3.13.9 and hash-bound local wheels for
ANYmaterial0.2.0, ANYfileio0.3.1, ANYgeometry0.4.2, ANYmesher0.4.0,
NumPy2.5.2, SciPy1.18.1, Shapely2.1.2 and threadpoolctl3.6.0.
Repository paths and research imports were excluded, and every loaded ANYsolver
module was checked to originate from its intended external target. The process
completed in50.029031 seconds, peak175288320 bytes; session68087 exited0
and its process tree is empty. No dependency or current mesher checkout changed.
This is a package interface check, not ecosystem/dependency qualification.

The original Python3.14 package check failed because removing repository search
paths removed editable ANYmaterial. Its explicit dependency correction then
exposed editable ANYmesher. Both failures remain preserved and terminal, without
canonical successful output. The subsequent matching-runtime check used the same
unchanged solver wheel plus the existing frozen cp313 mesher wheel. Neither a
solver rebuild nor a mechanical correction was made for these environment fixes.

## Preservation

Archive:
`C:/Users/AudunArnesenNyhus/AppData/Local/ANYrelease/ge-beam3-analysis-completion-20528db-20260909`

153 files plus manifest21281 bytes; manifest SHA-256:
0040f1f696c9a2f05266eb42f68fb9f407fedc9947cfb573b239a5f63ad8f369.
The read-only audit checked exact result bytes, wheel hash, all installed source
hashes, JUnit counts and all six terminal process receipts. The archive preserves
frozen source copies, helper programs, retained test data, commands/logs/receipts,
the unchanged wheel and all three successful scientific output sets, including
the earlier failed attempts. Generated build/install trees remain in their
external temporary roots; they are not claimed as unique archived evidence.

## Completion boundary

This is progress, not full qualification or default activation. The current
curved core still needs physical-fibre modal integration, larger-model modal
consumption, mixed-family integration and the remaining engineering acceptance/
independent-author review before final explicit public selection. The original
public ge-beam3 facade is not silently redirected. Objective finite-rotation
eccentric/curved beam-shell connections remain required by the overall goal.

Continue closing these application-facing gaps; do not restart passed N32
campaigns or introduce a new open-ended exploratory wave. All current workers
are terminal. No automatic retry, push, merge, release, version or tag change.
Main and the user's untracked ANYmesher compatibility plan remain untouched.
Full goal ACTIVE. NO_GO_PRODUCTION_RESTRICTION_UNCHANGED.
