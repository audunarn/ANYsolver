# E4-PL-Q1U: Execution-Guard Completion and Local Qualification

## Authority and objective

Q1U restarts the blocked Q1T qualification without changing its mechanics or scientific semantics.

```text
study: study_e4_pl_q1u.q1t_frozen_mechanics_execution_guard_completion_v1
candidate: candidate_e4_pl_q1u.wg2020_numbered_frame_surface_pl_planar_linear_iso_v1
branch: codex/s4-e4-pl-q1u-execution-guard-completion
worktree: C:\Github\ANYsolver\.perf2-worktrees\e4-pl-q1u-execution-guard-completion
```

The mandatory parent is commit `850733cc9d2f9185d0a73c5fa6c0acd89067caba`, tree `3d5db4e5cc321d5b91351280fe0b0ecd279814ac`, parent `083044167f9826e9868851c2709017112bc7553d`, subject `docs: close E4 PL Q1T contract-authority block`. It is not dynamically rediscovered or substituted.

The supplied attachment is background design input only: `Downloads/S4_E4_PL_Q1U_EXECUTION_GUARD_COMPLETION_PLAN.md`, 28,573 bytes, SHA-256 `D33A7F9F6510C8B1166FA37AA7930BF1783071600B9001E446F2883068C834FF`. This committed plan is governing authority where the background attachment differs, including the eleven-terminal table with exact-oracle identity at precedence 4.

Q1U corrects only the four Q1T contract/guard defects:

1. use `environment.record_path`, never `environment.path`;
2. use `BYTE_IDENTICAL_CANONICAL_CERTIFICATE_PAYLOAD`, never the old common-payload token;
3. validate complete review records, exact reviewed inputs and reviewer independence, and empty findings;
4. validate the complete nested contract hash DAG.

The accepted Q1T exact reference and independent SymPy oracle remain the scientific sources. All shell mechanics, exact algebra, interval algebra, cases, tolerances, scientific predicates, certificate semantics, and production behavior remain frozen.

## Frozen inheritance and boundaries

Bind 82 direct authority rows: the 49 inputs already directly bound by Q1T, all 14 Q1T Commit-1 paths, all 11 Q1T Commit-2 paths, and all eight Q1T blocked-closeout paths. Each row binds source path, source commit and tree, Git blob, byte count, SHA-256, and Q1U classification. Q1T contract-stage evidence is retained only as `REJECTED_CONTRACT_STAGE_EVIDENCE`; it is not execution authority.

Reuse the committed Q1T environment record at `docs/reference_cases/e4_pl_q1t_environment.json`, SHA-256 `5461206324E7FC2A52B334CE736A512EE71313ED79181438047E3E20069A9746`. SymPy 1.14.0 remains the oracle equality backend, mpmath 1.3.0 remains import-only, and CPython 3.13.9/pytest 9.0.1 remain frozen. The external environment root stays outside every Git worktree.

The shared Q1U authority guard may perform only strict canonical JSON parsing, path/symlink rejection, hashing, Git authority validation, review-content validation, environment graph validation, inventory validation, clean-tree checks, and output-presence checks. It must finish before registered geometry construction, mechanics evaluation, SymPy environment activation, solve, or scientific pytest collection. Imports may load inert definitions only.

No change is permitted under `src/`, `.github/`, `.gitattributes`, dependency/package metadata, workflows, APIs, selectors, serialization, dispatch, recovery, production tests, or defaults. No push, merge, publication, cleanup, historical rewrite, or Q1B plan is authorized. `ShellElement` remains the default and every outcome retains `NO_GO_PRODUCTION_RESTRICTION_UNCHANGED`.

## Four-stage authority chain

### Commit 1 — PLAN12

Subject: `docs: preregister E4 PL Q1U execution-guard completion`.

Create exactly the governing plan, canonical independent plan review, baseline, 82-row inheritance manifest, allowed extent, canonical contract vocabulary, canonical review schema, mechanics-equivalence contract, authority contract, terminal table, test inventory, and the four-node preregistration test. The review binds the other eleven PLAN12 paths and must have verdict `ACCEPT_Q1U_PREREGISTRATION_NO_P0_P1`.

Before Commit 1 there is no Q1U guard, implementation, scientific test, execution contract, output, mechanics observation, or Q1B plan. One plan correction followed by independent re-review is permitted.

### Commit 2 — IMPLEMENTATION14

Subject: `docs: freeze E4 PL Q1U guard-corrected implementations`.

After Commit 1, create the shared non-scientific authority guard; Q1U copies of reference, oracle, and scientific runner; implementation manifest; mechanics-equivalence evidence; independent implementation review; five scientific tests; one toy exact-backend test; and one non-mechanics authority-guard test.

The guard exposes one entry point receiving runner ID, authority, contract and environment paths plus caller-supplied hashes and repository root. Reference/oracle profiles require every outcome path absent. The scientific-test profile requires promoted reference raw, oracle raw, agreement, combined output, and authority copy at bound hashes while status, report, review, completion, test result, and closeout test remain absent.

Mechanics equivalence classifies every copied top-level function, class, method, and significant assignment. Normalized AST uses `ast.dump(..., include_attributes=False)` and retains bodies, defaults, annotations, decorators, and literals. Only registered Q1T→Q1U identifiers and paths normalize. Scientific nodes must be `IDENTICAL_SCIENTIFIC_BODY`; guard/CLI nodes may be `AUTHORIZED_GUARD_DELTA`; identity constants may be `AUTHORIZED_IDENTITY_RENAME`. No node may be unclassified and no scientific node may be a guard delta.

Before Commit 2, only syntax/import, AST/schema, mechanics-equivalence, negative-guard, static reachability, exact review-fixture, and toy-backend checks may run. Required review verdict: `ACCEPT_Q1U_IMPLEMENTATION_FREEZE_NO_P0_P1`. One static correction and independent re-review is permitted. After Commit 2, implementation and test sources are frozen.

### Commit 3 — CONTRACT3

Subject: `docs: authorize E4 PL Q1U scientific execution`.

Create exactly the execution contract, contract review, and contract test. The contract has exactly these top-level keys:

```text
agreement authorization candidate_id commit_ancestry environment
implementation_inputs inherited_inputs output_absences plan_inputs
production_restriction review_authorities runner_inventory runtime schema
scientific_inventory study_id terminal_authority
```

Every review has exactly `findings`, `reviewed_inputs`, `reviewer_independence`, `schema`, and `verdict`. Accepted reviews require `findings=[]`, the exact ordered expected inputs, and exact independence fields. The contract review binds the contract and contract test. The contract records its review with `hash_binding=EXTERNAL_AUTHORITY_RECORD` and does not predict the future review hash.

The contract test validates exact nested schemas and types, duplicate/nonfinite rejection, Commit-1/2 ancestry and extents, all Git objects and hashes, the 82 inherited rows, environment/runtime graph, complete review contents, runner and scientific inventories, terminal predicates, output absences, agreement token, production boundary, and prospective Commit-3 parent, subject, and exact three-path extent. Required review verdict: `ACCEPT_Q1U_EXECUTION_CONTRACT_NO_P0_P1`. One contract correction and independent re-review is permitted.

After Commit 3, create the canonical external 13-key authority record and run each runner once with `--authority-check-only`. The record contains schema, authorization, candidate/study IDs, Commit-3 commit/tree, contract/environment/review hashes, review verdicts, and runner IDs. All three checks must pass at the real Commit-3 HEAD before registered mechanics.

### Commit 4 — OUTCOME11

Subject: `docs: close E4 PL Q1U local qualification`.

After authority-only checks, run reference twice in fresh external directories and require byte-identical raw output; do the same for the oracle; then require byte-identical canonical certificate payloads across implementations. Promote both raw outputs, agreement, combined output, and the authority-record copy. Run the five scientific evidence-inspection nodes exactly once; they do not rerun mechanics. Promote the test result, obtain independent scientific review, and create status, report, completion, and static closeout test.

No source, test, contract, tolerance, or case change is permitted after the first registered process starts. A post-execution backend, guard, agreement, or review defect is `BLOCKED_E4_PL_Q1U_ORACLE_OR_REVIEW` and requires a newly named successor.

## Exact extents and blocked routes

The exhaustive stage sets are `PLAN12`, `IMPLEMENTATION14`, `CONTRACT3`, `OUTCOME11`, and `BLOCKED5`. Exact blocked routes are:

- Plan/inheritance block: parent Q1T closeout, `PLAN12 ∪ BLOCKED5` = 17 paths.
- Implementation block: parent accepted Commit 1, `IMPLEMENTATION14 ∪ BLOCKED5` = 19 paths.
- Contract block: parent accepted Commit 2, `CONTRACT3 ∪ BLOCKED5` = 8 paths.
- Post-authority block: parent accepted Commit 3, authority-record copy plus `BLOCKED5` = 6 paths.
- Successful closeout: parent accepted Commit 3, exact `OUTCOME11` = 11 paths.

Rejected reviews use exact `REJECT_*_P1` verdicts. Honest blocked reviews use `ACCEPT_Q1U_BLOCKED_CLOSEOUT_NO_P0_P1` and review only authority, precedence, evidence disposition, and repository boundaries. Missing scientific output is never fabricated.

## Tests and terminal calculus

Keep inventories separate:

- Q1T baseline: its single static closeout node only.
- Q1U preregistration: four plan-only nodes for baseline/inheritance, stage routes, vocabulary/review/terminal schemas, and production/absence boundaries.
- Implementation: one toy exact-backend node plus four non-mechanics guard nodes.
- Contract: complete hash DAG, mutation matrix, and three runner negative guards.
- Scientific: exactly five inherited nodes covering frame/fields, actual 38-field algebra, 224-station recovery, global supports/reactions, and terminal/agreement evidence.
- Closeout: one static node with no mechanics rerun.

First-match terminal precedence is:

1. `BLOCKED_E4_PL_Q1U_BASELINE_MISMATCH`
2. `BLOCKED_E4_PL_Q1U_INHERITANCE_MISMATCH`
3. `BLOCKED_E4_PL_Q1U_PLAN_AUTHORITY`
4. `BLOCKED_E4_PL_Q1U_EXACT_ORACLE_IDENTITY`
5. `BLOCKED_E4_PL_Q1U_IMPLEMENTATION_IDENTITY`
6. `BLOCKED_E4_PL_Q1U_CONTRACT_OR_NONDETERMINISM`
7. `BLOCKED_E4_PL_Q1U_ORACLE_OR_REVIEW`
8. `NO_GO_E4_PL_Q1U_LOCAL_ALGEBRA`
9. `NO_GO_E4_PL_Q1U_PATCH_OR_COVARIANCE`
10. `UNCLASSIFIED_E4_PL_Q1U_LOCAL_PLANAR_IDENTITY`
11. `PROVISIONAL_GO_E4_PL_Q1U_Q1B_PLAN`

The provisional GO authorizes only later preparation of a separately reviewed Q1B plan. It does not authorize Q1B execution, DNV qualification, assembled stability, locking, production registration, or any API/default change.
