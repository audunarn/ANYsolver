# Native spatial-couple restart development closeout

Freeze `ed8fcb6511e2c5e719acdf8490930b4b3325738c`, tree
`5f73889eb22fc6d2e5288fa942fd257c6f3ded22`, passes this private development
gate. The five-path implementation extent is recorded in the status. Existing
physical operators, material laws, public routing, B2/B3, Q4/S3, defaults,
versions, and historical qualification evidence are unchanged.

## Separate inventories

- Initial straight smoke: 2 passed, 67 deselected; 15.440 seconds.
- Complete rehearsal: 88 passed; 212.396 seconds.
- Prior spatial-couple regression: 18 passed; 148.812 seconds.
- Prior line-only restart regression: 57 passed; 231.310 seconds.
- Frozen cycle A: 88 passed; 169.193 seconds.
- Frozen cycle B: 88 passed; 165.190 seconds.

The smoke preceded the added strict-mutation/deadline tests. Both complete
replicas used the final clean freeze and passed clean-head/configured-runtime
guards before and after execution. Every child used one numerical thread,
24-GiB process-tree memory, a 600-second wall cap and 120-second CPU-inactivity
watchdog, and exited with zero active descendants. At most three children ran
concurrently. No retry was needed or made. Times are diagnostics, not speed
qualification. The runtime guard is not a complete dependency-graph authority.

The couple regression retains the earlier deliberate combined-load overflow
warning before its required rejection. It is preserved, not hidden or counted
as a physical qualification failure. All six inventories exited successfully.

## Authenticated history and actual solver continuation

The new schema binds both line-force and fixed-spatial nodal-couple constant/
proportional patterns and segment parameters. It retains the complete native
material/pose accepted chain, exact predecessor/origin/seeds and multiplicative
rotations. Free equilibrium subtracts each nodal couple once globally, including
connected junctions. Physical recovery remains the unchanged section-owned
recovery; applied couples are not injected into section resultants or a
fictitious conservative potential.

Cases are straight elastic, curved coupled plastic, and two connected curved
plastic elements. Every typed half-load checkpoint resumes through actual
global Newton to an accepted state byte-identical to the uninterrupted two-step
solve. Unloading creates a new authenticated segment and preserves accumulated
plastic history. Failed continuation retains the accepted state byte-for-byte.

All 123 canonical scientific files match between A, B and the complete
rehearsal. The 24 prior couple files and 84 prior line-only restart files match
their preserved archives byte-for-byte, at historical freezes 3c3731b and
2873413 respectively. These are regression comparisons, not independent
mechanical-oracle qualification.

Maximum free residual norm across the three cases is 2.3937237282894364e-14;
maximum recovered work discrepancy is 5.551115123125783e-17, both below 1e-11.
Recovery has 8, 8 and 16 stations for straight, curved and connected cases.

Mutations reject changed/missing moments, absent nodes, wrong load parameters,
line data, origins, seeds, predecessors, skipped epochs, changed rotations,
missing states, loaded genesis, booleans, wrong policy/schema, qualification
escalation and overflowing norm scales. Preflight rejects duplicate/nonfinite/
noncanonical JSON, wrong external hashes, wrong constants and changed supports
before solver entry. Line-only and couple-bearing schemas reject cross-use.
Missing hashes, foreign live checkpoint bindings and deadlines fail closed.

An external checkpoint hash is mandatory: resealing a different complete valid
history cannot prove it was authorized. No raw-state bypass was introduced.
The descriptor's restart permission denotes this private typed path only; it
does not grant production or conservative spectral qualification.

## Preservation and remaining work

Archive:
`C:\Users\AudunArnesenNyhus\AppData\Local\ANYrelease\ge-beam3-native-spatial-restart-ed8fcb6-20260907`.

There are 524 data files plus the canonical archive manifest: all six run
inventories, raw scientific packets, stdout/stderr, XML reports, exact test/
audit commands, frozen sources and the derived audit with supervisor records.
All byte counts and SHA-256 hashes were verified after copying. The manifest
is 83,612 bytes, SHA-256
`CFCFAD54209191FCBD4C0213660416CE0A807DEDB2F6BAE342965F3E58905998`.
Original run directories and historical archives remain intact; only the
separately verified transfer duplicate may be removed.

Independent review remains PENDING; production qualification remains false.
Next is source-bound distributed-couple work and its generally nonsymmetric
internal Jacobian/Schur reduction. The conservative Hessian-only boundary must
not be reused for an internal nonconservative couple without deriving and
checking the spatial connection terms. This gate did not implement that work.

General nonlinear section/path parity, consistent mass, modal/prestress/
buckling and slenderness/curved engineering qualification, independent review,
package/public integration and an objective beam-shell connection remain
outstanding. Main and the user's concurrent ANYmesher plan remain untouched.
