# N32 extended native branch and lifecycle gate

Parent0d1ccfa2bd23ca3d5c8e75b907e0088a840fc605. Exactly three additive research
paths: this plan, ge_beam3_n32_branch_continuation.py and its test. No production
or private mechanics/solver/state implementation changes. No tolerance/default/
section/load/mass/recovery changes. Preserve all previous evidence.

Bind original N32 owner-prefix manifest
7abaafe452adc09cd5258256c68231655d6970af628083fb63c7c2ab6934d3e4 through the existing
strict loader and its accepted original equilibrium chain. The original seed bytes
remain unchanged: plus0a6ffaf93e9044e2458c6a3088da349cdfe49d0febad1b219c31d1a04f45ffa6,
minus32a3cf1e904490257d25dce15c587c5aaf9adec096c5937bafa25c79dbffc68d.
The old unused programme/checkpoints remain immutable and are NOT relabelled.

Freeze targets(+.0075,+.010,+.015) and the corresponding negatives, starting at
the native accepted+/-0.0065equilibria. This expands the displacement range and
tests several real Newton advances; it is not a request to find a stable answer.
Retain32elements/65nodes/1158retained coordinates, the exact precise-geometric
policy, quarterspan node17zcontrol, crown33negativeYdead force, end clamps,
section and all original iteration/backtracking/rotation/correction limits.

Create a NEW owner.Context programme identity from the exact seed, authenticate
its native equilibrium and require byte-identical source mechanical/recovery
fields. The new programme has three frozen targets. Never substitute continuum
guesses for its later advances, reseed between targets or reinterpret old records.
Advance each target in a fresh process with owner.solve(stop_after=target),
passing the actual previous accepted checkpoint and its SHA. Native restore
replays the full prior chain before solving; no context timer is reset.
Targets1/2 return paused; target3 returns completed. Require contiguous
record hashes, actual physical targets, unchanged virgin histories, and all
native residual/correction metrics<=1e-11. Save genuine accepted prefixes on
failure; unexecuted targets must never appear in canonical evidence.

At positive target2 exercise cancellation at before_commit and committed.
Before-commit must return the exact target1capsule; after-commit must return the
ordinary target2capsule. Resume the ACTUAL cancelled-before output to target2
and require the ordinary target2capsule/state. No consumed worker is retried.
Cancellation is an explicitly registered separate lifecycle operation, not an
automatic failure retry. Fresh native-owner replay/recovery at target3 must
reproduce original checkpoint bytes and leave accepted state unchanged.

Wave sequence: plus-a enrollment and target1smoke first; only after success
enroll the other three signed replicas and advance their target1. Then advance
all four branches target2 and target3; run three positive cancellation/resume
operations at target2; replay all four completed target3branches. Total23
planned worker processes. At mostthreeconcurrent per dependency-ready stage,
one numerical thread,600seconds/24GiB per tree,120second native context and
CPU-inactivity,1800second full wave. No automatic retry, no extended limits,
no source/input mutation, no reused root. On any failure, preserve all launched
terminal receipts and do not launch later dependent stages.

Same-sign replicas must have byte-identical checkpoint/result/completion files
at each same operation/target and byte-identical final recovery. All23 processes
must be terminal before canonical aggregate. Every new result binds exact
source commit, programme, seed/prior/output hashes. Complete canonical evidence
is permitted only after actual advancement, lifecycle equality and final replay.
Strict JSON parsing rejects duplicate/nonfinite and noncanonical data using
the unchanged registered parser. Lifecycle tests reject wrong cursor/events,
unregistered operations, old programme substitution and capsule mutations.

This gate establishes native elastic continuation/lifecycle at three new targets
only. It does not establish independent continuum accuracy or stability there,
loading from rest, plastic/fibre continuation, broad solver/geometry/slenderness
parity or independent-author review. Previous+/-0.0065states have one negative
direction; stable postbuckling is not assumed. Next construct independent
reference/stability comparisons for actual new states if this gate completes.
The full beam and objective eccentric/curved finite-rotation beam-shell goal
remains active. No B2/B3/S3/Q4/default, selector, tag, version, merge or release.
NO_GO_PRODUCTION_RESTRICTION_UNCHANGED.
