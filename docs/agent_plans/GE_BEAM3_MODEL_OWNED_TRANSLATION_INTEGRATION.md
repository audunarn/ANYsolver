# Current-core model-owned continuation integration

Parent e2a92d88ebfb2b05906e60222d31123977fe4011. Close actual integration gaps;
do not rerun the passed 23-worker branch campaign or change its mechanics.

Definition V2 explicitly binds the existing precise-geometric arithmetic policy
and reconstructs the exact same generalized operator. Definition V1 bytes and
reconstruction remain unchanged. Unknown policy, cross-family use, extra/missing
fields or policy loss reject. Neither version transports accepted history.

NativeBeamAnalysis gains retained translation solve, native replay/adoption,
physical recovery and validated prefix extraction. The ordinary reference-origin
and elastic accepted-seed owners remain separate. A new analysis envelope binds
the complete definition/inertia graph, exact programme, workflow, seed hash and
unchanged backend bytes. Import requires original native mechanical replay; no
foreign state is resealed. Force envelopes and translation envelopes cannot be
interchanged. Fresh restart must use the same seed authority for seeded paths.
Observer mutation is checked before/after callbacks; concurrent analysis fails.

An explicit retained_refinement=True constructor option permits the already
tested 32-element retained domain and is included in the graph identity. Default
construction retains the previous 16-element bound and graph identity. This does
not enlarge older force/modal driver bounds. New controlled operations use the
tested local-validation policy, preserving original 120-second context limits.
All public aliases/defaults, legacy mechanics and qualification flags stay unchanged.

Rehearse exact definitions and old force routes plus new continuation, cancellation,
prefix/restart/recovery and mutation tests. Freeze the candidate after success.
Then run two fresh isolated model-owned replays of the accepted positive N32
three-target packet, reconstructing all 32 precise elements and physical inertias.
Require exact original backend, first-target prefix and physical recovery bytes;
require byte-identical output files between replicas. This is integration evidence,
not a new branch solve or independent continuum proof. Do not fabricate absent
outputs if a process fails.

Each worker: one numerical thread, 600 seconds/24GiB, 120-second CPU inactivity;
at most two replay workers, 1800-second wave, exclusive external directories and
no retry. Keep source/environment checks before mechanics and at finalization.
Retain prior immutable archive manifest
ea2f8dab163225f4c2b96b9e849ba50ca0048779f7f7b2abc396dd55f4a00ed5.

Remaining full goal includes other current-core section/control/modal parity,
reference/stability coverage gaps, independent review, installed explicit public
selection and objective eccentric/curved finite-rotation shell connections.
This step is real solver integration but cannot alone authorize activation.
