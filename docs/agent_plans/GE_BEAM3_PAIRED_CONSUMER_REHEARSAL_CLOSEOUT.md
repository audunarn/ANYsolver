# Consumer rehearsal and legacy scalar discrepancy

Freeze a9430a24cdbdf6c410d6799ca7c1d0d6bde60b4e, tree
cf1b82c2e0b6117d34ed0caef117bbb17adfe267. Two added research-only paths.
No production module, mechanics, material, state, recovery or default changed.

Separate inventories: six local tests/six records passed; each of the three
continuum tests/eight records passed; each of four buckling tests/23 records
passed. All nine process invocations including collection are terminal.
All source guards passed. Maximum concurrency3, longest child374.128seconds
before cleanup, peak under291MB. No retries, no deterministic repeats launched.

Local checks cover free-body rigid subspaces, shared trace elimination,
proper-frame/reversal covariance and accepted loaded-state history. Finest
continuum errors are1.15195%,0.89134%,0.94339%; grouped correlations exceed
0.9862. The independent reference records are byte-identical to the bound
previous campaign. Actual preload paths include22 signed spectra per mesh.
All88 accepted-state hashes, loads and equilibrium checks match the previous
dense-backend baseline, as do all four final critical brackets. Finest Euler
error is0.0339508%, below the unchanged2% engineering gate.

The archive audit added a comparison of old versus new scalar eigenvalues
at1e-11 normalized difference. That assertion failed; preserve audit.py and
its incident as failed, not passed or rewritten. A separate read-only
observation checked all88 points:19 differ by more than1e-11, maximum
4.92666e-11 at N8 point13. Signs, material histories, preload search paths,
critical brackets and engineering conclusions did not change. The initial
audit's pointwise numerical-equivalence assertion remains unproven.

Do not infer that either backend is more accurate from these differences
alone. Stop expansion into repeat cycles pending a saved-operator arithmetic
check. Compare old/new returned vectors against the original signed factor
chain and against the stored old dense operator, using exact supplied-binary64
Rayleigh quotients. Decompose the discrepancy into old eigenvalue extraction,
operator formation and paired-vector quotient differences. This requires no
new mechanical solve and must not change thresholds or historical evidence.

The external archive preserves every original/copy byte, raw proof and log,
frozen source, failed audit, explicit incident and separate full observation.
Its21528-byte manifest and observation hashes are bound in the status record.
The observation is not a passing substitute for the failed assertion.

Full beam and objective shell-connection goal remains active and incomplete.
No independent review, installed/public selection, general nonlinear/fibre/
solver parity or complete curved engineering qualification is claimed.
NO_GO_PRODUCTION_RESTRICTION_UNCHANGED.
