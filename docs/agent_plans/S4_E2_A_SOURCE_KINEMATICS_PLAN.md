# Candidate E2-A source identity and kinematic feasibility

## Authority and boundary

This proof-only wave starts from commit
`281ed90e148c125edbec27e7336a8f9f0df08edc`, tree
`1ee60da4717055f5cc1b37ff9369877bb1867861`, on branch
`codex/s4-e2-a-source-kinematics`.

The long-horizon program in the user-supplied
`S4_E1_REVISED_PROGRAM_PLAN.md` is bound as design input at raw SHA-256
`5BF221C0B75425E292D80EF59CA4B6613445DD0621664F9350043E3B5B9B3C68`.
It is not executed by this wave.  Candidate E1-RH is
`DEFERRED_NOT_RUN` and cannot block or qualify E2-A.

The registered identity is
`candidate_e2_a.wg2020_n7_k0_displacement_allman_q4_kinematic_v1`.
It is distinct from the immutable rank-deficient E1-A identity.  Every
accepted A, B, C, rank-four, E0, E1-A, and E1-R artifact and terminal remains
byte-identical.

This wave owns only content-addressed plans, derivations, reference cases,
oracles, outputs, reports, reviews, tests, and LF transport rules.  It may not
edit `src/`, public APIs, imports, exports, selectors, serialization, dispatch,
defaults, production code, or preserved untracked evidence.

## Two-tier baseline

The baseline is never represented as one live 110-test run:

1. The immutable E0 tier contains 94 ordered nodes at commit
   `87b639499187736c59d87bc4aa8e6bd7f819d28b`, tree
   `c01fd5cab7b63325e6cb5b70000f4586d4788563`.  Its canonical node list is
   9,842 bytes with SHA-256
   `29EF584E9B51E8420934A519B3C1E71BDD3082EFDC89DBADA4FCE0FFE8997B9F`.
2. The committed E1 tier contains 16 ordered nodes at the authority commit.
   Its canonical node list is 1,692 bytes with SHA-256
   `9835FB4580C886B52BFF5961A30CD78E921B5CEED92A918312149032748A7F63`.

The accepted E1 report's historical statement that 15 focused tests ran is
preserved exactly.  The successor records, without rewriting that report,
that the five committed E1 files now collect 16 tests.

## Frozen architecture

E2-A uses exactly the Wagner-Gruttmann `n=7`, `k=0` Hu-Washizu core and the
existing MITC4 transverse-shear rule.  EAS is not an alternative inside this
identity.  Surface integration is the registered positive 2 by 2 rule.

The Allman part must be a displacement-compatible enrichment with one common
operator `H(q)` for later load and mass derivations.  A strain-only field is
inadmissible.  The enrichment must couple absolute nodal drill to a discrete
in-plane chord/spin measure.  A rotation-only edge-difference lift is the
hostile E1-A control, not E2-A.

Before any outcome, freeze the node and edge order, orientation, local frame,
director, tensor/engineering orders, mixed fields, quadrature, condensation,
displacement operator, strain operator, and load-work convention.  Every
indispensable statement is tagged `P` (public primary source), `D`
(independently derived and reviewed), or `B` (background only).  Sestra is
`B` only and cannot set an equation, coefficient, threshold, or outcome.

If the source and exact derivation leave more than one non-equivalent
enrichment, the terminal is
`BLOCKED_E2_A_SOURCE_OR_FORMULATION_IDENTITY`.  Coefficients may never be
selected from ranks or benchmarks.

## Fatal exact screen

An independent standard-library exact oracle must not import candidate code.
On square and affine-parallelogram Q4s it proves or disproves:

* membrane rank nine on twelve in-plane translation/drill coordinates;
* full rank 18 with exactly the six physical rigid modes;
* no common-drill, checkerboard, or positive-mass zero-stiffness mode;
* pure common drill and translation-only in-plane spin are each energetic;
* their matching combined rigid state is exactly null;
* drill columns do not factor solely through the cyclic edge incidence;
* exact D4, reversal, frame, origin, and unit covariance;
* constant membrane, bending, and shear patch closure; and
* algebraic mixed/uncondensed and condensed equivalence.

The oracle also reproduces the committed E1-A edge-difference rank upper
bound of 17 as a hostile control.  Exact rational/radical evidence is
categorical; floating calculations are corroborative only.

## Extension-closure audit

No warped, curved, nonlinear, dynamic, buckling, locking, performance, or
E1-RH mechanics is executed.  A derivation audit must nevertheless identify
one coherent variational route from the frozen `H` and potential to warped
geometry, work-equivalent normal moments, finite rotations, geometric
stiffness, mass, loads, recovery, and section resultants.  Any later need to
change the frozen interpolation, mixed spaces, quadrature, condensation, or
work map creates a newly named successor.

## Evidence, resources, and terminals

The wave produces a governing plan, source registry, identity and formulation
derivation, exact cases and oracle, extension audit, emitted contract,
canonical output, qualification report, tests, and an independent review.
Two fresh oracle processes must emit byte-identical canonical UTF-8/LF JSON.

At most two scientific agents and one read-only reviewer participate.  No
high-precision shards, benchmark campaign, compiled optimization, or job over
30 minutes is authorized.  Work stops immediately on identity ambiguity or
an exact kinematic failure.

Terminal precedence is:

1. `BLOCKED_E2_A_BASELINE_IDENTITY`;
2. `BLOCKED_E2_A_SOURCE_OR_FORMULATION_IDENTITY`;
3. `BLOCKED_E2_A_ORACLE_OR_REVIEW`;
4. `NO_GO_E2_A_KINEMATIC_FEASIBILITY`;
5. `NO_GO_E2_A_EXTENSION_CLOSURE`;
6. `UNCLASSIFIED_E2_A_SOURCE_AND_KINEMATICS`;
7. `PROVISIONAL_GO_E2_A_LINEAR_REFERENCE_IMPLEMENTATION_PLAN`.

A provisional GO authorizes only a separately registered linear-reference
implementation plan.  Production remains
`NO_GO_PRODUCTION_RESTRICTION_UNCHANGED`.
