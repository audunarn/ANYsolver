# Frozen fine-mesh actual-onset narrowing

Base: clean closeout56a971326c9544017255263aa92e30046b3ca73f. This successor
preserves all prior failed/coarse searches and the accepted N20/N24 upper
probe. It does not change mechanics, sections, state ownership, quadrature,
geometry, loads, tolerances, deadlines, production routing or legacy elements.

Bind the upper status E200322FF23FF2D4B2649D550F4F1435D906B64AC984DA47E533823CBA16F99D,
its archive manifest675F9266E1F289B869C8F83C354387F8B2F37B054321D81AFC1EC2346D13CEE3,
all inherited lower inputs and both unchanged independent continuum root
records. Use their verified relative archive scientific paths. Old checkpoints
are bracket evidence only, never starting states for a different Programme.

For N20 and N24 start with actual observed [0.045,0.0456] count[0,1]. Evaluate
exactly three binary64 midpoint steps, updating left for count0 and right for
count1. Any other lateral count or failed authority/equilibrium/precision check
blocks the search. Report planar counts separately; no first-root or branch
uniqueness claim. Do not infer an equilibrium by interpolation.

Each new point begins virgin with the full Programme(.01,.02,.03,midpoint).
Four fresh child processes extend only the immediately preceding owned prefix
of this exact programme. A fifth captures physical factors and a sixth audits
the original exactly decoupled planar/lateral factors at Decimal80/100, all
four records' reflection, positive physical mass/algebraic trace and actual
equilibrium/load binding. The numerical control row is not physical stiffness
or mass. Preserve strict canonical encoding and all raw checkpoints/factors.

The final bracket must be <=0.000098 wide (three steps give about0.000075).
Compute the maximum relative endpoint error in BOTH drop and load against
every endpoint of both preserved continuum roots. Require strictly<2% for
both meshes. A complete failing comparison emits the genuine
NO_GO_FINE_CONTROLLED_ONSET_COMPARISON; no tuning or automatic expansion.
Success is PRIVATE_FINE_ONSET_ENGINEERING_PASS, not beam qualification.

Execute one full rehearsal first. Only an accepted complete rehearsal may
authorize formal-a; formal-b requires accepted formal-a. Each phase has a
fresh exclusive external root, up to two concurrent mesh chains, one numerical
thread per child, 24GiB per process tree,600s child/1800s wave and unchanged
nativeContext/inactivity limits. No automatic retries. Receipts must show all
37children (unit+36point stages) terminal with empty Jobs before acceptance.
Formal-b's canonical aggregate must match formal-a byte-for-byte; disagreement
leaves only diagnostics and no accepted aggregate. Source and full inputs stay
frozen for all phases. Raw timings/progress and absolute administrative paths
are external, not common scientific fields. The whole goal has no new timeout.

Tests cover every deterministic count branch, endpoint/load failures, schema,
hashes, prior identities, source/archive mutation, missing phase authority,
scientific claims, factor precision/mass, full-record reflection, cross-Programme
replay rejection, strict JSON and exclusive outputs. Independent review remains
PENDING. No public qualification claim can be made from these private gates.

Full remaining goal includes nonlinear/plastic spatial continuation, postbuckled
branches, full state/material/load/recovery/mass/solver/restart parity, external
18DOF production integration, complete environment attestation, independent
review, installed explicit selector and objective eccentric/curved beam-shell
connections. Existing B2/B3/S3/Q4/defaults remain unchanged.
