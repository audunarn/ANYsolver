# Native distributed-couple integration closeout

Freeze `65a52db6a91338edeb40c95ff7e2a683bae11f83`, tree
`5aefe41a7a58776eb7a64a3e2c1fb11a8feaa155`, passes this private development
gate. The six-path extent is in the status. The physical retained operator,
distributed-couple static reduction and old native load/element sources remain
unchanged. Existing B2/B3, Q4/S3 mechanics, defaults, public selectors, versions
and historical qualification evidence are not changed.

## Separate inventories

- Smoke: 4 passed, 22 deselected; 12.836 seconds.
- Complete rehearsal: 26 passed; 165.970 seconds.
- Prior spatial-couple regression: 18 passed; 108.054 seconds.
- Q4 current-state ownership regression: 12 passed; 7.626 seconds.
- Legacy B2/B3 nonlinear regression: 5 passed; 6.422 seconds.
- Frozen cycle A: 26 passed; 172.228 seconds.
- Frozen cycle B: 26 passed; 170.823 seconds.

Every child exited successfully with zero active descendants. One numerical
thread, 24-GiB process-tree memory, 600-second wall and 120-second CPU-inactivity
watchdogs were used, with at most three concurrent children. All waves stayed
below 1,800 seconds. No retry was needed or performed. Both frozen replicas
passed before/after clean-head/configured-runtime guards. Runtime is diagnostic,
not a performance qualification or complete dependency-graph certification.

The existing couple regression retains its deliberate intermediate NumPy norm
overflow warning before the required rejection. It is preserved, not hidden
or treated as a new scientific failure.

## Actual native state and solver evidence

The new direct Element class has three nodes, 18 external DOFs and a distinct
distributed-load state schema. It does not inherit legacy beam mechanics.
Line-force/distributed-couple patterns are owned by each native trial scope.
The state binds exact pose, epoch, predecessor, accepted origin, internal seeds,
general static response and complete load provenance. Accepted response replay
reconstructs the loaded spatial Jacobian, internal lift and Schur tangent at
the stored internal state; it does not rerun internal Newton.

The native validator rejects locally valid states from a different load trial
or store registration. Post-assembly authority failure discards the owned trial.
Accepted state survives discarded trials and failed global increments exactly.

The actual force driver owns effective patterns during Newton, line search and
reaction assembly. It uses GENERAL factorization for the nonsymmetric condensed
tangent. Nodal reference-line forces are external once; distributed couples
remain load work of the internal cell rotations. No fictitious moment potential
or conservative spectrum authority is introduced.

Straight, curved elastic, curved plastic and connected curved-plastic cases
pass actual equilibrium. The plastic-section global cases have respectively
1 and 17 positive accumulated plastic rows, proving actual plastic increments.
Shared rotations agree exactly. Pure distributed torsion with zero nodal load
matches theta(s)=m*(L*s-s*s/2)/GJ and its root reaction at the three nodes to
1e-11. This is one analytical case, not general engineering qualification.

Maximum errors and observed tangent asymmetry:

- Independent trial reconstruction: 1.4210854715202004e-14, gate 1e-11.
- Native directional derivative: 1.3811668504458773e-10, gate 1e-7.
- Global free equilibrium norm: 8.090165827469247e-14, gate 1e-11.
- Maximum factorized asymmetry norm: 0.06940166124286236; GENERAL was used.

All 26 canonical scientific packets are byte-identical between A, B and the
rehearsal. All 24 prior couple scientific files match the preserved 3c3731b
archive byte-for-byte. Other regression lanes are reported separately above;
their passing tests are not a claim of complete legacy or shell qualification.

## Preservation and next work

Archive:
`C:\Users\AudunArnesenNyhus\AppData\Local\ANYrelease\ge-beam3-native-distributed-65a52db-20260908`.

147 data files plus the canonical manifest preserve all seven inventories,
raw packets, stdout/stderr, XML reports, exact commands, supervisor records,
six frozen source files and four unchanged authority sources. Every byte count
and SHA-256 was verified after copying. Manifest: 22,826 bytes, SHA-256
`3C72F3B5B725143A6C9F91EE91F8B869ED9598BB1E0385E658C206BFE895EEDB`.
Original run directories and all historical archives remain intact. Only the
fully verified transfer duplicate may be removed.

Independent review remains PENDING; production qualification is false. This
driver starts from virgin state only and rejects raw initial states and
unissued driver use. Old line-only and nodal-couple checkpoints cannot authorize
distributed-load restart. The next gate is the complete typed distributed-load
accepted chain, actual restart/unload and native physical recovery.

Combined nodal/distributed-couple parity, general nonlinear section/path
parity, consistent mass, modal/prestress/buckling and slenderness/curved
engineering qualification, independent review, public/package integration and
an objective beam-shell connection remain outstanding. The full goal is not
complete. Main and the concurrent ANYmesher plan remain untouched.
