# Retained arch point-isolation successor

Base: 457ad74cf56c6f1e40091d5fccc45879a420d5e1. The reference-identity
optimization and its resolved-method correction are preserved externally in
`ge-beam3-reference-identity-457ad74-20260908` (182 entries). Manifest: 23,450
bytes, SHA-256 D051CF8F46E78B71E26AB19E86214356CFE6CEF75833A412EDBBF2D8E40361AF.
Audit: 14,321 bytes, SHA-256 64269CE8A7C98980C19F21EBB992DA0612D14F1FBACE257B07A78699969BB624.
Corrected separate inventories: identity 44, arc 50, guard 15, reference 32.
The corrected arc's 12 scientific files equal the pre-correction and original
archive byte for byte. Pre-correction translation (15), plastic history (38),
and coarse arch (15) scientific files also equal their preserved predecessors.
These numbers are separate inventories, not one combined test count.

The original 8/12-macro full-path invocations remain failed due to the existing
120-second context guard. They are not retried. No accepted old partial state
is imported into this successor. Start each registered new path from virgin.

Implement research-only scheduling of the SAME twelve 0.01 arc steps, same
2/4/8/12 meshes, equations, loads, quadrature, section, references, tolerances
and acceptance rules. Each fresh child advances exactly one accepted step by
calling the existing arc.solve(stop_after=index), with full predecessor-chain
restore and an external SHA-256. No synthetic state issuance, skipped replay,
changed controller or increased deadline. A second fresh context replays the
new complete prefix and compares its final accepted state to the independent-
equation BVP. Pass the preceding BVP's exact saved reference fields so initial
guesses and scientific serialization equal the monolithic comparison.

Assignments bind the clean source commit, mesh, point index and complete prior
transcript. Before importing numerical/mechanical modules, validate canonical
JSON, regular non-reparse files, hashes, schema, full contiguous step history
and source/runtime identities. Revalidate at finalization. Outputs are exclusive
and external; incomplete/failed worker files remain diagnostic only. Publish a
complete aggregate only after every launched child is terminal and every
registered point has succeeded. A numerical finest-mesh contradiction is
recorded as such, not hidden by a process-failure label. Never promote partial
points to an accepted full-path claim.

Bounds: 600 seconds/child, 24 GiB/process tree, one numerical thread, at most
three children concurrently, 1,800 seconds/complete wave, 120 seconds CPU
inactivity, and unchanged 120-second mechanical contexts. No automatic retry.
Each geometry runs its ordered steps serially; geometries may run concurrently.
Checkpoints/progress are printed at initialization, full prefix restoration,
existing controller checkpoints, and final comparison. Source edits wait until
all children are terminal. This is Normal-token worker-owned scheduling, not
a resource-ledger authorization or qualification finalizer.

First run protocol mutations and disposable process-safety tests. Then a full
two-macro smoke path must reproduce ALL 15 archived scientific files exactly.
Only then start virgin 8/12-macro paths. If replay/byte/compute bounds fail,
preserve the failure and investigate rather than rerun or increase bounds.
Only a complete passing rehearsal permits two fresh deterministic repeats.
Finest errors must remain below 2%; no coarse mesh qualifies the element.
Full spatial stability, independent authorship review, broader beam parity,
and objective shell-joint qualification remain open. No S3/Q4/B2/B3 mechanics,
default, public route, version, package, merge or release change is authorized.
