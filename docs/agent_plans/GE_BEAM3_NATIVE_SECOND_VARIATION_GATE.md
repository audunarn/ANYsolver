# Native second variation on actual signed N24 branch states

Parent 43d7c59ee4ce91e12b8f05cfdc3997ff529d59a7. Bind both immutable original
endpoint seed/checkpoint pairs, factor packets and accepted exact negative-work
witness/checker outputs. No mechanical solution search or old qualification
campaign rerun. Replay the actual saved chain in its authentic elastic-seed
owner; no state-schema conversion or fabricated accepted history.

For original-coordinate kinematic direction x, lift retained resultants with
dp=L^T L R x. Map all 24 kinematic and 18 resultant coordinates per element
through the actual connectivity and internal layout, checking shared nodes.
Compare the captured coupling with the replayed native coupling bit-for-bit.
Require the native stationary-lift residual C dp-Jx <=1e-11 normalized. Compare
the full native mixed-Hessian directional work with the exact saved-factor work
to1e-11 normalized. Do not assume an approximate compliance factor is exact.

Use the unchanged native RetainedGeneralizedOperator.evaluate increment API,
holding accepted material origins fixed. Evaluate at zero and +/-h for exactly
h={1e-4,5e-5}; no changes to these steps after observing results. Include actual
spatial-dead-load incremental work (its Hessian is zero). Require negative scalar
potential second variation and negative analytic-residual directional work at
both steps. Compare the full870-coordinate residual directional difference with
the native mixed Hessian times the lifted direction, not just its scalar product.
The gate for potential, scalar work and full residual derivative is1e-7, each
normalized by max(1, norm of its reference). Retain all426 physical free
coordinates and all24elements; do not add the numerical displacement controller
as a physical support. Five trial points,120 native element evaluations per run.

No trial history commit or accepted-state update. Require every trial to remain
elastic-interior with unchanged material history. Verify original checkpoint,
accepted state and all192 recovered stations byte-identical before/after trials.
Failure blocks this gate, never repairs or reclassifies historical evidence.
This native finite-direction test is a different calculation, not independently
authored mechanical theory or complete branch qualification.

Unit-test actual small seeded-state variation and immutability, resultant lift,
mapping mutations, full-vector validation, false commits/control constraints,
wrong signs/work, nonfinite metrics and frozen step rejection. Freeze before
positive N24 smoke. If it passes, run negative and both fresh replicas, max three
concurrent. Require byte-identical same-sign canonical outputs and all process
trees terminal before aggregation. No automatic retries or partial aggregate.
One numerical thread,600seconds/24GiB per child, unchanged120second owner and
inactivity deadlines,1800second wave. Log progress at replay and each trial point.

No src, element, material, recovery, API, alias, default, version or dependency
changes. Full spatial postbuckling, physical loading path, plastic continuation,
broad parity, independent review, installed selection and objective beam-shell
connections remain open. NO_GO_PRODUCTION_RESTRICTION_UNCHANGED.

## Logging-only successor after the consumed failed smoke

Frozen 301820cc194377031e30a1be39f09e85bc72093d completed all five positive
trial points but failed the final 1e-7 directional-agreement gate. Runtime
114.0451894 seconds, child exit 1, empty terminal process tree. No negative or
replica workers ran. No native result or aggregate was created; final recovery
and checkpoint checks after adjudication did not run. Metrics were not saved,
so the failing quantity and cause cannot be inferred from that attempt.

Preserve all 12 files in external archive
ge-beam3-native-second-variation-301820c-20260909, manifest 1653 bytes, SHA-256
c796f3e3302088b02ded939784682b5b791fba1167915bf824373a943a09b7b8.
This successor changes logging only: save every completed trial potential,
element potential and full residual, plus baseline direction, Hessian product,
element work and lift errors. Save hash-bound UNADJUDICATED metrics before the
unchanged gate. Diagnostics do not assert successful later state/recovery checks
and cannot substitute for native.json or an accepted aggregate. Identify failed
metrics in exceptions. Use a new frozen revision, helper and exclusive external
directory; preserve the consumed attempt. No step, threshold or mechanics change.
Positive smoke still gates the signed replica wave; no automatic retry.
