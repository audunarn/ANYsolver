# Saved-factor remedy result and restart handoff

Frozen11b549a8748e26b6903a4bd8958662a7d258b7ce, tree
e9dca6e036caffa3e5f539a5926108331cde443e. Three research-only files added.
No production source, mechanics, recovery, state, aliases or defaults changed.

Separate inventories: one low-part arithmetic unit test passed, one record;
two saved-factor experiments passed, two records. No mechanical solves were
rerun. Saved roots and inputs remained byte-identical. This is neither a
new accepted spectrum nor reclassification of the original NO-GO.

Curved E at rho10000: paired kinetic-Gram error8.083e-16 and original signed
Ritz error2.795e-16. The high-only vector still fails at1.16104e-11.
Straight E at rho1e6: paired kinetic-Gram error7.851e-16 and signed Ritz
error1.809e-13. The high-only vector still fails at1.78313e-10.
All twelve roots are unchanged; neither clipping nor tolerance relaxation
was used. The two-pass kinetic reorthogonalization addresses the close-pair
cross-subspace error, and retaining expansion residuals addresses rounding.

Both workers terminated cleanly, maximum child3.415seconds before cleanup,
peak178565120bytes, one worker at a time, no retries or resource changes.
Original logs and source copies, strict canonical diagnostics, separate
JUnit counts and byte/hash manifest are externally preserved and verified.
Status binds the1812-byte manifest and read-only audit. A tool-output display
TypeError occurred after a successful archive-staging copy command; no test
or scientific worker failed, and the subsequent audit verified every copy.
No historical data was edited or deleted. Independent review remains PENDING.

## Next implementation gate

Create a private numerical successor, retaining the current kernel and its
failure record. Port the bounded reorthogonalization and paired expansion.
Represent validated physical vectors by explicitly owned high/low arrays;
fingerprint and validate both. A high-only array must not silently inherit
the full vector's validation. Define factor actions and modal correlations
using the complete pair, not high+low rounded immediately to one binary64
array. Update only private consumers, with wrong/missing/mutated low-part
tests and deterministic serialization.

Keep actual current-history material compliance, signed geometric stiffness,
physical inertia, all original root/action/Ritz/inertia checks, and current
state guards. Recheck full original-factor action, not merely the small
modal Gram, before returning accepted private results. Preserve sign/order,
negative roots, cluster completeness, cancellation and process bounds.

Freeze and run small signed/cluster/representation tests, current material
boundary and trial-ownership tests, all four specimens at each registered
slenderness (all12 roots), then continuum-group and actual prestress cases.
Only a complete passing rehearsal permits fresh deterministic repeat cycles.
No automatic retry of any consumed run. Reuse evidence as hash-bound inputs,
not as substitutes for validating the new implementation.

The full straight/curved production beam and objective shell-connection goal
remains active and incomplete, including material/load/solver/restart parity,
engineering convergence, installed opt-in integration and independent review.
Existing B2/B3/Q4/S3 and main remain untouched.
NO_GO_PRODUCTION_RESTRICTION_UNCHANGED.
