# Current GE-B3 core: production integration boundary

Restart audit parent: `64212b32ead396e0019f9f0599cd00e9b16148df`, tree
`0e7cdfae6548ef0b28f4e99e64e40f66c4978990`. The plastic lifecycle and fine-onset
closeouts remain immutable. Neither needs rerunning for this source-only audit.
This document supersedes no scientific result or earlier formulation identity.

## What the actual interfaces provide

| Interface | Actual scope | Must not be inferred |
| --- | --- | --- |
| `ge-beam3` factory / `ge_beam3_element.py` | Earlier straight mixed macro facade, identity `GE_BEAM3_DC_MIXED_K1_MACRO_V2` | Current curved/generalized core activation |
| `NativeGeneralizedStaticElement` | Private curved ellipsoid-section scalar adapter, 18 external DOFs, issued native trial/commit ownership | Complete section families, public mass or global production parity |
| Retained generalized / arc contexts | Assembled nodal coordinates plus 24 internal unknowns per macro; context-owned origins, predictors and accepted chains | Interchangeable native/force/arc checkpoints |
| Generalized modal pencil | Eliminate 18 inertia-free resultants; retain six physical cell rotations; assemble before eliminating massless nodal traces | Static reduction of physical cell inertia, or active-plastic frequency authority |
| Fine-onset status | Refined lateral inertia crossing at planar equilibria | An actual spatial postbuckled branch, first-root or uniqueness proof |
| Plastic lifecycle status | Two specified actively plastic three-increment curved arc paths and authenticated interruption/restart | Arbitrary loading history, mid-history reversal, independent whole-beam qualification |

The element's three nodes and six external DOFs per node are compatible with
solver-owned internal coordinates. They do not justify replacing the 24-coordinate
dynamic pencil with the static 18-coordinate tangent/mass. Native state schemas,
retained force histories and arc histories have different owners and must remain
distinct; a migration must replay through those owners, not reseal another packet.

## Next implementation, in dependency order

1. Define the current-core production adapter and solver-owned internal-coordinate
   contract, using the existing native state transactions and analytic operators.
   Keep the earlier facade/serialized identity intact. No selector change yet.
   Explicitly separate static Schur, retained continuation and descriptor dynamics;
   do not silently fall back between them at a singular internal block.
2. Exercise that adapter through actual ANYsolver dispatch, assembled shared nodes,
   rejected-step rollback, original-origin replay, recovery and restart. Use the
   already preserved histories as immutable comparisons where the input identity
   truly matches. Cover the actual supported section families, not an ellipsoid
   surrogate relabelled as fibre/elastic authority. Preserve paired/factor paths.
3. Close spatial postbuckled continuation separately: freeze a physical load or
   constraint programme and independent reference before execution. Start from a
   genuine owned virgin/prefix state. Do not perturb/reseal an onset equilibrium
   and call it an accepted branch. Positive lateral negative-inertia count alone
   is not a branch solution. No threshold/coefficient tuning against the result.
4. Consolidate static, nonlinear, modal/prestressed/buckling and remaining parity
   under the same candidate graph, then independent review and installed-wheel
   explicit selection. Qualification remains closed until these gates pass.
5. Qualify objective eccentric/curved beam-shell connections as a separate part of
   the full programme; standalone integration does not authorize those joints.

## Lightweight regression hardening added here

`ge_beam3_integration_boundary_audit.py` reads seven source files and hash-binds
the two latest status files. AST checks guard the exact old factory target,
facade identity and declared gaps, private native qualification flag, external
topology, unsupported static mass route, distinct state identifiers, modal mass
and history policies. Its output is a source diagnostic, **not** a certificate,
complete call-graph proof, archive verification or independent scientific review.
It executes no production imports or scientific cases. Passing it cannot authorize
activation. Later reviewed integration work must deliberately update these
boundary checks and provenance, not disable them to obtain a green test.

The standalone unittest inventory mutation-checks redirects, relabelling, gap
removal, premature activation, mass/history policy changes, missing inputs,
status hashes and duplicate/nonfinite JSON. No mechanics, scientific protocols,
legacy elements, defaults or public interfaces change in this step.

Standalone validation on 2026-09-09: Python 3.14, `-B`, this test file only:
13 tests passed in 0.756 seconds after adding numeric-overflow rejection.
This is one regression inventory, not additional qualification evidence. An
earlier 13-test development invocation also passed; counts are not combined.
The standalone process confirmed no ANYsolver, NumPy, SciPy or SymPy imports.
Both inherited status byte hashes remained identical. Source review here is
same-author; independent review remains pending. No scientific process ran.

New scientific work retains one numerical thread, 24 GiB and 600 seconds per
child, at most three children, 1800 seconds per wave, checkpoints, whole-tree
cleanup and no automatic retries. This read-only regression needs none of the
prior formal waves. Full objective remains ACTIVE and incomplete.

`NO_GO_PRODUCTION_RESTRICTION_UNCHANGED`.
