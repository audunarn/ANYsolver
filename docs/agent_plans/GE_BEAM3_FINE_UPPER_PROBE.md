# Bounded actual fine-mesh upper endpoint, not a qualification cycle

Start after captured-guard development closeout 35db92a. Preserve all earlier
onset NO-GO, capacity, matched-drop and optimization records. This is a new
fixed probe, not a retry or reclassification of those consumed runs.

Use the unchanged full-spatial N20 and N24 curved arch models and native
four-target programmes (0.01, 0.02, 0.03, 0.0456). Inherit the accepted
0.045/count-zero results through their archive manifest and original checkpoint
and factor hashes, as lower-endpoint EVIDENCE ONLY. Never import them as states
for the new programme. The two preserved independent continuum root records
remain hash-bound inputs; no reference solve is rerun.

For each mesh start virgin in a fresh process, then execute four prefix workers,
each using the exact same complete new programme and the immediately preceding
owned checkpoint. A fifth worker captures its actual physical factors. A sixth
independently audits the original planar/lateral factors with Decimal 80 and
100 digits, exact decoupling, mass/trace positivity, full equilibrium/load
binding and reflection. The implementation is copied from the accepted
matched-drop worker with only the research protocol/schema binding changed.
No numerical control equation enters physical stiffness or mass.

First run the pure authority/mutation suite. If it passes, execute one bounded
probe wave, at most two simultaneous mesh chains. Each child retains one
numerical thread, 24 GiB tree memory, 600 seconds, existing inactivity/native
context checks and complete process-tree cleanup. The entire wave is bounded
by 1,800 seconds. Stop on failure; preserve partials and retry nothing.

Process/authority failure yields BLOCKED_FINE_UPPER_PROCESS_OR_EVIDENCE.
Successful actual audits with exactly one lateral negative direction at both
upper points yield PRIVATE_FINE_ONSET_BRACKETS_OBSERVED. Otherwise preserve
PRIVATE_FINE_UPPER_DID_NOT_BRACKET, without changing the endpoint or launching
an automatic expansion. Planar counts are reported separately.

Even successful observed brackets are 0.0006 wide and are not accepted critical
points. They authorize preparation of a separately frozen narrowing protocol,
not its automatic execution. Retain the existing <=0.000098 final bracket
width and conservative <2% errors for BOTH drop and load against all independent
reference endpoints. Full rehearsal and two fresh deterministic search cycles
are still required. No first-root, branch-uniqueness, postbuckled-path or
production qualification claim. Independent review remains pending.

This four-path research addition changes no mechanics, tolerance, section,
state schema, native deadline, production selector, B2/B3/S3/Q4 or default.
The complete straight/curved beam and objective beam-shell connection goal
remains active and incomplete.
