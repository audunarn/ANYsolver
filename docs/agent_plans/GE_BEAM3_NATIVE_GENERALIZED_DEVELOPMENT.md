# Native generalized-section static and owned-state development

Base c665937a5a4b9ac458c95b427cc5c53cf3a2f1fe. Six additive paths including the incident.
Port the preserved distributed-couple static boundary and native transaction
adapter to the separately frozen generalized operator. Keep the historical
physical-fibre path, existing B2/B3 and Q4/S3, aliases, defaults and evidence
unchanged. Use distinct formulation, state schema, static policy and load scope.

Internal equilibrium solves the 24 cell-rotation/resultant equations at fixed
accepted section origins. Reuse reference-arclength line work and independent
spatial distributed couples without nodal lumping. The spatial Jacobian uses
the material-minus-conservative-line residual in its SO(3) connection, not
the net residual after subtracting nonconservative couples. Condense the
general Jacobian without symmetrization; pull it into nodal increment charts
including the force-dependent chart derivative.

Native assembly uses the actual NonlinearStateStore and issued rotation views.
Commit validates element/store/pose identity, exact load scope, accepted origin,
internal seeds, predecessor/epoch, full static replay and output hash. Rejected
or foreign trials cannot advance committed material state. This is owned
in-memory state handling, not authenticated restart or global solver authority.

Fixtures use the preserved straight/curved coordinates and generalized coupled
C/M from the section tests. Elastic yield=1e6; plastic yield=.025; hardening=.6.
Both are explicit fixture data, not changed section equations or tuned mechanics.
Require actual positive plastic evolution in every generalized component.

Tests cover actual assembly, independent line/couple work and chart maps,
fixed-origin directional tangents, commit/discard/replay, state/hash/schema/load
mutations, foreign registrations, post-assembly failure cleanup, resource limits
and unsupported dynamics/recovery/public routes.
Include a connected two-element curved transaction with exact shared-node
rotation agreement, and conservative chart symmetry when the couple is zero.
These checks do not qualify a global force-controlled load programme.
Global equilibrium, restart, nodal-plus-distributed combined loading, recovery adapters and
remaining production parity are later integration gates, not implied here.

Smoke, complete rehearsal, clean freeze, two deterministic fresh-directory
cycles. One numerical thread, 24 GiB, 600 seconds and 120-second CPU inactivity
per child, at most three children and 1,800 seconds per wave. No automatic retry.
All bounds protect actual process trees. Preserve failures before correction.
Existing tolerances 1e-11 invariant/work and 1e-7 directional remain unchanged.
The pre-freeze finite-difference resolution incident is preserved separately.
Retain all central differences at 2e-5, 1e-5, 5e-6 and 2.5e-6; require both
finest raw derivatives and all three Richardson estimates within 1e-7.
If the coarse differences exceed 1e-7, require less than .3 error ratio at
every halving, consistent with the observed second-order truncation regime.
Independent review PENDING; production and dynamic authority false.
