# Signed N20 elastic continuation and cancellation lifecycle

Parent d9c081b3b06df8d7ce89c701ec48687c9f54cbd2. Research-only successor:
the existing private elastic owner and all physical operators remain unchanged.
Owner file SHA256 9d0bf7a01a0577a1867090a1ac4828eb6df2c45d902bcb1767280f007faa9045.
Keep the prior positive branch and its exact capsules immutable.

Use the preserved negative-small equilibrium as an externally hashed seed,
mechanically replayed by the unchanged owner. Continue -.003 -> -.0045 in one
smoke process, then resume the original prefix to -.006 twice in separate
processes. Compare actual replica checkpoint/recovery bytes and physical
reflection of the previously accepted positive branch: polar positions,
S*Q*D director frames, S*R*S cell rotations and signed work-conjugate fields.
S=diag(1,1,-1),D=diag(1,-1,1), field signs(1,-1,1,-1,1,-1).
Require normalized differences <=1e-11, 20 elements and160 recovery stations.

Separately resume the preserved POSITIVE .0045 prefix and request cancellation
at the actual target2 before_commit and committed safe points. Require the
former to return EXACT original one-record prefix bytes and the latter the
EXACT previously completed two-record capsule. Mechanically replay both actual
cancelled results with fresh owners and recover. Then resume the original
cancel-before result in another process, requiring the original full capsule.
Do not manufacture equivalent checkpoints or merely run a zero-step stand-in.

Cancellation observations identify the exact requested callback and target;
the result must be cancelled with the expected accepted cursor. Rehashed
mutations to operation, cursor, cancellation observation, source or checkpoint
must be rejected. No N20 plastic cancellation is claimed; this owner remains
elastic-only and no physical loading path from rest is asserted.

First tests, then frozen negative smoke, then at most3workers in a bounded
wave; each600seconds,24GiB,one numerical thread,1800seconds/wave and existing
120second context/CPU-inactivity checks. No automatic retry. Preserve partial
results, originals, commands and receipts externally. Canonical completion only
after all launched processes are terminal and actual file comparisons pass.

This gate closes signed segment reflection and actual N20 cancellation/restart
only, not FE refinement, branch uniqueness/stability, full postbuckling,
independent authorship review, production activation or beam-shell joints.
NO_GO_PRODUCTION_RESTRICTION_UNCHANGED. No src, defaults or historical edits.
