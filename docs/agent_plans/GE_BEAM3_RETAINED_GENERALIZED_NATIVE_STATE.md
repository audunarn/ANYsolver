# Native retained-generalized distributed state successor

Base `f2b51900be36b3a4395facda5037ec1d41ab9f02` preserves the passing
retained research conditioning funnel and its failed predecessors.
Add private native state and controller modules, tests and this contract;
do not change existing production operators, defaults, public routes or history.

Capture the actual supported homogeneous standalone generalized model and its
ordered node/element identities. Retain24 internal unknowns per element in
the global Newton solve; external nodes still have exactly six DOFs. Reuse
only the same generalized potential, section history codec, physical load
work and spatial Jacobian. Runtime code must not import research mechanics.
Support connected meshes through shared physical nodal frames. Keep existing
bounded model/domain rejection; no shell-joint authority is implied.

Own mechanical arrays in immutable buffers. Keep accepted origins separate
from resulting histories. Every trial uses the previous accepted histories;
commit only after1e-11 equilibrium, compatibility and remaining mixed-correction
checks, reproduced section histories and physical resultant recovery. Stage
the complete canonical capsule before atomically publishing state/cursor/history.
Cancellation before publication retains the old capsule; after publication it
retains the new capsule. Do not mutate FEModel state or use legacy mechanics.

Restart requires exact external SHA-256 authority. Bind formulation, model,
program, nodes/operators through model identity, virgin genesis, ordered load
targets, mechanical values, origins, histories, residuals, work and recovery.
Re-evaluate accepted records from their saved origins without Newton or history
advancement. Strict canonical JSON rejects duplicates, nonfinite/boolean arrays,
extra fields, changed loads/models, foreign formulations and corrupted records.
Internal hashes are integrity checks, not signatures or proof of iteration counts.

Tests compare the native port with all six frozen research states, paused/
resumed/uninterrupted capsules, connected plastic load/unload/reversal,
recovery replay, mutation rejection, cancellation around publication, failed
iterations, and malformed/foreign checkpoints. Run one moderate port smoke
before a bounded complete rehearsal. Keep inventories separate and record
actual failures without retrying the same freeze.

At most three workers, one numerical thread and24GiB per process tree,
600seconds per child and1,800seconds per wave. The private context has an
additional120-second cooperative bound. Stop larger execution on smoke failure;
preserve raw outputs. Two deterministic cycles follow only a passing rehearsal.

This is private controller development, not public or full beam qualification.
General fibre adapters, reference-backed nonlinear/curved/postbuckling studies,
factor-modal integration, objective shell connections, independent review,
complete environment graph and installed packaging remain required.
NO_GO_PRODUCTION_RESTRICTION_UNCHANGED; independent review PENDING.
