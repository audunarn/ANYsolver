# Retained arch loaded-state snapshot checks

Source parent: 89446eeac882ff48707b6aeae1c3f3e56e4f2fb3. Research-only successor;
no mechanics, defaults, existing element or historical evidence changes.

Consume the preserved repeat archive, manifest SHA-256
14D83C2B09CCE8ACF04649CE5E4F4D09FBBB500986AD2BAC949AD0A0CB9DD4AA.
Authenticate each packet before numerical imports, then perform full native
accepted-prefix replay and require identical serialization. No new arc solve
or continuum-reference solve is permitted. Step 1 has positive accepted slope;
step 12 has negative accepted slope. These are the pre/post-limit samples.

Freeze source before smoke: two-macro step 1. After smoke passes, check the
finest twelve-macro states at steps 1 and 12, in two fresh deterministic cycles.
Each worker checks every element under the existing general and large-dyadic
poses. Preserve paired current coordinates using exact rational transforms.
Reference geometry stays fixed: Q'=SQ, U'=SU, retained p invariant. The local
42-coordinate map rotates the first eight spatial three-vectors, retaining p.
Check potential, residual, chart Hessian, spatial Jacobian, kinematics, virtual
work, physical recovery, current frames, station work and dead-force virtual work
at normalized 1e-11. Check three mixed directions at three centered-difference
steps for energy, chart residual and spatial residual derivatives at 1e-7.
Differences are test diagnostics, never mechanics. Keep predecessor origins
fixed and prove all accepted state/history/checkpoint bytes remain unchanged.

Do not submit transformed snapshots to the clamped Context or call them an
actual transformed global equilibrium solve. This gate does not establish
reference-coordinate covariance, spatial stability, plastic arc, full material
parity, independent review, public integration or beam-shell joint qualification.

Use the unchanged Job supervisor: at most three workers, one numerical thread,
24 GiB/process tree, 600 seconds/child, 1800 seconds/wave, 120 seconds CPU
inactivity. Existing 120-second mechanical Context is unchanged. No automatic
retry, no partial canonical output. On failure preserve logs and investigate;
do not loosen gates. Protocol unit inventory is separate from the smoke and
loaded-state inventories. Complete source/environment checks before and after
each worker; publish exclusive canonical science only after validation. Require
byte-identical science between complete cycles, with process receipts external.

Successful terminal: UNCLASSIFIED_GE_BEAM3_LOADED_ARCH_SNAPSHOT_ONLY.
Failure remains diagnostic/NO-GO for this gate, never production qualification.
Full straight/curved GE-B3 and objective beam-shell connection goal remains open.
