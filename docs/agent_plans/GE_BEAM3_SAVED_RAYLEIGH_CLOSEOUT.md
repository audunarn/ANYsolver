# Signed Rayleigh diagnosis complete; paired repeat gate ready

Frozen8e36b532be903320a4fde94c7516b4b31d5b2aed, tree
7c0d990ae5aaae2995fe88fb523e64267be890b0. Three research-only files;
production mechanics, state, recovery, numerical kernels and defaults unchanged.

Separate inventories: exact-action unit one test/one record passed; N4 saved
audit one test/five records passed; N8 saved audit one test/16 records passed.
Every one of the19 observed scalar discrepancies was decomposed exactly using
stdlib Fraction arithmetic on supplied binary64 matrices/vectors. Both mode
halves were retained. No mechanical state or solve was recomputed.

Across these points the largest normalized contributions were:

- New scalar minus original-factor quotient:1.264e-16.
- New versus old vector quotient on the same original factors:8.707e-22.
- Original-factor versus old dense-operator quotient:1.244e-13.
- Old dense-operator quotient minus old reported eigenvalue:4.927e-11.

Thus the observed old/new scalar discrepancy is predominantly old dense
eigenvalue extraction error on these saved cases, not a new physical-mode or
material-state difference. Exact decomposition identities hold for every
point, not merely for the worst specimen. This is an independent arithmetic
reconstruction, not an independent source-equation mechanics oracle or review.
No general claim about unrelated old solver paths is implied.

The earlier extra assertion of1e-11 old/new scalar equality remains false,
and its failed audit is preserved unchanged. This diagnosis does not rewrite
it as a pass or enlarge any tolerance. The intended current-path engineering,
work, inertia and state checks already passed; old scalar values are historical
diagnostics, not a replacement reference for the new original-factor checks.
All88 preload histories, loads and final buckling brackets and all continuum
reference files were already shown unchanged. The actual physical gates remain
the same. Repeats must match the NEW accepted rehearsal bytes, not fabricate
identity with a superseded numerical representation.

All three process trees terminal; longest child4.218seconds before cleanup,
peak181MB or less, maximum two concurrent. Frozen guards, exclusive outputs,
strict canonical JSON, exact rational sums, per-file byte/hash verification
and original-copy identity all pass. The4890-byte archive manifest and audit
are bound in the canonical status. All originals and failed evidence retained.

## Next bounded repeat wave

No scientific repeat has started. Freeze an external coordinator binding the
current clean commit and unchanged private implementation/test hashes, plus
the e4e10fb paired rehearsal and a9430a2 consumer rehearsal archives and this
disclosure. Run two fresh-directory copies of each separate inventory:

- Paired local32-node suite and three four-specimen/all12-mode slenderness lanes.
- Consumer local six-node suite and the three unchanged continuum-group lanes.
- Four actual-preload buckling refinement nodes, each with22 states/23 records.

Verify each output file byte-for-byte against its passing rehearsal, retain
lane counts separately and bind every terminal receipt. At most three child
workers, one numerical thread each,24GiB/600seconds/120second CPU-inactivity
per child; at most1800seconds per complete repeat wave. No automatic retries.
If one fails, launch nothing further and allow already-launched bounded
workers to reach terminal state unless a resource breach requires termination.
Never form a passing scientific aggregate from a partial wave.

Only after deterministic repeats pass, assess remaining full beam requirements
and independent review/installed opt-in integration. Broad nonlinear/material/
load/solver/restart parity, curved engineering coverage and objective finite-
rotation shell connection remain within the active goal. No production
qualification, publication, selector or default activation is authorized by
this arithmetic result. NO_GO_PRODUCTION_RESTRICTION_UNCHANGED.
