# Saved-state continuum lateral comparison

Successor to private closeout `6f2d3dbb303fc5259356d96b95e819cb7558ced0`.
Use the nine saved mesh-specific states (4/8/12 macrocells, steps 6/7/9).
Bind the accepted arch-repeat point/reference fields and the repeated zero-shift
inertia aggregate by exact manifest/byte/hash authority before numerical imports.
No production beam, discrete operator, base BVP, arc continuation, new spectrum,
or root-bisection computation may run in this comparison.

Require matching actual crown displacement and native load/checkpoint identity.
The continuum load is its own saved value at that displacement, not forced to
the native load. Bind height .1, EA1000, GA400, planar bending .01, torsion .02
and lateral bending .02. Preserve the existing BVP9 boundary <=1e-11 and
differential/sensitivity/work <=1e-8 checks. Extract exactly the existing 129
uniform left-half sample coordinates from the saved union grid, with no
interpolation, extrapolation or repeated base solve.

Use the separately reconstructed continuum lateral Jacobi equations and their
documented knot-resolved successor propagator. Both saved-field strides 1 and 2
are evaluated on 129 and 257 nodes per half, preserving DOP853 rtol1e-11 and
atol1e-13. Require normalized full transfer refinement difference <=1e-11;
require consistent nonzero determinant signs across resolutions and strides.
Keep stride transfer differences diagnostic. Each propagation retains its
30-second cooperative/10000-callback ceiling. No automatic retry or use of the
previous inaccurate whole-interval adaptive transfer.

Record full transfers, boundary matrices, determinant signs, singular values,
refinement and Hamiltonian/symplectic diagnostics, exact sampled input hashes,
native negative counts and their parity comparison. Determinant parity does
not determine the full continuum inertia or prove a unique neutral root.
Neither agreement at these coarse samples nor a coarse-mesh mismatch supplies
the missing quantitative critical-point convergence gate. This comparison is
diagnostic and cannot qualify the current beam or adopt old discrete P5 results.
The continuum equations are separate mechanics but same authorship; independent
review remains pending. No certified interval/eigenvalue claim.

Unit-test exact fixed-grid extraction, schema/material/displacement/load/hash
mutations, forbidden production imports/base solves and determinant-parity
interpretation. Smoke the first four-macro sample before the remaining eight
rehearsal cases. After complete rehearsal run two fresh complete cycles, one
child per mesh, at most three simultaneously. Require byte-identical canonical
scientific outputs and aggregate. One numerical thread,24GiB/tree,600s/child,
1800s/wave and120sCPU-inactivity limit. Publish no partial canonical aggregate.
No mechanics, public aliases, defaults, historical evidence or source environment
change. The full GE-B3 goal remains active and incomplete.
