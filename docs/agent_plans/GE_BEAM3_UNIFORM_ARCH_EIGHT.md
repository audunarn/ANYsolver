# Eight-macro uniform arch comparison

Base 26aa0f5ba09b57615430675a10d82eb2057f1240. Preserve both failed two-macro
arc and accepted four-macro displacement evidence. No existing source changes.

Run the same three crown drops with eight macros/seventeen nodes and node 9
uy control. Keep geometry, section, quadrature, load, solver, tolerances,
state and physical recovery unchanged. Both end nodes are clamped; all other
spatial DOFs remain free. Bind the accepted four-macro status and compare its
three load errors directly. Evaluate the unchanged continuum BVP9 at the 64
native station locations per target; retain the original sampling union.
Report full position/frame/section-field differences and compliance norms.
An observed improvement is not a full engineering or postbuckling certificate.

The four-macro complete checkpoint is 591213 bytes. Linear size estimates are
1182426 bytes for eight and 2364852 for sixteen; these are planning estimates,
not exact encoded-size proofs. The existing cap is 2097152 bytes. Do not launch
the sixteen-macro three-step chain under the unchanged codec. A larger native
history needs a separately reviewed, bounded serialization/storage successor;
never discard genesis or accepted states to fit the current envelope.

One native rehearsal, 600-second wall, one numerical thread, 24 GiB Windows
Job process-tree memory and 120-second CPU-inactivity bound. No automatic
retry. Only after it passes freeze these two added research paths and run
two fresh-directory replicas, byte-identical to the rehearsal. At most three
workers and 1800 seconds per wave. The earlier unit inventory is inherited,
not rerun or recounted as new tests; no mechanics/checker formula changes.

Acceptance of this diagnostic requires complete three-target state/recovery,
exact checkpoint roundtrip, elastic histories, 64 stations per target and
deterministic raw outputs. It does not waive the separate 2% engineering target
or establish spatial branch stability. Preserve a failure before diagnosing.

Next: resolve practical checkpoint extent, complete mesh/field convergence,
then bounded objective arc step control and actual limit-point/postbuckling
validation. Physical mass, spectral workflows, broader parity, independent
review, installed-package/public integration and objective beam-shell joints
remain open. B2/B3/Q4/S3 mechanics, public defaults, packages and historical
evidence are unchanged. The full beam/joint goal remains active.
