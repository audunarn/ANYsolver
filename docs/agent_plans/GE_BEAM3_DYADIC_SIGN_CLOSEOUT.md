# Exact-sign filtering checkpoint

Frozen source `4204f3a38c65fc215a76c595f994a7a71c317d54` completed both
authorized development invocations once, with exit 0 and no remaining workers.
No failed or historical output was modified and no automatic retry occurred.

The 69-coordinate saved-pencil measurement used one warm-up plus eleven
alternating paired calls. Every exact inertia was (64 positive, 5 negative,
0 zero). Median integer Bareiss time was 1.7401108 seconds; the outward-dyadic
filter plus exact fallback took 0.0719675 seconds, approximately 24.2 times
faster on this call. Raw CPU and median/MAD/p95 observations remain external,
bound by SHA-256 5a9ca8a039aaa943547a01ad3923ffbdba41d9bc6f3804cee34ce27d4d25516d.
This is not a general solver performance or release qualification claim.

The complete two-state six-macro diagnostic finished in 22.774 seconds versus
the prior observed 144.401 seconds. All ten raw input/reference/checkpoint/
native-spectrum artifacts are byte-identical. The entire canonical aggregate
body is identical except for the frozen source revision. The new 3490-byte
aggregate has SHA-256 f50285799e45a7fdbbcb7d9cad997b13e83a8e7bf38e6904c4c1e247595d5362.
Both original scientific accuracy limitations and zero/two-negative-mode
observations remain unchanged. No nonlinear or reference equilibrium ran.

Separate passing inventories: initial arithmetic/allocation 73; complete
preparation 87; saved reproduction/statistics inspection 7. They overlap and
must not be summed into a unique-test count. Exact endpoint arithmetic is
checked against separate Fraction calculations; shifted inertia is checked
against rational Schur complements. These are same-author development tests,
not completed independent scientific review.

All original resource, root-width, residual and state controls remain in force.
The dyadic filter returns a sign only from an enclosure excluding zero. It
escalates from 256 to 512 bits then falls back to exact integer congruence if
unresolved, under one unchanged total 30-second sign-call deadline.

## Resume work

The twelve-macro saved model has 222 assembled spectral and 141 dynamic
coordinates. It needs separately declared 256/160 spectral/exact allocations
and replay under its already authorized 512-coordinate retained-state budget.
Do not silently change defaults or constructor deadlines. Preregister and test
those options before one fresh bounded diagnostic; reuse the preserved
nonlinear state and continuum references. Continue with loaded-state mesh
convergence and buckling/postbuckling, broader geometry/slenderness/material
parity, independent review, standalone public opt-in packaging, and the
objective eccentric/curved beam-shell connection. Full qualification remains
incomplete. Existing B2/B3, Q4/S3, public defaults and historical evidence are
unchanged.
