# Native adaptive arc development closeout

Implementation a44b7c2d9ce297603e15e63802a2df5764a671de,
tree 0a2c954e54923b9a3d850bed7f63fa532f3e9f8a. Exactly four implementation paths:
shared fixed/native arc driver, new private adaptive driver, tests and plan.
No beam/shell mechanical operator, coefficient, quadrature, tolerance,
public selector, default or version change.

## Accepted evidence

Separate frozen inventories, each with successful rehearsal and two
byte-identical scientific repeats:

- Local: 41 tests and 45 scientific JSON files per invocation.
- Curved plastic source: 1 test and 4 scientific files per invocation.
- Curved plastic rejection: 1 test and 4 scientific files per invocation.

Four separate regressions also passed with exact historical scientific bytes:

- Fixed arc local: 17 tests, 20 files.
- Fixed arc safety: 19 tests, 19 files.
- Translation-to-arc source: 43 tests, 49 files.
- Sixteen-macro seeded arch crossing: 1 test, 7 files.

The unchanged historical crossing took 473.33409949998895 seconds. This was
the longest child; all others also stayed below 600 seconds and 24 GiB.
The frozen scheduling wave lasted about 649.80 seconds with at most three
concurrent workers. No worker failed or was automatically retried. All are
terminal. Earlier pre-freeze smoke inventories remain separately labelled.

## Control and state findings

Natural off-axis bar convergence required four bounded halvings:
.2, .1, .05, .025 were rejected; .0125 converged. The next .0125 step
also converged. All rejected attempts retained the same accepted-origin hash.

The registered proper rigid-motion covariance errors were at most
5.863421285458166e-16, with identical step/disposition schedule.
Curved continuation retained eight positive plastic-history stations.
After rejection of a real plastic trial, accepted state and the entire inner
packet matched direct fixed smaller-step execution byte-for-byte. Raw
accumulated-history diagnostics retain their compensated high/low pairs;
small negative low components are not negative physical accumulated history.

Only typed convergence exhaustion allows cutback. Authority, state,
factorization, observer, validation and cancellation failures remain fatal.
Failed-trial trace is authenticated solver diagnostics, not independently
reexecuted scientific proof. Full accepted history and predictor replay remain.

## Preservation and limits

The external archive contains 439 data files plus its manifest. Every file
was checked by byte count and SHA-256; original run directories remain.
Only hash-verified duplicate staging may be removed. Historical source,
crossing and failed-candidate evidence remain unchanged.

Outcome: PASS_DEVELOPMENT_NATIVE_ADAPTIVE_ARC_ONLY.
Independent review remains PENDING. This is not a production-qualification
or public-integration decision.

Next: separately freeze adaptive arch continuation plus actual curved-plastic
pause/restart tests. Wider postbuckling/slenderness/spatial stability, broader
typed cutback and load/state parity, practical scale, physical mass/modal/
prestress/buckling, independent review, installed package integration and
objective beam-shell connections remain required for the full goal.
