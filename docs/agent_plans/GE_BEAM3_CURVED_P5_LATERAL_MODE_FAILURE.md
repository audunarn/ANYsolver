# P5 lateral mode recovery — preserved failed first comparison

The first proposed linear mode comparison failed before publishing a result:
`LateralReferenceError: saved-transfer/mode integration disagreement`.
The second comparison was not started. No threshold was changed or failure
reclassified. The original discrete arch request, raw trials, continuum roots
and spatial-mode inspection are unchanged.

Attempted external root:
`C:\Users\AudunArnesenNyhus\AppData\Local\ANYrelease\ge-beam3-p5-lateral-mode-20260906-cdc110e7399240eaa41c84ff02e04405`.
Its `first` and `second` directories exist, but neither contains a file.
The traceback was preserved in the task command output; there is no raw
failure packet, partial comparison, successful output hash or agreement record
to invent. The command stopped on the first nonzero exit.

Executed source SHA-256 identities:

- Mode reference: C653C95BADF3CCD08C0DF8693FEC3FA6273C2DD10FB821B8DACDE0F77654ECAA.
- Comparison reader: 811C21C18439A1281E11B38C26C622AB6CEA3415DCE9E3FF594895F6463CA1D0.

The preceding small suite passed 17 tests in 0.47 seconds, including constant-
coefficient work and a straight neutral shape. Those checks did not establish
the variable-coefficient saved-transfer agreement and cannot override this
failure. The cause is not yet determined at this checkpoint.

Next inspect a separately labeled diagnostic of the same linear equations,
retaining expected and independently integrated endpoint vectors and errors.
Do not rerun the comparison command unchanged, waive its 1e-8 transfer gate,
modify a preserved root record or classify a result until the discrepancy is
understood. Any correction must preserve this failed source state and add a
regression exposing the missed behavior. This is research, not qualification;
all production mechanics/defaults and restrictions remain unchanged.
