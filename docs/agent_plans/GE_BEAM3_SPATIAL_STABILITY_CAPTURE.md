# Full spatial endpoint stability — bounded development gate

Parent: c1613bef9d637b79c99617c3ec7fd9f59c8564cc.
Do not rerun accepted onset, continuation or refinement campaigns. Authenticate
the preserved actual N24 signed .006 endpoints and their original elastic seeds.
Replay each original checkpoint in its own elastic-seed owner, then evaluate
conservative fixed-dead-load factors. The numerical lateral controller is not
a physical support. No planar-family partition, symmetry averaging, entry
dropping, or state-schema conversion is allowed.

The new private capture adapter copies the existing translation-owned factor
construction unchanged, substituting only the authentic seed owner, seed binding,
seed displacement target and policy. Old mechanics and old capture remain intact.
No public selection, package/version, B2/B3/S3/Q4 or default changes.

The separate standard-library checker reconstructs K=(L R)^T(L R)+H and physical
mass from original binary64 factors at 80 and 100 Decimal digits. Sparse scalar
or 2x2 symmetric Schur updates retain all entries. Minimum-degree pivot candidate,
local magnitude check, maximum front 96, at most 2,000,000 updates, maximum
growth 1e12 and unresolved-pivot floor scale*1e-50. Reverse every Schur update
and require full-matrix reconstruction relative error <=1e-60. No interval
certification or independently authored mechanics is claimed.

Background: LAPACK symmetric indefinite factorization uses block-diagonal 1x1
and 2x2 pivots; its testing reconstructs the original matrix to assess residual.
This checker is separately implemented, not a claim to implement LAPACK pivoting:
https://www.netlib.org/lapack/explore-html/d6/d90/group__hesv__driver__grp.html
https://www.netlib.org/lapack/explore-html/d2/d38/ssyt01_8f_a987adc5149b1ff7c4e4abd1f9aa37025.html

Test actual small seeded-state Schur equality/control freedom and owner mutations.
Test known signed congruences through dimension 438 at both precisions, dense
cross-checks on small matrices, 2x2 pivots, tiny nonzero coupling, singularity,
asymmetry, nonfinite inputs, cancellation and fill bounds. Run a positive N24
capture smoke first. If successful, capture the negative endpoint and run both
precision checks per endpoint in fresh processes. Require identical inertia
counts and pivot paths across precisions; never classify rounded eigenspectra.

Each child: 600 seconds, 24 GiB, one numerical-library thread; at most three
concurrent workers; wave 1800 seconds; unchanged 120-second CPU inactivity
watchdog. Checker internal deadline 120 seconds. No automatic retries.
Failures or disagreements block this gate. A valid negative inertia is a truthful
unstable equilibrium result, not automatically a formulation NO-GO. A positive
result proves only the sampled factor state, not all branches or production.

Preserve factors, audits, receipts, commands and raw logs externally. No partial
canonical aggregate. Full standalone and objective beam-shell qualification
remain open. NO_GO_PRODUCTION_RESTRICTION_UNCHANGED.
