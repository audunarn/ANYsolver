# Twelve-macro curved physical-fibre arch diagnostic

Base: `b4735472d8f9afa5c14bd117708d6f09a604adfa`, tree
`bc8245263c36603dbf522060a51c3254cff09b32`. The completed six-macro
diagnostic and failed four-macro invocation remain immutable. This is a new
development refinement, not a retry, qualification campaign or release.

## Allocation and unchanged mechanics

The same exact parabolic reference, physical-fibre section, station rule,
clamped ends and four crown-displacement targets are used with twelve macros.
There are 25 nodes, 150 nodal coordinates, 288 internal coordinates, 438 total
retained coordinates, 426 free coordinates and a 427-dimensional control
border. No element equation, Newton criterion, section parameter, reference
load, target, chart limit or accepted-history rule changes.

The private default allocation limit remains 256. An explicit 512 budget is
propagated through the private context/driver and bound into its capture hash.
The default-256 hash structure remains unchanged. Twelve macros reject the
default budget. Invalid and oversized allocations reject before dense assembly.
One 512-by-512 binary64 array occupies 2 MiB; this is not a total-RSS estimate.
The 24-GiB process-tree budget and all existing time limits remain in force.

## Execution and interpretation

Freeze the exact source before running the existing six/twelve diagnostic
module with `--macros 12` and its exact `--revision`. Use the fresh external
directory `C:/Users/AudunArnesenNyhus/AppData/Local/ANYrelease/ge-beam3-fibre-arch12-20260907-v1`.
The parent validates source and runtime before mechanics, forces one numerical
thread, contains the entire child tree in a Windows Job, enforces 600 seconds
wall time, 120 seconds inactivity and 24 GiB, and proves cleanup before
exclusive canonical publication. The driver also retains its existing
120-second cooperative deadline. No automatic retry is permitted.

The active Normal-token workspace policy permits worker-owned scheduling
without a new global resource request or grant. This supersedes resource-only
holds, not source freezing, bounded execution or scientific acceptance.
Do not alter old requests, ledger rows or another owner's lock.

The native four-target checkpoint is replayed. Reference loads are the
previously hash-bound nominal BVP9 values; the BVP is not rerun. The output is
load-only and remains `DEVELOPMENT_LOAD_COMPARISON_ONLY`, with independent
review pending and production qualification false. Even if all four errors
are below 2%, this does not establish full-field convergence, same-branch
identity, broad nonlinear/history/modal qualification or beam-shell coupling.
Preserve failed or partial data externally without canonical promotion.

## Verification and boundaries

Before freezing, run small fixture, allocation, forwarding, strict-reader and
mocked-process tests. They cover both six/twelve scopes, ordering, hashes,
exclusive output, timeout, inactivity, memory, root/descendant failure and
unproven cleanup. Check preserved four/six outputs in memory without mechanics
and require their original bytes. Bind test-report and source hashes in a
preparation record. After execution bind all output sizes/hashes, timestamps,
exit status and the exact candidate commit/tree in a separate result record.

Existing B2/B3, qualified Q4/S3, selectors, aliases, production defaults,
dependencies and historical evidence are unchanged. The separate objective
connection branch is untouched. No merge, version change or publication is
authorized by this diagnostic.
