# Explicit negative-work witness for saved N24 spatial endpoints

Parent 038eb75780c019db615109f82fe8a414c3050a1f. Preserve accepted energy-inertia
results and every preceding failed gate unchanged. This gate produces a direct
negative-work witness for both signed N24 amplitude-0.006 endpoints. It does not
rerun their mechanical solution/capture, qualify an entire branch or change any
element, section, state, recovery, alias or default.

Bind original factor packets and the accepted 100-digit energy audits through
their existing archive manifests and exact hashes. Form the same full energy
matrix at 100 digits and retain the same positive diagonal scaling and bounded
pivot path. Reconstruct all Schur updates with the previous checks. Choose a
negative direction in the first negative pivot block, zero other diagonal-block
coordinates, and back-substitute x_p=y_p-D_p^-1 B_p^T x_rest. Map back through
the positive scales. Normalize by the signed largest-magnitude coordinate so
the maximum absolute original coordinate is exactly one. Retain every free
coordinate; the numerical lateral controller is not a physical support.

The separate checker MUST NOT use the factorization, pivot count or symmetrized
matrix to decide the sign. Convert each ORIGINAL binary64 factor entry with
Fraction.from_float and each finite bounded Decimal witness coordinate to its
exact rational value. Evaluate q=||L R x||^2+x^T H x directly with all original
entries, including both unequal transposed entries. Require q<0 exactly, with
no float sign comparison, tolerance or interval inference. Publish reduced
rational numerator/denominator values for material, geometric and total work.
Hash-bind witness, source factors, free map and checker output. Reject duplicate
JSON keys, nonfinite/oversized coordinates, malformed extents and false claims.

Test scalar/two-coordinate negative pivot directions, reverse substitution,
coordinate congruence, exact material/geometric work, positive and zero witness
rejection, all-positive producer rejection, coordinate/factor/free-map mutations.
An exact negative value certifies only the saved factor model and supplied
direction, not interval accuracy of the physical discretization or independent
authorship review.

Freeze before positive producer smoke. If it passes, produce the negative
witness and fresh replicas of both producers. Require producer replicas to be
byte-identical. Check each original witness twice in fresh checker processes;
require byte-identical checker replicas. A complete aggregate is created only
after all eight workers reach terminal states and all gates agree.

One numerical thread per child; max three concurrent; 600 seconds and 24 GiB
per process tree; 120-second producer/checker internal deadlines; 1800-second
wave limit and unchanged inactivity monitoring. No automatic retry or consumed
directory reuse. Keep failures and partial logs outside canonical evidence.

Successful outcome: PRIVATE_EXACT_SIGNED_N24_NEGATIVE_WORK_VERIFIED. This is
an unstable saved equilibrium, not an automatic formulation rejection. Full
spatial postbuckling, physical path from rest, plastic continuation, broad parity,
independent review, installed explicit selection and objective beam-shell
connections remain open. NO_GO_PRODUCTION_RESTRICTION_UNCHANGED.
