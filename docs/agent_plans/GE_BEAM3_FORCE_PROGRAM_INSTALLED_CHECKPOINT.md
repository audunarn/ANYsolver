# GE-B3 force program: isolated-wheel development checkpoint

## Accepted development evidence, not qualification

The private source candidate is commit
`eec5f5f4032cfddb82f9d8b6ecdef1d291d5d2ce`, tree
`bfa7b485d6dd420706872304230b25ccdbb9d729`. The source-stage regression remains
151 passed in 137.44 seconds, plus five static binding checks. No source,
mechanics, selector, default, version or historical evidence changed in this
package-verification successor. Independent review remains pending.

The new offline installed-wheel test passed once in **101.51 seconds**. It
built one wheel from a disposable source snapshot with CRLF-transformed bound
modules, installed it into a new external virtual environment and executed the
same check in two fresh isolated processes/directories. All 52 canonical source
bindings and actual installed file hashes were checked. Every imported ANYsolver
module came from the virtual environment; repository paths and research imports
were forbidden. Build and installation never modified the source checkout or
production environment.

The installed runtime was Python 3.13.9, NumPy 2.5.2 and SciPy 1.18.1, with one
numerical-library thread. The ten offline dependency artifact identities came
from the preserved P3 wheelhouse record. Its historical request/authorization
was not reused. This was a small correctness test, not a resource campaign or
performance claim. Each subprocess had a 120-second observation bound; this
test harness is not a formal process-tree/memory watchdog.

Both installed processes independently exercised plastic loading, unloading and
reversal with simultaneous nodal and native line forces, pause/restore in fresh
models, accepted-state recovery, shared-node models translated by `2^40` and
precommit cancellation. The two scientific records are byte-identical, as are
the two exported accepted global checkpoints. This establishes these small
installed cases only; it does not establish full engineering qualification,
general material parity, loaded modal/buckling or beam-shell connection safety.
There is no cross-runtime byte-equality claim against the source-test runtime.

## Permanent archive

`C:/Users/AudunArnesenNyhus/AppData/Local/ANYrelease/ge-beam3-force-program-development-20260906-948d4f6f2742`

Eighteen files, **1,497,799 bytes**, preserve the wheel, source map, standalone
check, receipt, both result records, both accepted checkpoints and every captured
build/install/process stdout/stderr stream. The canonical installed evidence
record binds per-file byte counts and SHA-256 hashes. Files were copied
exclusively, then verified from the normal workspace context against those
recorded hashes. Only verified relay duplicates and their empty directories were
removed; the original temporary output and permanent archive remain.

- Private wheel: 1,340,515 bytes,
  `948d4f6f2742da5fdd8738e61e4a96e93ed618cd982c205ac80cf473bd20bd73`.
- Each scientific record: 12,639 bytes,
  `00c60d3c6847b4616d4525fb1a72765d4533cc02220e8230995847e43a09dfdb`.
- Each accepted checkpoint: 36,449 bytes,
  `a47f9e8115d139b40004c06af9e94ebc8bc8ccc8477ff720f8135706996b2449`.

The wheel retains the existing development metadata version `0.4.2`. It is
**not** a replacement publication of that version and must not be uploaded.

## Continuation

The next engineering work is displacement/arc-length control using the already
implemented native parameter derivative, including restart, cancellation and
accepted-history safety across limit-point paths. Adaptive cutback, broader
constraints/sections/loads, loaded modal/buckling, independent review and full
straight/curved engineering qualification remain. Objective finite-rotation
beam-shell connections remain a separate part of the active overall goal.

Existing B2/B3, Q4/S3 and the limited accepted straight GE-B3 remain unchanged.
No formal request, production activation, public driver registration, release or
package publication is authorized by this development checkpoint.
