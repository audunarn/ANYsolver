# Native combined-couple solver and restart development

Base: 1c850b4d26f633737c772326dde8878669761b5b. Preserve all completed
distributed and nodal-couple evidence and every physical beam/shell operator.
Compose the preserved fixed-spatial nodal couple work map with the preserved
reference-arclength distributed-couple general static reduction. No potential
is invented for a spatial couple; no condensed tangent is symmetrized.

The actual native force solver must subtract the nodal Exp-chart external
tangent from the distributed static Schur tangent and retain GENERAL
factorization. Count a shared-node nodal moment once globally. Keep distributed
couples attached to their internal cell rotations. Bind both source load-policy
IDs in the combined programme. No legacy mechanical operator is reused.

Introduce a distinct complete accepted-chain checkpoint with a distributed
LoadPoint and constant/proportional nodal moments. Authenticate external bytes,
strict canonical schemas, model/support/native-pose history and load-path
equilibrium. Reconstruct internal responses using the unchanged distributed
state validator; subtract nodal moments once from accepted spatial residuals.
Admit old distributed checkpoints only through their own old route, never
reinterpret them as a combined chain. Keep every input and store ownership
guard and fail-closed native rollback behavior.

Test the complete combined residual/tangent with independent closed Rodrigues
work and directional checks (1e-11 work, 1e-7 tangent); analytical combined
distributed-plus-tip torsion and reactions (1e-11); zero-nodal equivalence,
fixed-node moment reactions, authority failures and finite-range rejection.
Use straight elastic, curved plastic and connected plastic restart/recovery
shards, including shared-node loading, continuation, unloading, failed-step
preservation, resealed load/response/history mutations and exact serialization.

Run smoke before rehearsal. Freeze only after local and three geometry shards
pass. Run two fresh-directory frozen cycles; require byte-identical scientific
files by lane. Rerun the prior distributed restart shards and prior nodal-couple
regression, plus Q4 ownership and legacy B2/B3 regressions, separately; compare
their previously preserved scientific packets. No historical evidence edits.

One numerical thread, 24-GiB process-tree memory, 600-second wall and
120-second CPU-inactivity limits per child; at most three concurrent children,
1,800 seconds per wave, exclusive output directories and no automatic retry.
Keep all failed attempts and partial logs outside accepted canonical evidence.

This is private development, not production qualification or spectral/dynamic
authority. Independent review remains PENDING. General nonlinear section/path
parity, consistent mass, modal/prestress/buckling, slenderness/curved engineering,
public packaging and the objective beam-shell connection remain outstanding.
No defaults, aliases, versions, B2/B3 or Q4/S3 mechanics change.

Pre-freeze fixture correction: the initial curved-plastic combined load had
zero plastic increments. Preserve that failed coverage witness; never count it
as plastic qualification. The corrected nodal moment is (.015,-.01,.008),
aligned with the signs of the distributed bending components, for all three
geometries. Do not change the section, yield law or positive-history assertion.
Repeat corrected smoke/rehearsal before freezing. See the initial incident.
