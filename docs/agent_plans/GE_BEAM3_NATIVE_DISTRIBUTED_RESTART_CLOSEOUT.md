# Native distributed-load restart and recovery closeout

Implementation freeze: `ecfb8cfa09520de26066036b60bcf43fe5de7145`.
Tree: `fe2dadc5995fb684498a3821a915d22f96c8cfe5`.
Base: `57ba457e5e2a17d4dce18d137c2b8f06e6ba8a86`.

This private development gate passes. It is not production qualification,
independent review, public integration or new scientific authority for shells.
The seven implementation paths and five unchanged source identities are bound
in the status. The physical retained operator, distributed-couple static
reduction and native distributed element are unchanged. Existing B2/B3,
qualified Q4/S3 mechanics, defaults, public aliases and versions are unchanged.

## Separate test inventories

| Lane | Passed | Deselected | Supervisor wall seconds |
| --- | ---: | ---: | ---: |
| Straight recovery/continuation smoke | 2 | 73 | 17.846 |
| Rehearsal: straight elastic | 25 | 50 | 20.053 |
| Rehearsal: curved plastic | 25 | 50 | 26.267 |
| Rehearsal: connected plastic | 25 | 50 | 168.008 |
| Prior distributed integration regression | 26 | 0 | 167.804 |
| Q4 current-state ownership regression | 12 | 0 | 8.828 |
| Legacy B2/B3 nonlinear regression | 5 | 0 | 4.619 |
| Frozen A: straight elastic | 25 | 50 | 20.454 |
| Frozen A: curved plastic | 25 | 50 | 26.664 |
| Frozen A: connected plastic | 25 | 50 | 164.791 |
| Frozen B: straight elastic | 25 | 50 | 20.051 |
| Frozen B: curved plastic | 25 | 50 | 26.466 |
| Frozen B: connected plastic | 25 | 50 | 165.181 |

Each child used one numerical-library thread, a 24-GiB process-tree memory
limit, a 600-second wall deadline and a 120-second CPU-inactivity watchdog.
At most three children ran together. Every process exited successfully with
zero active descendants, and every wave remained below 1,800 seconds. No
automatic retry or failed attempt occurred. The maximum observed child peak
memory was 309,628,928 bytes. These timings are diagnostics, not a performance
qualification or claim of complete dependency-graph certification.

The frozen cycles passed clean-head/configured-runtime guards before and
after execution. Their parent Git commands retain the existing inaccessible
global-ignore warnings; no configuration was changed. Worker stderr is empty.

## State, continuation and recovery

The distinct typed checkpoint binds externally authenticated bytes, complete
accepted history, model/support/DOF identity, effective line-force and
distributed-couple patterns, epochs, predecessor hashes, origins, internal
seeds, multiplicative nodal rotations and the general static response.

The live load programme owns the decoded initial snapshot. Shared solver
capture accepts only states matching that snapshot, and restores homogeneous
selection-only coordinates exactly. Raw initial states, foreign states,
altered supports, old schemas, missing external hashes and inconsistent
constant endpoint loads fail closed.

For all three geometries, actual restarted and uninterrupted accepted states
are byte-identical. Unloading continues the accepted material history; a
failed subsequent solve leaves the accepted state unchanged. The curved and
connected plastic cases contain respectively 1 and 17 positive accumulated
plastic rows; this is not an elastic-only test of a plastic section.

Physical recovery uses the preserved station equations and accepted internal
coordinates with fixed origins. It returns section-supplied fibre stresses,
physical strains/resultants, current/reference frames and positions. Applied
line forces and couples are load provenance, not additions to section
resultants. Recovery does not advance history.

Maximum observed errors across the three geometries:

- Free equilibrium norm: 1.5152188578189095e-14.
- Independently reconstructed station position: 1.1188630228279524e-16.
- Local/global physical work: 2.7755575615628914e-17.

Each is below its unchanged 1e-11 test gate. Tests also exercise typed
roundtrip, ownership, live-chain mutation, resealed state/response fields,
load/hash consistency, duplicate/nonfinite/noncanonical parsing, schema/count
and validation-deadline rejection. A self hash does not authenticate an
entire maliciously replaced valid history: the external SHA-256 remains
mandatory.

For each geometry, all 37 canonical scientific files are byte-identical
between frozen A, frozen B and rehearsal. All 26 prior distributed integration
scientific files match the preserved 65a52db archive byte-for-byte. The shell
and legacy regression inventories above do not imply full requalification.

## Preservation and next gate

External archive:
`C:\Users\AudunArnesenNyhus\AppData\Local\ANYrelease\ge-beam3-native-distributed-restart-ecfb8cf-20260908`.

455 data files plus the canonical manifest preserve the thirteen separate
inventories, raw outputs, stdout/stderr, XML, exact commands, supervisor
records, frozen source extent, unchanged source authorities and audits.
Every file count, byte count and SHA-256 was verified after copying.

Manifest: 75,865 bytes, SHA-256
`E35FF9C9227C973C9FE1CD42A7CB699ACAA6DE55C9BC514F2ABBAB9E414D15AB`.

All original run directories and historical evidence remain intact. Only the
hash-verified temporary transfer duplicate may be removed.

Independent review remains PENDING; production qualification, conservative
spectral authority and dynamic-reduction authority remain false. The next
gate is combined nodal/distributed-couple loading through one actual native
solver path and one authenticated restart chain. General nonlinear
section/path parity, consistent mass, modal/prestress/buckling, slenderness
and curved engineering qualification, independent review, package/public
integration and the objective beam-shell connection remain outstanding.

The full GE-B3 goal is incomplete. Main and the user-owned concurrent
ANYmesher compatibility plan remain untouched. Resume from this worktree;
do not rerun completed evidence merely because the application restarts.
