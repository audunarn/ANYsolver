# Bounded retained-system refinement capacity

Parent da4826c0cd6245ce4007dd4de4484d1f8b933b56. Preserve the N12 onset
NO-GO and all raw evidence unchanged. This stage enables and measures a small
finer-mesh path; it does not rerun the onset campaign or grant qualification.

Keep the condensed native generalized driver's sixteen-element limit. Add a
separate retained model identity validation route with at most 24 elements and
512 nodal DOFs, reusing every existing class, section, model, material-frame,
node/DOF, support and mutation check. Preserve identity bytes at old sizes.
Retained global unknowns remain bounded at 1024; translation-owned physical
factor coordinates at 512. Other spectral paths keep their existing limits.
The shared model identity hash describes the unchanged physical model, not a
claim of qualification at any size.

Do not modify potential, section laws, projection, quadrature, nonlinear
iterations/backtracking, linear algebra, tolerances, recovery, accepted-state
schema, mass, aliases, existing B2/B3/S3/Q4, or defaults. Preserve the independent
Decimal auditor's 256-coordinate/512-link bounds, 80/100 digits and 120s guard.

Before any loaded finer mesh: test retained/condensed capacity separation,
oversized rejection, exact supported maps, authority mutation, cancellation,
and old-size model identity equality. A fixed N4 programme (.01,.02,.03,
.03634765625), split using existing native stop_after and complete same-programme
replay in fresh Contexts, must reproduce both the archived bb40689 checkpoint
and original physical factor packet byte-for-byte. No cross-programme cast,
extension, reseal or reused old state is permitted.

Run the full retained translation controller and modal test suites alongside
the new capacity tests, in isolated bounded child processes. Run the capacity
tests twice into fresh directories and require byte-identical scientific data.
Test the new upper N24 model at virgin state with complete physical factor
capture and independent Decimal80/100 positive zero-inertia audits. This is a
capacity smoke only, not the missing loaded critical-point comparison.

Every worker: one numerical thread, 24GiB/tree, 600s; every wave:1800s,
at most three children; existing native120s Context and CPU-inactivity bounds.
Exclusive external outputs, complete process-tree termination, no automatic
retry. Failed smoke blocks loaded work. Measure larger restore/factor cost;
do not lengthen the native Context or reduce authority checks.

Only after this smoke succeeds may a separately frozen loaded finer-mesh
programme be prepared. Retain the exact 2% load AND displacement endpoint
criterion, source-bound continuum reference and scientific NO-GO precedence.
The full straight/curved production beam, postbuckling, material/state/solver
parity, independent review, installed opt-in and objective beam-shell joint
requirements remain active and incomplete.
