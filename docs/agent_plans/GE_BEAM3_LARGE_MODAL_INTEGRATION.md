# Larger modal numerical integration

Preserve the accepted N32 mechanics, factors and independent-reference comparisons.
This bounded successor consumes saved packets; it does not rerun equilibrium,
continuation, physical reference generation or loading history.

Explicit private capacity permits 640 coordinates,12288 rows and640 links;
the historical default remains256/8192/512. Exact integer sparse multiplication
and reassociation form original-coordinate material/geometric and mass forms
before complete-map congruence. Final entries remain rounded once. No dropped
nonzero, rank truncation, mechanics or tolerance change. Preserve signed roots,
repeated-mode clusters, paired vector halves and original-factor audits.

Development rehearsal before the final paired-map optimization:57 tests passed
in56.38s; bounded receipt58.153084s, peak255180800 bytes, exit0, empty child tree.
This is not the final frozen verification inventory.

Frozen verification: bounded existing-domain/exactness/cluster regression and
saved N32 plus/minus packets, at most3 concurrent children;600s/24GiB/one numerical
thread each, wave1800s, original CPU-inactivity120s, no automatic retry.
Workers use explicit bounds(-1,20),6 modes, root width1e-10/relative1e-12.
Original numerical kernel audits remain1e-11. Against preserved100-digit spectra,
require normalized eigenvalue difference<=1e-9 and physical mass MAC>=1-1e-9.
Do not erase the genuine negative lowest mode or label its state stable.
Require fresh replicas to agree byte-for-byte before accepting numerical integration.

This is private numerical support, not yet model-owned large modal integration,
independent-author review, public selector qualification or complete beam-shell parity.
The native owner120s capture deadline is not reset or extended. Any later long
model-owned spectral solve requires explicit immutable packet ownership and
full model/input checks, not reuse of an expired capture context.

Existing B2/B3/Q4/S3/defaults, production qualification and main remain unchanged.
NO_GO_PRODUCTION_RESTRICTION_UNCHANGED.
