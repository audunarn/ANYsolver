# Actual signed continuation of the precise candidate

Parent 5bd2b47f152ee36f70b89f0c221e4d2336aa6371. Bind its accepted signed N24
native scalar-work gate, arithmetic fingerprint and source seeds/checkpoints.
Archive manifest SHA-256
5be3566fbf5f420f73eef5856a56447c21d44ea8cd23303e714505f7ab9c063c.
Do not rerun the scalar gate or relabel an old arithmetic checkpoint.

Use each original precise seed and its empty accepted checkpoint, with the
already registered one-target programme +/-0.0065. Continue actual Newton from
the +/-0.006 equilibrium. No new targets, source fields, tolerance changes,
coefficient changes, initial-guess substitution or artificial lateral force.
Keep 24 elements, 49 nodes, 294 nodal DOFs, 870 retained coordinates, 192 stations.
The original left-quarter-span displacement control and crown dead load remain.

Positive advance is the smoke. If successful, run negative advance, positive
cancel-before-commit and positive cancel-after-commit concurrently. Then run
fresh positive and negative advance replicas and actual pre-commit-cancellation
resume. The latter consumes the exact cancelled output capsule, not a regenerated
substitute. No automatic retries. Seven registered workers, max three concurrent.

Require completed cursor1 for advances/resume, cancelled cursor0 before commit,
cancelled cursor1 after commit, and actual recorded target1 callback events.
Before-commit checkpoint must be byte-identical to source genesis. After-commit,
resumed and replicated checkpoints must match the ordinary completed positive
checkpoint exactly. Both negative replicas must match. Fresh owners mechanically
replay the actual output capsules and produce identical recovery. Each accepted
record must retain original 1e-11 residual/correction acceptance and virgin history.
Always save failed status and actual accepted prefix before adjudication.

Every worker: one numerical thread,600seconds,24GiB per process tree; unchanged
120second per-owner and CPU-inactivity deadlines;1800second wave. Exclusive fresh
external directories, checkpoint logs and complete-tree termination. Aggregate
only after all launched workers reach terminal state and every gate passes.
Preserve partial/failure evidence externally. No fabricated absent results.

Unit-test disposition/event mutations, exact hash-bound source custody, prefix
expectations and actual small-model corrected-policy cancellation/restart.
This gate demonstrates actual elastic spatial continuation and lifecycle only.
It does not establish stable postbuckling, a physical path from rest, full plastic
continuation, independent mechanical review, full parity, public qualification
or beam-shell connection authority. Independent spatial-stability verification
remains necessary after continuation. Existing B2/B3/S3/Q4 and defaults unchanged.
