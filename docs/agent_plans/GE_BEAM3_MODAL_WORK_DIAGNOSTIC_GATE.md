# Saved modal-work discrepancy diagnostic

Parent 599efa9da79c1ec980a022c5ed70dc4fecbfbe5f. Four research-only paths.
Preserve the completed a6914e6 comparison failure and its 50-file archive
manifest 78cf530f830e7dd24e13bba1732a0f6947b7897293505b006d12154ba0e8a92f.
The six-mode 2% criterion remains failed. No mechanics or qualification change.

## Frozen calculation

Both +/-0.0065 endpoints; all six saved modes in existing order, identity MAC
assignment. Bind original native L/R/H/B packets, accepted native100 spectra,
complete reference endpoint polynomials, and finest 128Gauss/12bubble reference
profile by their existing manifest and member SHA-256. Checks precede numerical
work and repeat after it; source/Python/runtime-version guard at both ends.

Native work: evaluate ||LRq||^2, q^T H q, ||Bq||^2 directly at Decimal80 from
original binary64 inputs. No assembled rounded K and no trace re-elimination.
Require reconstructed original-factor Rayleigh eigenvalue error <=1e-11.
Retain all 438 coordinates including massless traces; require positive material
work and physical kinetic work. Preserve the signed total and cancellation ratio.

Reference work: same four segments, 128Gauss, finest 306-coordinate basis.
Separate material first-variation-squared from force/moment geometric terms in
the existing full spatial Hessian. Reconstruct saved trial Hessian and physical
mass with normalized matrix error <=1e-11. Independently evaluate cross-product
modal densities, requiring discrepancy divided by 1+absolute component works
<=1e-11. Record separate Rayleigh errors without substituting them for the
original frozen eigenanalysis acceptance. Use each state's OWN modes and fields.

Subtract mass-normalized native/reference material and geometric work for every
mode. Verify their sum reproduces the total difference <=1e-11 normalized.
These differences are diagnostic: distinct equilibrium states and mode spaces
prevent attributing either difference exclusively to discretization or load error.
Do not invent continuum derivatives for discontinuous native rotations, change
mass, impose the continuation constraint on modes, tune coefficients or drop modes.

## Execution

Unit-test signed work, independent Fraction arithmetic, physical normalization,
cancellation, decomposition mutation, source hashes and absence of solver calls.
Freeze clean source first. Positive smoke once, then the other three fresh workers,
max3concurrent, one numerical thread, 600s/24GiB child,1800s wave;120s per inner
native/reference calculation and existing CPU-inactivity watchdog retained.
Two replicas per sign; require byte-identical diagnostic outputs and all launched
trees terminal before aggregate. No native/BVP/eigensolver execution or automatic
retry. Preserve partial logs if a diagnostic check fails.

Passing this diagnostic means consistent decomposition ONLY. The earlier modal
comparison remains false; no production qualification, finite-velocity dynamics,
independent author review, activation, publication or defaults change.
NO_GO_PRODUCTION_RESTRICTION_UNCHANGED.

