# Sixteen-macro arch capacity and accuracy diagnostic

Base 121d196b578ef949410abdaf9e83cabea0e0744c. Bind the completed explicit
HISTORY8M checkpoint-profile gate and preserved eight-macro comparison.
Change only this plan and a separately named sixteen-macro research test.
No source mechanics, section, loads, frames, recovery, quadrature, tolerances,
public aliases, defaults, package metadata or historical evidence changes.

Refine the same parabolic arch to sixteen three-node macros (33 nodes).
Use the same clamped endpoints, physical section orientation, elastic
generalized law and uniform reference-arclength pressure. Preserve the exact
three crown drops: .0009924835187201996, .002196594350540417,
.004368321784681136. Use the explicit caller-selected
GE_BEAM3_NATIVE_GENERALIZED_HISTORY_8M_V2 profile, not implicit acceptance.

Require three successful equilibria, a genuine mechanical checkpoint above
2 MiB and below 8 MiB, full authenticated decode/re-encode equality, and zero
plastic state. Compare unchanged recovered physical fields directly with the
same BVP9 continuum reconstruction at 128 native stations per target.
Record load errors and resultant-compliance norm, position, frame and
constitutive errors against eight-macro evidence. Do not interpolate final
reference fields or change 2% engineering error. Accuracy is measured, not
assumed; a completed comparison alone is not qualification or stability proof.

Run one bounded rehearsal first. On success freeze the exact two-path change,
then run two fresh-directory replicas and require byte-identical scientific
outputs. A failure preserves the complete partial output and blocks these
replicas; no automatic retry, no resource/validation limit relaxation.

Each child uses one numerical thread, a 24-GiB process-tree memory limit,
600-second wall and 120-second CPU-inactivity bounds, progress checkpoints,
and complete child-tree termination in finally. At most three concurrent
children; frozen wave at most 1800 seconds. No formal qualification campaign.

Archive commands, raw runs, baseline/source identities, supervisor observations,
and read-only comparison audit. Retain any real failure honestly. Independent
review remains pending. No public activation, push, merge, publication or
beam-shell qualification follows from this diagnostic.
