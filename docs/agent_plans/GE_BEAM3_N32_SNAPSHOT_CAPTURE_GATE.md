# N32 immutable compliance snapshot capture successor

Parent d356171f6566f1f2856a93225762055ec5edb527. The failed original concurrent
capture wave remains failed; all raw data and genuinely issued prefixes remain
immutable under manifest 7abaafe452adc09cd5258256c68231655d6970af628083fb63c7c2ab6934d3e4.
Its successful positive packet is 29,652,413 bytes, SHA-256
3d64d11ed63ae8401aa755a6d362a7891301306cd55f4225c924a40426ea5e13.
Both signed equilibria and all four issued/replayed prefixes already exist.
This successor never repeats the equilibrium solves or advances history.

Exact six-path implementation extent: this plan; new research worker
ge_beam3_n32_snapshot_capture.py and its test; new private
_ge_beam3_compliance_snapshot_guard.py and its test; existing private
_ge_beam3_elastic_seed_modal.py. No B2/B3/S3/Q4, selector/default or mechanics
coefficient changes. No spectral/reference code or acceptance threshold changes.

Explicit opt-in GE_BEAM3_COMPLIANCE_SNAPSHOT_GUARD_V1 copies the 18x18 material
compliance into immutable bytes after full native owner/input validation. Pure
original compliance arithmetic uses that copy only, with cancellation and the
original context's 120-second deadline at every existing inner checkpoint.
Full validation runs again in finally before any result escapes. The full model
is not repeatedly hashed within this isolated arithmetic: 133 expensive checks
become two full checks plus 133 cheap checks per factor. Core evaluation,
kinetic factor and packet validation remain fully guarded. Default None keeps
the original compliance callback path. Context time is never reset or extended.

The capture-only worker hash-validates both old replicas' seed, checkpoint and
recovery, as well as original equilibrium provenance. Native modal.prepare still
constructs its own native owner and restores/checks the exact checkpoint before
factor evaluation; the two extra historical enrollment/replay stages are not
repeated. The old programme cursor remains zero. This is not an accepted
physical loading history from rest, and not an eigenvalue qualification.

Require original full 582-coordinate factor shape and identity, 570 free,
189 massless nodal traces, 381 physical coordinates, 64 internal rotations and
all 32 positive compliance factors. The positive physical packet body must be
byte-identical to the archived successful original packet. New wrappers bind
the successor commit and explicit execution guard policy. Two replicas for each
sign must have byte-identical outputs. Negative equivalence to an absent old
packet is never claimed. Full source validation precedes mechanics and follows
capture; all existing hash-bound source inputs are rechecked before completion.

Tests cover factor bytes/error equality at three scales, full-check count,
immutable copy, source mutation, cancellation, unchanged deadline, bad policy,
nonpositive compliance, actual issued sources, archive mutation, original packet
equivalence/mutation, and capture-only routing. No independent author review is
claimed. After clean freeze run one positive smoke, then the other three workers
concurrently only if it passes. Max three workers, one numerical thread,
600 seconds/24 GiB per process tree, 120 seconds CPU inactivity, 1800 seconds
per wave. No automatic retry, no reused root, no partial canonical aggregate.
Preserve all terminal process receipts and any partial failures before closeout.

Success closes guarded factor capture only. N32 six-mode spectral comparison,
full inertia, broad nonlinear/material/state/restart parity, independent review
and objective beam-shell joints remain open. The genuine N24 2% comparison
failure is unchanged. No push, release, activation or version/default changes.
NO_GO_PRODUCTION_RESTRICTION_UNCHANGED.
