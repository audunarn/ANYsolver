# Live reference-identity construction optimization

Base19a44ca67cfdfe9bdb7e4b924babff0c94986798. Preserve the b97ac8c arch
rehearsal and profile archive: manifest10459 bytes,
SHA35B506E6509550C51E4E33378AE1FBA41ADEE6F7928482438D58DF1996C38458.
Eight/twelve-macro paths stopped at the unchanged120s Context guard; they
are not physical failures and will not be automatically retried.

Measured12-macroassembly:976fullguardcalls/4.301cumulative seconds;
23,472referencefingerprints/2.585seconds out of4.870 outer profiled seconds.
Optimize only repeated canonical identity construction; keep all full-model,
support audit, DOF, section, program, load and deadline checks at every call.

Add a private immutable CapturedReferenceIdentity to the retained operator.
Compute the ORIGINAL canonical fingerprint once. Every later call reads exact
live array contents/shapes/dtypes, half-frame branches, every regularity field,
all tolerances, centered coefficient pairs, node parameters and global schema
identifiers. Only a complete snapshot match returns the old identity directly.
On representation mismatch, the original fingerprint is recomputed and MUST
equal the original; do not absorb or replace the captured authority. This keeps
canonical equivalences such as signed zero and the centered schema override.
Changed methods or captured binding components fail closed. Element descriptors
use the same checked original hash. No global cache, object-only shortcut,
sampling interval, equation, reference evaluation or canonical schema change.

Test complete field/global/array/in-place/nonfinite/captured-binding mutations,
method replacement, exact canonical equivalence and the closed-world source
field map. Run existing full retained guard and centered reference suites.
Profile the same virgin12-macroassembly and compare all supported scientific
outputs with preserved archives, including arc and controlled plastic histories.
Only after cost and equivalence checks should a separately registered new8/12
invocation run. Never relax120s/600s/24GiB/one-thread/three-worker bounds.
No automatic retry; two deterministic qualification repeats still require a
complete passing rehearsal. Independent review remains pending and no public
integration, S3/Q4/B2/B3/default/package or historical evidence change occurs.
