# Actual N20/N24 spatial branch refinement

Parent334287b9b93814634de472457cb567db2ed5a3ac. Research-only successor;
all private owners, section laws and mechanical operators remain unchanged.
N24 original onset capsule518339bytes SHA256
dd7271775f79640f53cb142125b2c3cd20b850caa7c6ffb804a8c237940880c1,
under fine-onset-formal-b-61ac5d8 archive, point2/stage4. Authenticate and
replay its original programme(.01,.02,.03,.045225) as a numerical guess only;
do not rerun the onset campaign or import its history as a spatial trajectory.

Run new N24 signed small seeds at +/-.003 with the existing trial-only
initializer, then actual signed continuation to +/-.0045 and +/-.006 with the
existing explicit elastic seed owner. N24 has49nodes,24elements,192stations;
quartercontrolnode13,crownforce25. Same arch, material, supports and targets.
No tuning, stiffness scaling, stabilization or tolerance changes. Every
accepted prefix and complete capsule is mechanically replayed by a fresh owner.

First positive seed smoke. At most3concurrent workers thereafter, new external
directories,600s/24GiB/one numerical thread perprocess and1800s/wave; existing
120s context/CPU inactivity checks retained. No automatic retry. Preserve
all failures and actual accepted prefixes. Compare complete final replicas
where scheduled; no claim of two full qualification cycles in this development.

Compare four N24 endpoints(+/-.003 seed and +/-.006 continued state) against
unchanged spatial continuum equations/profile BVP9. The original N20 solution
is only its Newton initial guess. Resample the solved continuum at49nodes and
192stations; require exact agreement with the previously saved reference at
shared sites and its metadata, plus the established boundary/off-collocation/
norm and64/128energy checks. Do not reuse an N20-sized field map for N24.

Frozen acceptance for this refinement diagnostic: all four load, full nodal
displacement, station-resultant energy norm and strain-energy errors must
decrease from their bound N20 counterparts AND each remain below2%. Report
ratios regardless of pass/fail. Do not infer asymptotic rate or universal
locking immunity from two meshes. Both signs must pass. Deterministic
reference comparisons are repeated into fresh directories. No existing
evidence is rewritten and no absent proof is invented on failure.

This is one missing spatial branch/refinement layer, not full stability,
plastic continuation, path from rest, independent author review, public
selection, package release or objective beam-shell qualification.
NO_GO_PRODUCTION_RESTRICTION_UNCHANGED. Main and ecosystem work untouched.
