# Native uniform-arch load-peak diagnostic

Base 9d6882144fe01d462df3b532f1e6da247ffe9d7f. Preserve the accepted sixteen-macro
pre-peak result and explicit HISTORY8M codec. Change only one research slope
helper, one test file and this plan; no production/private source mechanics.

Freeze crown drops (.006,.008,.010) under a NEW complete displacement programme.
Use the exact existing sixteen-macro model, section, load, quadrature, physical
frames, supports, elastic history and recovery. Do not extend or reseal an old
programme checkpoint. No earlier failed worker is restarted or retried.

At each accepted state, reconstruct the existing analytic tangent and condensed
load-parameter column on a cloned model/store, preserving original history.
Solve the GENERAL bordered system K du + F_lambda d_lambda=0, du_control=1.
Do not invert K alone, use eigenvalues to classify, or finite-difference frames.
The crown-drop slope is minus d_lambda/du_y. Require finite slope, linear-system
residual <=1e-11 and accepted-origin equilibrium <=1e-11 (unchanged chain
validation tolerance, not a relaxed Newton tolerance). Verify snapshot hashes
before/after. All solver controls and Newton threshold 1e-12 remain unchanged.

First run the separate unit/reference inventory: exact scalar bordered slopes
including singular K at a turning point, malformed-input rejection, actual
analytic bar slope 8, and BVP9 reference samples spanning positive to negative
slope. Then run one bounded native rehearsal. Require complete three-target
equilibrium, exact authenticated checkpoint roundtrip, zero plastic histories,
direct BVP9 fields at 128 stations/target, constitutive consistency <=1e-11,
native positive first/negative last slope, decreasing last sampled load, and
all sampled load errors <2%. Record normalized slope errors diagnostically;
do not claim relative slope accuracy near a zero derivative or branch uniqueness.

Write the comparison before its final scientific assertions so a contradiction
is preserved, not mistaken for absent evidence. If the rehearsal fails, preserve
its result and logs; do not run replicas or tune mechanics/tolerances against it.
If it passes, freeze the three-path implementation and run both inventories
twice in fresh external directories; require byte-identical scientific files.

Each child: one numerical thread, 24 GiB process-tree memory, 600-second wall
and 120-second CPU-inactivity limits. At most three children, wave <=1800 seconds.
Complete tree termination in finally, immutable frozen source, exclusive outputs,
no automatic retries. Preserve all partial/failed evidence externally.

This can establish a sampled native descending branch with measured accuracy,
not an arc-length crossing, full spatial stability, full postbuckling qualification,
or independent authorship review. Those and mass/spectra, broader parity,
slenderness, package integration and objective shell connections remain required.
