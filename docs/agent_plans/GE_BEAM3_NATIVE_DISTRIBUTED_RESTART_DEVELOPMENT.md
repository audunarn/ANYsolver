# Native distributed-load chain restart and physical recovery

Base: 57ba457e5e2a17d4dce18d137c2b8f06e6ba8a86. Preserve the physical
operator, general distributed static reduction, native element and historical
evidence. Add a distinct typed distributed-load accepted-chain schema and
native recovery using the unchanged physical recovery equations.

Bind exact line-force/couple constant and proportional patterns, segment
parameter, model/support identity, epoch/predecessor/origin/seeds, native nodal
rotations and general static response. Validate complete history and free
equilibrium with the actual solver's line-force reference scale. Reject
nonfinite norms. Never reinterpret an old line-only or nodal-couple checkpoint.
Require an externally authenticated SHA-256, strict canonical JSON, exact
schemas, duplicate/nonfinite rejection, owned arrays and typed response fields.
No checkpoint grants production, conservative-spectral or dynamic authority.

The live programme binds the decoded initial state and its external hash.
Shared solver capture must compare caller states against that authenticated
initial state before accepting an owned copy. Verify initial displacements and
selection-only support reconstruction exactly; do not round a native pose
through LSQR. Match restart constant loads to the accepted endpoint. Preserve
the previous virgin-run programme identities and evidence where possible.

Recover physical station strains, section-supplied fibre stresses, resultants,
reference/current frames and positions from the accepted internal state with
fixed origins. Loads are provenance, not additions to physical resultants.
Check global/local work, independently reconstructed station positions and
unchanged history. Keep the existing production recovery paths unchanged.

Three cases: straight elastic, curved coupled plastic, connected curved plastic.
Test typed roundtrip, split-run versus uninterrupted bitwise accepted state,
unloading, failed-step preservation, resealed state/load/response mutations,
preflight rejection, ownership, active-chain binding and deadlines. Update only
the earlier raw-restart rejection message assertion to recognize the new
authenticated route; preserve its physical scientific packets. Also run the
Q4 ownership and legacy B2/B3 nonlinear regressions separately.

Smoke before rehearsal. Use one test shard per geometry (at most three in
parallel) to avoid a growing monolithic run. Each child uses one numerical
thread, 24-GiB tree memory, 600-second wall and 120-second CPU-inactivity caps;
each wave is bounded to 1,800 seconds. No automatic retry, exclusive output
directories and complete process-tree termination. Freeze after all rehearsals
pass; run two complete three-shard cycles and require byte-identical canonical
scientific output per geometry. Preserve all raw logs and failed attempts.

This remains a private development gate. Independent review stays PENDING.
Combined nodal/distributed-couple parity, general nonlinear section/path parity,
mass and spectral/engineering qualification, public/package integration and
the objective beam-shell connection remain outstanding. No default, alias,
version, existing B2/B3/Q4/S3 mechanics or qualification evidence changes.
