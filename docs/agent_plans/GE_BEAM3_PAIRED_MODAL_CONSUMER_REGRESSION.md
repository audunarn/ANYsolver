# Paired modal consumer regression

Base80544994ddc6436acb2ff9536832547f930c8ddc, tree
318324230577e0ae4ada91d3738ec8ca5fcba254. Two added research-only paths:
this plan and consumer test. Production modules and numerical tolerances
unchanged. Previous failures and passed rehearsal remain preserved.

Freeze before execution. Separate inventories: collect-only13 nodes; local
six nodes (straight/curved free-body six-rigid subspaces, connected shared
trace, proper-frame/reversal operator covariance, actual loaded conservative
state); three continuum nodes, one per previously frozen geometry; four
actual-preload buckling nodes, one each at1/2/4/8macros.

Use the paired adapter and explicit apply_mode_map. For continuum comparisons,
construct the station velocity map and apply it to the entire mode pair before
rounding. Keep frozen independent Ritz/ODE reference equations, reference-only
closed groups, N1/2/4/8 refinements, frequency/kinetic/correlation tests and
engineering limits exactly as previously accepted. Use the original test
functions with only their private adapter and station-map interface substituted.

Retain actual accepted material states for conservative prestress; execute the
original eighteen bisections and22 preloaded spectra per buckling geometry,
not unloaded substitutions or synthetic geometric tangents. Current frequencies
are not load factors. The inherited finest Euler criterion remains below2%.

Run local first. On pass, execute the three continuum lanes concurrently.
Only on their pass run buckling1/2/4 concurrently and then buckling8. Preserve
any earlier launched worker to terminal state; no expansion after failure.
No automatic retries. Fresh-directory deterministic repeats require a complete
passing rehearsal; this initial consumer wave does not authorize production.

Each child one numerical thread,24GiB process tree,600second wall,
120second CPU-inactivity watchdog. At most3 concurrent and1800seconds per
complete diagnostic wave; processes/descendants terminated on resource breach.
Exclusive fresh external outputs, frozen pre/post guards and terminal receipts.
Raw partial records stay external and are never represented as full evidence.
Independent review PENDING. Full production beam and objective shell-connection
programme remains active. NO_GO_PRODUCTION_RESTRICTION_UNCHANGED.
