# Sixteen-macro source/empty-handoff cost diagnostic

Base 09889e4d5f236c1514bf35bad2329174f7b836bf. Freeze only this plan and a
research test. No mechanics, checkpoint, solver or tolerance change.

Bind the accepted pre-peak sixteen-macro status and its external archive
manifest. Read the exact cycle-A checkpoint (2372889 bytes, SHA-256
C65730E97EC1097832C95DA21312EC67162A8B0B2422274C701BE4131B1EFCA0).
Reconstruct its exact existing translation programme/model. Require all
manifest, source and status hashes before native state evaluation.

Construct an explicit HISTORY8M TranslationArcSource and an arc descriptor with
three .12 steps, length scale .01, parameter scale .1, forward trajectory sign.
Run stop_after=0: this performs source/history validation and empty handoff
serialization only, not Newton continuation. Measure this phase, full decode
and exact re-encode separately. Keep timings in diagnostic output only.

Require paused status, zero new arc records, all four source snapshots,
unchanged source payload/prefix, exact roundtrip, retained source parameter
and size below 8 MiB. The existing 60-second validator deadlines remain
authoritative. Preserve any incident, logs and actually created packet;
never fabricate an output that was not created.

Freeze before one bounded invocation. No automatic retry or two-cycle
qualification claim: this is a capacity/cost diagnostic to decide whether a
separately frozen actual seeded crossing is feasible. Each process uses one
numerical thread, 24 GiB tree memory, 600-second wall and 120-second CPU
inactivity. Entire tree is terminated in finally. No other worktree/evidence
is modified. The descriptor is not authority to run its arc steps here.

If successful, preregister the actual crossing and its field/reference checks.
If a bound fails, preserve it and improve authenticated validation efficiency
without relaxing the bound or omitting history. Full beam qualification,
independent review, mass/spectra, parity, practical scale and objective shell
connections remain open.
