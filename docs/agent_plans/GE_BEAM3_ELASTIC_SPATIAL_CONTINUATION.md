# Authenticated elastic spatial continuation from an explicit equilibrium

Parent ed90c657da53eac511d3e35433e2c890c5c8155c. Private successor state owner,
not public selection or changes to any element formulation. Reuse the existing
retained physical assembly, full displacement-control border and natural-merit
Newton step. Do not change the ordinary virgin translation owner's singular
genesis rejection, accepted capsules or mechanical equations.

An externally hashed seed packet contains exact model/operator/control/load
identities, compensated geometry, physical frames, retained resultants, load
factor, displacement control and a source hash. It contains NO material history.
The new owner reconstructs from immutable virgin section origins, recomputes
equilibrium, compatibility, the remaining bordered correction and recovery,
and rejects any nonvirgin trial response. Only then is its distinct seed record
issued. The seed is an explicit equilibrium initial condition, NOT an assertion
of a physical loading path from rest or a plastic restart.

Subsequent records bind the seed, full target programme, predecessor and actual
replayed geometry, work, reactions, section history, recovery and correction.
Copies/foreign states and rehashed mutations are rejected. Capsule restores
reconstruct the original seed and every accepted record, never rewriting the
supplied capsule. Cancellation before commit returns the exact predecessor;
cancellation after commit returns the just-accepted prefix. No zero-load or
force-program capsule may masquerade as this successor schema.

Admission/unit tests precede an N20 smoke. Use the preserved positive-small
spatial initializer only as a seed INPUT; it is independently re-evaluated.
First continue .003 -> .0045, then .006. The existing .006 independent seed and
continuum result remain comparison inputs only; do not replace the trajectory
with that answer. Bound each actual child at 600s/24GiB/one numerical thread,
max3 concurrent and1800s/wave, no automatic retries. Existing120s context and
CPU inactivity limits remain. Split replay/continuation into fresh bounded
processes if needed, not by widening the frozen context bound.

Gate evidence must distinguish actual accepted continuation from initial
equilibrium provenance; production_qualified and physical_loading_path_from_rest
remain false. Elastic-only continuation is one missing part of full spatial
postbuckling, not a substitute for plastic history, refinement, source review,
general solver parity or beam-shell connection qualification.
