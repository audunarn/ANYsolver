# Preserved four-macro arch load audit (no mechanics or reference rerun)

Parent serialization successor: `f35d340652dc02a8e5b2691acd81e930b52d91b5`,
tree `f6a9a2836378f7d416cff77e193176ca7b349b36`. The resource administrator
accepted that narrow source correction for preparation, not for a new run or
mechanics qualification. This separate standard-library reader needs neither
a native beam solve nor a continuum BVP, and consumes no resource request.

## Bound inputs

- Original failed request: `b89e903dc17d44408ce0c42d11c6b61f` at `3f05a4ee`.
- Its unchanged checkpoint: 95,786 bytes, SHA-256
  `bcf90a0eca2923ee04ab00bf61b6e1eabdac40779671f90666bdbc2964420a1f`.
- Existing reference-comparison evidence in commit
  `3f05a4ee728f632aace088e67bdf9d97bcf805ed`, blob
  `9eae70f2b25c3764691c6756892cdd1427fa37be`, path
  `docs/reference_cases/ge_beam3_fibre_arch_comparison_development_evidence.json`.
  Its Git-normalized LF SHA-256 is
  `c2401dfc756cf7d95005ee6c92f43ae5960c82b361e7477a68cd2b676e025728`.

The four earlier nominal BVP9 loads, not absent failed-worker intermediates,
are used as reference data. This does not claim new same-runtime comparison
evidence, a new independent author, or an exact dyadic section certificate.

## Reader and publication contract

`ge_beam3_preserved_arch_load_comparison.py` imports only the standard library.
It verifies input hashes, strict canonical JSON, duplicate/nonfinite refusal,
checkpoint/record seals, record chaining, node/element counts, ordered targets,
zero saved physical histories and preserved reference scope. It recomputes
both two- and four-macro relative load errors; stored summary errors are not
trusted. This is byte/record validation, not a native mechanical replay.

The CLI binds a clean frozen source commit and the exact original reference
Git blob. It bounds reads, validates everything before creating output,
stages complete bytes on the output volume, flushes and verifies them, then
creates the destination exclusively. Pending files are diagnostic; a failed
write never creates a canonical partial file. Output must be external to the
repository. No input, prior output or historical evidence is overwritten.

Execute twice in distinct fresh external directories against the same frozen
source and preserved inputs. Require byte-identical load-audit outputs. These
are two cheap reader invocations, not repeated scientific or qualification
cycles. There is no numerical-library thread, memory-heavy solve or consumed
resource request to repeat. No new resource authorization is implied.

The output schema is `GE_BEAM3_PRESERVED_FOUR_MACRO_LOAD_AUDIT_V1`, with status
`DEVELOPMENT_LOAD_COMPARISON_ONLY`. The following remain false:

- historical request successful or its canonical comparison repaired;
- mechanics replayed or reference recomputed;
- geometry/frame comparison completed;
- same equilibrium branch proved or production qualified.

## Checks and next scientific step

Initial synthetic reader suite: 27 passed in 0.16 seconds. Tests include exact
input identities, mutated/resealed history and reference semantics, hash and
strict-JSON refusal, deterministic output, and fixed nonqualification flags.
No actual mechanical or reference computation is part of that suite.

The load-only audit can substantiate the observed refinement trend. It cannot
recover missing full station fields, prove branch identity or meet the full
engineering qualification. Next freeze a genuinely new refinement/accuracy
scope, rather than repeat the solved four-macro targets solely to obtain a
green process status. Preserve all old native/reference data and failures.
Any additional mechanical run must have a new exact resource request, bounded
process tree and no automatic retry. Do not tune mechanics or thresholds to
fit the observed load errors.

Existing B2/B3, Q4/S3, defaults, aliases, production recovery, dependencies and
historical qualification evidence remain unchanged. The broader standalone
beam and objective beam-shell connection goal remains active and unfinished.
