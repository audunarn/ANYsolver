# Explicitly bounded loaded-state refinement

Parent `36764d6c42652b946e3030aa7080aa9f3864f870` preserved the completed
four-macro loaded spectra with coarse errors up to 21.24 percent. Next use
the preserved six-macro native states, not another nonlinear history solve.
The original independent continuum equilibria and degree16 reference spectra
are reused byte-for-byte, bound through their canonical four-macro aggregate
SHA-256 c0a4a321eeba0de537282b4f20c951bf4678c9c50143663562b223eb8e7eca54.

## Explicit private allocations, unchanged mechanics

Six macros require 114 assembled spectral coordinates and 69 dynamic ones.
The private controlled-fibre constructor gains an explicit 128-coordinate
option; its default stays 80. Its packet identity binds the nondefault option.
The exact shifted-inertia backend gains an explicit 96-coordinate option;
its default stays 64. Admission is checked before numeric work, and the
reduced dynamic dimension cannot exceed its chosen exact-sign budget.
All existing time, memory, residual, root-width and cancellation controls
remain unchanged. No physical coordinate is removed or mass regularized.
The retained nonlinear replay still uses its existing 256-coordinate budget.

An exactly represented integer congruence checks the full 96-coordinate
arithmetic bound with positive, negative and zero modes. Rejection tests
cover unspecified/invalid budgets, defaults and propagation to exact fallback.
These are resource-allocation extensions, not new mechanical coefficients.

## One frozen diagnostic

Bind six-macro checkpoint 141754 bytes, SHA-256
20a1a48cf6e2dfdc669c4f2d823deb89a9da8c6560ab716eb32e95b1b917a940.
Replay its exact accepted history, derive prefixes at drops .01 and .055,
and compute six signed fixed-dead-load modes of each prefix. Remove the
continuation constraint as before and freeze accepted plastic coordinates.
Retain the nominal physical inertia and all first four-macro search settings.
No nonlinear or reference equilibrium solves are permitted in this wave.

One process, one numerical thread, 24 GiB process-tree bound, 600-second wall
bound, 120-second inactivity bound and unchanged 120-second spectral
constructor deadline. No automatic retry. Use fresh external exclusive
outputs; preserve partial diagnostics and publish only a complete validated
aggregate after process cleanup and final source/input checks.

Report signed roots, native/reference equilibrium loads, spectral residuals
and error changes relative to four macros. Improvement is an observation,
not a substituted qualification gate. No buckling factor or same-branch
claim is made; finer convergence and standalone/joint qualification remain.
Q4/S3, B2/B3, public routing, packages, defaults and historical evidence are
unchanged. No default activation or release is authorized by this step.

The first unfrozen wiring suite found two serialization integration errors
(11 passed, 2 failed): the plain-JSON aggregate serializer cannot encode native
dataclass packets. The native packet now uses its existing canonical serializer;
plain-JSON validation remains unchanged. The failed preparation XML is preserved
in temporary run `ge-beam3-six-spectra-prep-b731874c83f440f6a9068607aaa59033`.
No spectral solve or formal invocation occurred in this preparation failure.
