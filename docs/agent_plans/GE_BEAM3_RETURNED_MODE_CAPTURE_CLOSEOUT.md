# Returned-mode diagnostic closeout

Frozen commit18845e148067e57ce2ec58bad8af4efa1ec2faf0, tree
a7c670222c824a7b878a8fa67c0c75a8935fe63f. Three research-only paths;
no numerical kernel or beam/shell mechanics changed. Earlier NO-GO retained.

Separate inventories: two trace-ownership tests passed (no science records);
two specimen-capture tests passed (four external records). A passing capture
means the known rejection was observed, not that the modal specimen passed.
Both source guards passed, both process trees empty, no retries. Capture
child took5.217seconds before cleanup and used236900352 peak bytes.

Curved E, rho10000: reduced and unrounded-map original-chain signed Ritz
error2.795e-16. The rounded physical vectors instead give1.16104e-11,
above the unchanged1e-11 limit. Kinetic orthogonality remains near9e-16.
Physical expansion rounding is responsible for this observed rejection.

Straight E, rho1e6: reduced and unrounded-map mass-Gram error0.027717697;
physical expansion does not materially change it. The largest off-diagonal
is0.019599372 between zero-based modes8 and9, a closely spaced high-frequency
pair. Reduced signed Ritz error remains1.809e-13, but this does not substitute
for kinetic orthogonality. Physical expansion additionally increases original
signed Ritz error to1.44837e-10. Thus both extraction and representation need
attention; correcting only one cannot qualify this specimen.

Raw arrays and exact-dyadic comparison matrices are preserved externally,
with canonical JSON, separate JUnit counts, terminal receipts, frozen sources,
supervisor, read-only audit and a per-file byte/hash manifest. Original
temporary results and all historical evidence remain unchanged. The status
record binds the2013-byte manifest and the independent-review-pending audit.

Next bounded experiment uses these saved factors only: two-pass kinetic-metric
orthogonalization, then a paired high/low physical-vector expansion. Recheck
the complete original signed energy, physical kinetic Gram and roots at all
existing thresholds. Do not accept normalized vectors without those checks.
This is an arithmetic representation experiment, not a mechanics correction,
new accepted spectrum, continuum rerun or independent mechanical oracle.
Production integration must handle both halves explicitly; discarding the
low part or passing a factorized diagnostic as an accepted binary64 mode is
not permissible. A production numerical successor needs a separate freeze,
full rehearsal and deterministic regression before qualification claims.

Full straight/curved GE-B3 and objective shell-connection programme remains
active and incomplete. NO_GO_PRODUCTION_RESTRICTION_UNCHANGED.
