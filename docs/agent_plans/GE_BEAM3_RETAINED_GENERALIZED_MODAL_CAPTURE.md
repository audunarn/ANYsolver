# Authenticated retained-state conservative modal capture

Base ownership closeout: `4999af5`. Add a private adapter and tests only; do
not change existing element equations, state formats, paired spectral arithmetic,
public routes, defaults or qualification evidence.

Consume an externally SHA-bound complete retained accepted-chain capsule, never
a copied raw State or fabricated old condensed-state record. Authenticate and
replay it using the corrected Context before constructing any spectral factors.
Conservative dead line forces only: reject a program containing spatial couples,
even at a zero final parameter. No newly introduced nodal force/moment interface.

Replay current material response from committed histories, not prior increment
origins. Require unchanged histories and strictly elastic-interior stations with
the existing 64-epsilon yield-boundary margin. No plastic-frequency claim.
Preserve the original compliance/kinematic/geometric factor chain and physical
24-point cell inertia. Nodal rotation traces remain exactly massless algebraic
coordinates; do not statically reduce physical cell inertia. Bind current replay
to the accepted residual and unchanged 1e-11 equilibrium/compatibility gates.
The rounded stiffness is diagnostic; use the existing paired-vector eigensolver.

Test virgin factor equality against the previous native adapter across straight,
curved and common-rotation cases at slenderness 100, 10000 and 1000000; connected
capture, actual moderate dead-force equilibrium, authenticated roundtrip,
byte-determinism, external-hash/inertia/model/program/packet/cancellation guards,
and material branch rejection. Capture tests are not new continuum qualification.
Run a moderate smoke before remaining cases. Any failed smoke stops expansion.
Run the passing tests twice in fresh directories and compare scientific bytes.

One numerical thread, 24 GiB and 600 seconds per child; at most three children,
1800 seconds per wave, 120-second CPU inactivity watchdog, exclusive outputs,
terminal tree accounting, frozen pre/post source guards and no automatic retry.
Context additionally retains its 120-second cooperative bound. Independent review,
full material/engineering/joint/packaging gates and environment graph remain open.
NO_GO_PRODUCTION_RESTRICTION_UNCHANGED.
