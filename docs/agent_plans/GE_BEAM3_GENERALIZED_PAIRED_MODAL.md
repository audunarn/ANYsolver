# Private paired generalized modal successor

Base da757a510ac1b2c53601c659ce3f181e8c15fca8, tree
0e13f536507aed9bd5db121b06fb549cf360e54e. Preserve previous kernels and
all failures and saved-factor experiments. Five added paths: this plan,
private paired arithmetic, private paired kernel, current-material paired
adapter and test. No existing source file changed.

Reuse unchanged current-history/state/factor capture. Numerical successor
ports the observed two-pass kinetic orthogonalization and exact high/low
expansion. Existing signed roots, cluster/inertia searches, reduced action,
original signed Ritz and physical-inertia tolerances stay unchanged. Compute
full free-coordinate residual with original dyadic factor-chain actions and
both vector halves; keep the existing full normwise backward threshold.
The factor packet is unchanged input authority; the new result carries a
distinct private numerical policy and a hash over every result field.

Own and fingerprint both halves. No validated full_modes high-only alias:
it fails explicitly. apply_mode_map validates the complete result before
and after exact paired action. This is not a public solver/selector change.
No beam potential, section, material, mass definition, recovery, state law,
legacy beam or shell change. No public qualification or defaults change.

Freeze before execution. Separate inventories: collect-only35 nodes;
local32 nodes; inherited dense-local17; inherited prestress-local3; each
of three slenderness nodes covers four physical geometries/orientations and
all12 roots against the hash-bound100-digit supplied-factor audit. Run local
and old-route guard/state regressions first, then the three slenderness lanes.
No repeat cycle follows a failed rehearsal. Successful rehearsal precedes
separately bounded continuum and prestress regression and deterministic
fresh-directory repeats. Do not count the Decimal audit as an independent
mechanical oracle or a new engineering reference.

Tests cover low-part preservation, missing/wrong/mutated half, mutable data,
root/result identity, deterministic serialization, explicit collapsed-access
rejection, all preserved input/state guards, signed and complete clusters,
current material boundaries and trial ownership, actual tension/compression,
straight/curved reference systems and all registered slenderness specimens.

Each child one numerical thread,24GiB,600seconds,120second CPU-inactivity
watchdog; at most3 concurrent,1800second whole wave, exclusive fresh external
outputs, pre/post frozen guards, complete process-tree cleanup, no retries.
Raw incomplete results are retained externally; malformed or failed output
cannot become canonical qualification evidence. Independent review PENDING.
Full beam and objective shell-connection goal remains active and incomplete.
NO_GO_PRODUCTION_RESTRICTION_UNCHANGED.
