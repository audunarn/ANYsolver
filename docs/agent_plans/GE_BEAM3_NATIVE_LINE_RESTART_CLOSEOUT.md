# Native load-chain restart/recovery development closeout

Frozen `28734131b56ad879c02657c83b879ba1794b1a5b`, tree
`7fe0a71647764a6fc9acc5784fcbf78ddb1ea691`, passes this private development
gate. The six-path extent is in the plan/status. Qualified Q4/S3, B2/B3,
all physical kernels, material laws, qualification records and defaults remain
unchanged. This is not independent qualification or public activation.

## Separate inventories

- Straight restart/recovery smoke: 2 passed, 49 deselected; 13.037 seconds.
- Complete restart/recovery rehearsal: 57 passed; 223.130 seconds.
- Existing global line-load regression: 11 passed; 78.382 seconds.
- Q4 current-state ownership regression: 12 passed; 8.625 seconds.
- Frozen cycle A: 57 passed; 327.218 seconds.
- Frozen cycle B: 57 passed; 328.790 seconds.

All 84 canonical scientific JSON files are byte-identical between frozen
cycles. Both concurrent children used distinct fresh directories, one numerical
thread each, a 24-GiB process-tree memory cap, 600-second wall cap and 120-second
CPU-inactivity watchdog. All children exited with zero active descendants. No
automatic retry or mechanical test failure occurred. Exact clean-head and
configured-runtime checks passed before/after both cycles; this is not complete
dependency-graph qualification. Timings are diagnostics, not performance claims.

## Evidence now established

Actual straight elastic, curved plastic and two-connected-curved-plastic solves
complete two increments. Typed half-load prefixes resume through the real Newton
driver to byte-identical uninterrupted final displacements and complete element
states. A new unloading segment retains monotonically accumulated plastic history
and round-trips its complete typed chain. Deliberately failed restarted increments
leave accepted checkpoint states unchanged. Shared-node rotation and assembled
free-coordinate equilibrium pass for the connected case.

Snapshots bind exact constant pattern, proportional pattern and binary64
parameter; each effective pattern must equal every element state's applied load.
These segment descriptions are preserved as provenance. Uninterrupted and
resumed checkpoint envelopes therefore legitimately differ even though final
physical states match exactly. No history or load-factor relabeling is used.

The largest free residual norm is 1.0315221319200514e-13 against 1e-11.
Accepted physical station recovery preserves the fixed material origins/history
and uses recovered internal rotations/resultants. Independent lifted-Q2 position
and frame-work checks give maximum errors 1.1107649934270853e-16 and
2.220446049250313e-16 respectively. The connected plastic case has 40 positive
accumulated-plasticity rows; the single curved case has 11; the elastic control
has none. These are bounded case checks, not general section parity.

Strict typed parsing rejects duplicate/nonfinite/noncanonical data, booleans in
float arrays, schema changes, incomplete/skip histories, changed path/pattern,
origins/seeds/predecessors and support identity. Resuming with a different initial
constant load is rejected before Newton. State capture detaches typed arrays and
rechecks the operation guard even if serialization raises. Exact supported DOF
selection avoids introducing least-squares roundoff into the restart pose.

The separate global regression's 15 packets also exactly match preserved
f5cbe04 cycle A, including its archive byte counts/SHA-256. No historical
campaign was rerun for that comparison and no original evidence was modified.

## Archive and continuation

External archive:
`C:\Users\AudunArnesenNyhus\AppData\Local\ANYrelease\ge-beam3-native-line-restart-2873413-20260907`.
It contains 312 data files plus its strict canonical `archive-manifest.json`:
all run outputs/logs/XML, six frozen paths, six exact test commands, the historical
read-only audit command and supervisor observations. Every file byte count/hash
is verified. Manifest SHA-256:
`E092B644F1EE5D6FC7A70C6571ECB0EDDE1AAA81DF13B8FF0994174D41E1E03A`.
The committed archive index is an exact copy, and status is canonical JSON.
Original temporary run directories and all archives remain preserved; only the
fully verified duplicate transfer directory may be removed after final audit.

Independent review remains PENDING; production qualification is false. No
public selector, arbitrary initial-state dict, general staged/arc-length path,
mass/dynamic policy, release or default authority is added.

Next connect objective spatial nodal/distributed couples through the actual
native solver with their correct work and load tangents. Constant spatial couples
are nonconservative on SO(3), so do not use a fictitious rotation-vector potential
or a symmetric-only solve. Preserve this gate. General nonlinear section/path
parity, mass/modal/buckling/slenderness/engineering and packaging gates, independent
review and the objective beam-shell connection remain required for the full goal.
