# GE-B3 relative-spectrum installed-wheel checkpoint

This research/test-only checkpoint binds candidate
`72e095f5dba8bc0dca11b84131bcf1da4af06bc8`, tree
`664b2f518758f1011ea640988de5230f69e0394d`. No production source,
mechanics, state schema, tolerance, defaults, aliases or earlier evidence is
changed. Independent review remains PENDING.

## Isolation and preservation of identities

The test exported only source/package inputs from that exact Git commit. It
did not build from mutable checkout source. All 65 preserved framework,
native-control and reference-spectrum module identities were checked against
the archived tree. The build copy deliberately used CRLF for those modules;
normalized UTF8/LF identities and actual installed byte hashes were checked
separately. This is a line-ending fixture, not a mechanics change.

One private wheel was built without build isolation and installed into a fresh
external virtual environment, using only ten pre-existing, hash-verified offline
dependency artifacts. Their old requests were not executed or reused. No
production environment, network install or published package was changed.
The source archive, wheel, source map, executable smoke script, receipt,
stdout/stderr and complete test diagnostics are all preserved.

Two fresh isolated processes ran in distinct directories. Repository paths
were removed and research/test imports were prohibited. Every loaded ANYsolver
module originated under the installed target. No new private symbol was
exported from the package root. Each child had one numerical-library thread,
a 180-second wall timeout, streamed external logs and process-tree termination
on timeout. There was no automatic retry. These were small correctness checks,
not performance or formal qualification waves.

## Installed results

The installed-wheel test passed in **29.58 seconds**, without test failures.
The installed environment was Python 3.13.9, NumPy 2.5.2 and SciPy 1.18.1;
the source-runtime checks used different NumPy/SciPy versions. No broad
bitwise equality between these runtime versions is claimed. The separate
static suite passed 28 checks in 0.58 seconds: five new, six V2 reference,
six V1 factor, six straight-prestress and five loaded-modal checks.

Both eight-macro straight reference cases, L/h=100 and 1,000,000, passed the
first-six-frequency 2% engineering check against a separately reconstructed
continuum ODE in the standalone script. The largest discrepancy was about
1.4821%; the three paired bending modes satisfied the unchanged 1e-11 check.
The small driver test retained 1e-20 as distinct from exact zero.

Native states were serialized through their typed codec, JSON-round-tripped,
then restored through native validation before replaying the reference API.
The complete packet/modes matched the original call exactly. This does not
claim that directly JSON-decoding a diagnostic dataclass representation is a
valid restart operation. A separate actual force-control equilibrium produced
a nonvirgin state; the reference-only API rejected it both with its displaced
coordinates and with a falsely zero displacement, without changing history.

The two complete result records and all three diagnostics per process were
byte-identical. The reference diagnostics retain native packets, modes, typed
states and continuum targets. The loaded diagnostic retains the actual native
force-control checkpoint. No absent result was fabricated.

## Exact artifacts

Archive (25 files, 4,836,387 bytes):

`C:/Users/AudunArnesenNyhus/AppData/Local/ANYrelease/ge-beam3-relative-spectrum-installed-20260907-578d454bdfc7`

The wheel is 1,379,054 bytes, SHA-256
`578d454bdfc7d2416dbf35711ec44ddd4d33ce3cc2ec1cb990fe000011cfe8cb`.
It retains development metadata version 0.4.2; **it must not replace an already
published 0.4.2 artifact**. This checkpoint authorizes no publication or tag.

Each result is 16,536 bytes, SHA-256
`60f277b70ab4732d3539343164f90b873ade798d77e914814adb13365bc75394`.
The exact source archive is bound by SHA-256
`912302190c9dec9e13a577680f5477eca44b5058c737a7eb80190f845a3b3167`.
The per-file manifest and dependency identities are in
`docs/reference_cases/ge_beam3_relative_spectrum_installed_evidence.json`.
All archive bytes/hashes were verified from the normal workspace context.
Only verified relay duplicates are removed; original outputs and older
archives remain intact. Warnings were limited to inaccessible Git ignore
configuration and deliberately disabled byte-compilation.

## Remaining programme

Next is the signed prestressed high-contrast path: the reference PSD solver
cannot replace it or hide negative modes. General curved/slender qualification,
broader section/load/constraint parity, nonlinear state coverage, independent
review and objective beam-shell connections remain open. The programme is not
complete and the private curved candidate is not publicly qualified.
