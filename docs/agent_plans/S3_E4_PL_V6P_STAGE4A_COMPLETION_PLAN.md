# S3 V6P Stage 4A Completion

V6P joins the 69 immutable V6M scientific leaves with the 12 immutable V6O
missing leaves. It does not rerun mechanics. Each source group is validated
against its own frozen candidate and catalog, and the join is permitted only
by the accepted V6N guard-optimization equivalence record.

The joined 81-record evidence is reconstructed into the three registered
diagonal proofs. The independently authored Stage 4A checker verifies each
proof twice in fresh bounded processes. Replica bytes must agree. Two complete
fresh-directory completion cycles must produce byte-identical canonical
results.

The gate is fail closed. A process or evidence defect is BLOCKED, a scientific
contradiction remains NO-GO, and a complete accepted result authorizes only the
next separately frozen Stage 4B extension. S3 activation and default changes
remain unauthorized; Q4 is unchanged.
