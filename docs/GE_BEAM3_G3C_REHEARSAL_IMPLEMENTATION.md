# G3c authentic rehearsal implementation

Additive private harness from accepted design closeout 6d4eeb8. No inherited
owner, preflight, mechanics, source map, scientific tolerances or defaults change.
This implementation is not execution authority until its clean commit and full
input inventory receive independent scoped acceptance.

The runner admits exactly ten ordered waves: none, cm3, positive, preflight,
virgin_review, negative-0 through negative-4. Each history is one real owner
through all five loads (plus four CM3 preparation commands). Every initial and
accepted packet is stored exclusively and manifests bind the registered command
sequence, scale, complete prefix chain and exact bytes. Actual stage tests retain
native 42-variable/internal/Schur and same-pose transaction diagnostics.

The 142 original negative member/origin probes retain their prescribed rejection
phase. Packet attacks mutate actual supplied raw checkpoints, repair available
hashes, preserve changed targets and bind before/after hashes. Coherent pose
attacks update every shared native/graph copy and adapter pose commitment.
Semantic attacks must pass preflight then fail genuine final-prefix replay;
source reads and review copies are separate attacks, never frozen-file edits.
Only exact ValueError diagnostics pass; resource failures and arbitrary exceptions
do not. Positive controls use external producer-manifest digests, not self-derived
admission authority. Synthetic static fixtures explicitly do not prove mechanics.

Evidence DAG: reviewed commit/input list -> lease -> raw packet/attack files ->
worker artifacts and passing test completion -> parent terminal process receipt
with closed stdout/stderr hashes -> ordered wave diagnostic. The lease references
only previous terminal waves, never its own outputs. Every predecessor receipt
is checked against the same candidate/review, exact wave inventory, resource
bounds, raw file hashes, packet directory contents and selected origin hash.
The next wave cannot bypass a missing or failed predecessor.

Process handling retains the reviewed complete-tree Job wrapper and close_tree:
600 seconds/child, 1800 seconds/wave, 24 GiB/tree, one numerical thread, at most
three workers and 120 seconds without CPU/output progress. Stop unlaunched work
after failure; already launched trees drain. No automatic retry. Runtime and
full authority checked before numerical imports and at finalization. Existing
normal-token policy requires no new global resource ledger or lock.

Commands (isolated Windows capsule environment):

    python -I -S -B scripts/run_ge_beam3_g3c_rehearsal.py --wave none --review PATH --review-sha256 SHA
    python -I -S -B scripts/run_ge_beam3_g3c_rehearsal.py --wave cm3 --review PATH --review-sha256 SHA --previous-wave PATH --previous-sha256 SHA

Each later command similarly binds its immediately preceding ordered wave.
Fresh external Temp directories are printed; preserve/archive them with exact
hashes after independent evidence inspection. Wave files are diagnostic status,
never full G3c scientific GO. Full 375/3450 histories/prefixes, MO16 recovery,
remaining transport/atomicity and two formal cycles remain open, as do G4/G5.

Before execution, independent review must cover actual constructors, exact
exception allowlists, complete receipt DAG and failure/no-partial behavior.
No main update, publication, qualified shell change or public activation occurs.

## Pre-execution review correction

Initial freeze 062f760 was blocked before numerical execution. RI-01 found
rehashed negative receipts could change the reason or before-hash. The corrected
parent verifier reconstructs the registered attack bytes and complete expected
record from the verified producer origin and frozen source/review input; it
compares exact raw bytes, original hash, intended diagnostic, member and phase.
Rehashed reason, before-hash and attack-byte regression probes must all reject.

RI-02 found the wave clock omitted coordinator validation. One monotonic start
now precedes coordinator parsing/authority and is shared through prerequisites,
worker deadlines, final authority and staged publication. No worker launches
after that budget is exhausted. Published elapsed time includes these phases.
Manifest/completion/status JSON uses exclusive same-volume pending files and
Windows atomic rename; a timed-out publication retains only pending diagnostics.
No automatic recovery or overwrite is performed by the runner.
