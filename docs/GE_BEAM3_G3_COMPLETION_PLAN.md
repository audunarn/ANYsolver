# G3 completion: positive common-pose adapters

Design candidate, not execution or qualification authority. Base is accepted
G3b closeout `773b61b5370a5f8365a376e958244defe1e9cb79`, tree
`768eb0d9d3b302020ee336eecbc4451244b208f9`. G3a S15/native S18 and G3b
S16/reference-mixed S18 are accepted; their immutable evidence is inherited,
not rerun. G3 remains incomplete until positive G3c S17 and its finite mixed
transaction/cache/restart obligations pass. Negative adapter rejection is not
positive qualification. G4 histories and G5 public integration remain later.

## Completion requirements, not a reduced substitute

Finish four explicit private native/B2, native/B3, native/Q4 and native/S3-V2D
common-pose adapters; then a bounded multi-joint/multifamily graph owner with
actual family mechanics. A pair-level port proof, synthetic spring, reference
matrix, or existing one-shell owned workflow cannot replace that owner.
No selector/default/package change, native/legacy/Q4/S3 local mechanics edit,
old evidence edit, material-history expansion or ecosystem change is permitted.

Five positive fixtures are completely specified in the companion JSON:

| Fixture | Elements | Nodes | Rigid pose joints | Support/joint rows |
| --- | ---: | ---: | ---: | ---: |
| J_B2_PAIR | 3 | 8 | 2 | 24 |
| J_B3_PAIR | 3 | 9 | 2 | 24 |
| J_Q4_PAIR | 3 | 10 | 2 | 24 |
| J_S3_PAIR | 3 | 9 | 2 | 24 |
| J_MULTIFAMILY_LOOP | 6 | 17 | 8 | 54 |

Each pair contains two native arms attached at distinct ports of one real
nonnative element, with 1/8-length eccentric offsets. Both native roots are
supported. The loop contains two incident native elements and one element of
each nonnative family, joined at two ports per family; its native root is fixed.
Nodes remain disjoint across families. Native/B3 ordering is end-midpoint-end.
Port frames are explicit physical inputs, not inferred global-axis roll.
Element-owned section triads remain distinct from nodal port frames.
Even-numbered joints use master Rz(pi/2) and slave Rx(pi/2), explicitly stored
as exact signed-permutation matrices, so their relative C is nonidentity.
Odd-numbered joints retain identity frames. Check nontrivial C, master/slave
material bases and work-dual multiplier transformations; a common global
rotation alone cannot test a missing C factor.

## Equation and state map to be independently reviewed

For each port freeze reference position X and physical reference frame D0.
The authoritative current pose is x=X+u and D=Q D0. The graph owns one Q per
physical native/shared-pose node; incident element triads are never averaged.
For master m and slave s define a=Dm0^T(Xs-Xm), C=Dm0^T Ds0 and

`gt = Dm^T (xs-xm) - a`, `gr = Log(C^T Dm^T Ds)`.

Use the complete six-component multiplier work `mu^T [gt,gr]`. Multipliers
are material port components. Derive spatial action/reaction and offset moment
from this work, not from equal additive rotation slots. The private existing
RigidPoseJoint is source-bound background; new independent value, differential
and Hessian reconstruction must verify its use in the actual graph chart.

The owner chart is committed-base left multiplication:
`Qtrial = Exp(eta) Qaccepted`. Rejected proposals never become new anchors.
With P=diag(I,Jleft(eta)), chart residual is `g=P^T r` and its actual derivative
is `H=P^T J P + (dP)^T r` when J differentiates spatial wrench with respect
to spatial increments. Include the correct selected chart on every column;
do not reuse a spatial-row/additive-column Jacobian as a symmetric Hessian.
Joint multiplier blocks include both Jg and `sum(mu_i Hess(gi))`.

If a family uses total additive coordinates theta, its physical pose is
`Exp(theta) D0`, NOT Exp(sum accepted increments). Derive the map from the
graph's issued Q to the family's chart and both derivatives. A principal Log
alone is not an owner rebase policy. Before implementation acceptance, prove
coordinate-chart continuity, explicit accepted-state re-expression/rebase and
unrestricted common rigid motion while preserving the relative 0.9*pi bound.
Never silently branch-switch, erase history, rotate reference material axes,
or reject a valid common rigid motion merely to make a test easier.

Native residual, 24-coordinate stationary elimination, material law and
recovery remain the accepted exact-elastic route. B2/B3 use their actual scalar
elastic nonlinear response, not `compute_internal_forces` or K*u away from the
reference state. B3 generalized-section corotational rejection remains a true
legacy unsupported combination, not a GE capability to fake.
The selected legacy route is `corotational.corotational_element_response`
with tangent_mode='consistent' and history-free scalar local
geometric_nonlinearity='von_karman'. It is not B2's separately selectable
`_compute_corotational_nonlinear_response`; never select or double-wrap that
different path. These choices are explicit fixture definition fields.

The current generic legacy corotational route rotates local forces forward;
its consistent Jacobian uses finite-difference rigid-frame sensitivity. It is
not automatically a conservative potential or fully analytic operator. Bind
this fact explicitly. No new interface derivative may use numerical frame
differentiation. Establish the required analytic chart pullback and distinguish
an inherited family derivative from the new adapter's derivative. If the
unchanged family route cannot satisfy the frozen work/directional/objectivity
gates, preserve the counterexample and propose a separately reviewed successor;
do not symmetrize it, tune its coefficients or relabel it conservative.

For Q4/S3, the existing private Procrustes variational shell V2 map is the
selected background work convention. Its exact local shell operators remain
unchanged. Verify the complete deformation map, its first/second derivatives,
actual spatial/chart conversion and material-origin linkage independently in
the new graph owner. Existing owned-shell acceptance is not generic adapter
authority. Numerical drill work/reactions stay separate from physical resultants.

## Ordered implementation funnel

The contract review includes a fatal inherited-route admissibility gate BEFORE
any positive adapter implementation. Source inspection has found an unresolved
B2/B3 obstruction: `corotational._beam_rigid_rotation` averages principal nodal
rotation Logs. For an undeformed x-axis beam use d=1/10000 in units of pi:
B2 nodal twists [-d,d], or B3 twists [-2d,d,d]. Their mean is zero. Superpose
a common pi twist. Principal Logs now average to zero for B2 and -pi/3 for
B3, whereas covariance requires pi. The corresponding rotation matrices differ
(B3 by 2*pi/3 modulo 2*pi). Relative rotations remain at most 3*d*pi, far below
0.9*pi. Re-expressing total rotation vectors cannot change this internal
Log(Exp(theta)) calculation. This is a source-equation counterexample, not a
new executed graph qualification result or a change to accepted G3b evidence.

The static test reproduces these principal-angle identities with exact rational
arithmetic. It is not a production-function execution. Before proceeding,
independently adjudicate the bound source trace and freeze a separate remedy
contract. A corrected family frame or an objective adapter-side moving reference
is NOT authorized by this design: either changes the selected route and needs
explicit equations, work/tangent/state maps and reviewed authority. Do not narrow
the common-rotation domain or pass this prerequisite through expected rejection.
While unresolved, positive runtime implementation and formal G3c execution are
blocked; G3/G4/G5 cannot be declared complete by this contract.

1. Contract/source/admission audit: hash-bind these equations, actual sources,
   definitions and cases; independently review before runtime implementation.
2. Local adapter smoke: begin native/B2, then B3/Q4/S3. Check physical values,
   analytic work-dual maps, all first/second derivative terms, reference
   stiffness, common rigid motion (including pi and 1.4*pi cases), proper global
   re-expression, reference reversal and explicit normal authority. Use frozen
   nonzero multipliers and all three directional steps, not the best step.
3. Real-family trial bridge: reconstruct each family's response independently
   at the same pose/origin. Verify native stationary/Schur equality including
   nonzero internal residual and load, physical recovery and interface wrench
   balance. Conservative cases require pulled-back symmetry; nonconservative
   cases require their actual derivative/work policy, never energy claims.
4. One global owner: complete all five fixtures, two-joint and loop assembly,
   deterministic ordering, Newton/line search, prescribed noncommuting roots,
   loading/unloading, prepare-all acceptance, injected last-family failure,
   cancellation, issued-token rejection, factor identity and independent histories.
5. Authenticated replay: freeze an explicit new G3c envelope, not a modified
   G3a/G3b schema. Validate all shapes/IDs/policies before state construction;
   replay genuine accepted prefixes and require continuation equality. Test
   graph, normal, chart, section, constraint, epoch, factor and history tampering.
6. Freeze complete candidate/runner/test/environment graph; independent review,
   full rehearsal, two fresh bounded formal cycles, independent evidence review
   and static-only closeout. Then compose accepted G3a/G3b/G3c in a static G3
   completion record. No subgate or pair fixture alone closes all G3.

Every trial token binds owner, graph, adapter IDs, accepted epoch/hash and
sequence. Prepare native internals and every nonnative state before one immutable
publication. A failure commits none and releases all owned locks. External
definition mutation invalidates the owner; do not silently repair and reseal.
Checkpoint limit is 8 MiB and 128 accepted entries. Freeze the complete nested
schema before coding replay. Cache identity includes state/pose, all family
definitions, section/reference/normal authority, constraint chart and loads.

## Required evidence and safeguards

The following deterministic rules define the five variants, not merely labels:
BASE uses the listed records. SHUFFLED_INSERTION reverses each node, element and
joint insertion list without changing any ID or connectivity. RENUMBERED maps
node n to 10000+7*n, element e to 20000+5*e, joint j to 30000+3*j and maps all
connectivity, supports, load and joint references identically. Re-sort canonical
outputs by these IDs; inverse-map before physical comparisons.
CONNECTIVITY_REVERSED uses local permutations NATIVE/B3 [2,1,0], B2 [1,0],
Q4 [0,3,2,1], S3 [0,2,1]. Physical node IDs, positions, orientation vectors,
owner normals and physical port frames are unchanged; derive the element triad
anew from reversed connectivity and the same physical orientation. Transport
the scalar section through that triad, never reverse the physical shell
director or reinterpret top/bottom. This is numbering reversal, not physical
director reversal. No new material or shell recovery convention is authorized.
PROPER_GLOBAL_TRANSFORM uses W=[[0,-1,0],[1,0,0],[0,0,1]], t=[2,-3,1]:
X'=W*X+t, u'=W*u, Q'=W*Q*W^T, D0'=W*D0, force'=W*force.
Orientations, owner normals and spatial prescribed increment vectors multiply
by W; material port multipliers remain unchanged. Transform reactions and
direction vectors likewise; inverse transport before invariant comparisons.

At the five load stages, the smallest fixed node has prescribed orientation
targets [I, Exp(v1), Exp(v2)Exp(v1), Exp(v1), I], respectively; all other fixed
nodes remain at I. Here v1/v2 are the two frozen root_increment_vectors in
their listed order. This is an absolute target sequence, not additive
accumulation or a rebase on rejected trials. Fixed translations remain zero.
Common rigid-motion objectivity is distinct from coordinate re-expression:
keep X, D0 and reference sections fixed; transform current x*=W*x+t and
Q*=W*Q (NOT W*Q*W^T), so u*=W*(X+u)+t-X. Rotate spatial loads by W and
transform prescribed current support x/Q by these same left-action rules.
Use the listed common_rotation_vectors for W=Exp(vector) and t=[2,-3,1].
Do not rotate the reference configuration in this diagnostic, or apply a
rigid displacement against untransformed fixed supports. The separate
PROPER_GLOBAL_TRANSFORM above is a coordinate re-expression and therefore
does transform reference coordinates/frames and conjugate Q.

Apply BASE, SHUFFLED_INSERTION, RENUMBERED, CONNECTIVITY_REVERSED and
PROPER_GLOBAL_TRANSFORM to all five graphs: 25 declared graph variants. Exact
insertion-order serialization must match; physical transports use their explicit
maps, not raw component equality. Additionally check the joint multiplier work,
common rigid rotations and chart perturbations in the frozen fixture JSON.
Use load factors [0,0.5,1,0.25,0], the supplied noncommuting root increments,
and all three force scales. Cases cannot be dropped after observing results.
History-free exact elasticity only; nodal dead forces and prescribed physical
root orientations are the load channels. Nonzero port multipliers exercise
moment work without inventing an unqualified nodal-couple loading API.

Limits: 8 total elements, 32 nodes, 192 external coordinates, 384 external plus
native internal coordinates before multipliers, and 96 constraint rows. Check
limits and connectivity before allocation/evaluation. Component rigid support
rank uses the inherited 64*eps*max(shape)*max(1,sigma_max) floor, not a relaxed
stiffness cutoff. No pseudoinverse, penalty, clipping or empirical stabilization.

Invariant/work/covariance/Schur checks use norm(a-b)/max(1,norm(a),norm(b)) <=1e-11
with explicit unit length/force/moment scales. Directional checks <=1e-7 at each
h in [1e-4,1e-5,1e-6]. Incidence/affine identities use rational equality.
Rollback, restart and deterministic scientific outputs use byte equality.
Retain stricter native/local guards. An inability to meet them is a finding.

One numerical thread, 24 GiB/tree, 600 seconds/child, 120 seconds without CPU
or output progress, at most three children, 1800 seconds/wave. Checkpoints:
capture, pose, family trial, local solve, assembly, prepare, acceptance, restart,
output. Kill/reap complete child trees on breach; keep partial diagnostics,
retry nothing automatically and never reuse a consumed formal directory.
Separate contract, local-adapter, graph, state/restart, infrastructure and formal
inventories. Timings are diagnostics, not qualification speed thresholds.

Terminal precedence: baseline/authority; process/evidence; state/restart;
pose/work/derivative; graph/assembly; then scoped positive G3c confirmation.
All required adapter families and all five fixtures must pass before
`PROVISIONAL_GO_GE_BEAM3_G3C_COMMON_POSE_ELASTIC_GRAPH_ONLY`. Unexecuted, partial,
negative-only or reference-only evidence cannot produce that terminal.
Only a later static composition may state G3 complete. No claim of G4/G5,
full domain parity, public activation, release or changed defaults follows.
