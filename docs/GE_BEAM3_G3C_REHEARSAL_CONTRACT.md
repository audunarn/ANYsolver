# G3c complete-history rehearsal and authentic mutation freeze

Design only, based on clean ef3df43c4e724e7b2e3d4b98510ed4e2eecd3367,
tree 772e7b6708c9c5d2d37f42261662e871584a9de8. This four-path increment
does not run mechanics or authorize execution. Implementation must be separately
frozen and independently reviewed. No inherited mechanics or evidence changes.

## Histories and bounds

Run each of B2, B3, Q4, S3 and multifamily-loop BASE graphs through NONE at
S0=0.01, then CM3 at S2=10.0. These are members of the existing complete matrix,
not new mechanics. NONE has five genuine load stages; CM3 has four genuine
preparation stages followed by five load stages. Preserve every initial and
accepted checkpoint: ten histories, 70 stage events, 80 prefix packets.
These are separate inventories, not 80 restart tests.

Use five single-history children in each sequential history wave. Require the
preceding wave to pass before launching the next. Retain inherited 600-second
child, 1800-second wave, 24-GiB process-tree, one-thread, three-worker and
120-second inactivity limits. Every launched tree must reach terminal zero;
close handles in finally, preserve failed/partial external logs, never retry.
Measured two-stage smoke took at most 119.663 seconds per child; initial B2
replay took at most 166.511 seconds. These measurements do not guarantee that
complete CM3 histories fit. A breach blocks; do not silently extend limits.

Every accepted stage verifies the existing native full/internal/Schur,
accepted-origin and transaction invariants at unchanged tolerances. Preserve
the complete actual packet, not a synthesized candidate state. Full G3c
MO01-MO18, 375 histories and 3450 prefix tests are not replaced by this rehearsal.

## Checkpoint authority and positive controls

Write each packet exclusively outside the repository. Completion binds ordered
case/prefix, filename, positive byte count and SHA-256 for all expected packets,
candidate/tree/runtime, lease, tests, stdout/stderr and process terminal state.
Resolve only regular non-reparse files inside that child's directory. Reject
missing, extra, duplicate, truncated or mismatched files. No partial canonical
completion on failure. Retain immutable producer manifests and raw packets.

Use exactly the inherited origins: B2 BASE S0 NONE prefix2 and multifamily-loop
BASE S2 CM3 prefix9. Before any mutation, genuinely resume each in a fresh child
and require exact final packet bytes. Expected input digests must come from
the verified producer manifest, not from an untrusted packet being admitted.
Bind the entire producer/review/lease/hash DAG before numerical imports.

## Authentic mutation inventory

The registry expands all 24 inherited categories, all 71 members, at both
origins: 142 negative probes. No representative-only substitution or no-op may
count. Preserve each attack packet, changed-path description, before/after
hashes, exact rejection phase and observed diagnostic externally. Deliberately
rehashed negative packets have separate attack authority and are never accepted
positive evidence. Successful rejection requires the registered reason and
phase, not an arbitrary exception, process failure or timeout.

Structural/type/schema/command/known-link probes must reject in full nested
preflight before a counting construction sentinel is called. The implementation
review mutation belongs to the real runner review verifier before construction;
modify a disposable copy, never the accepted review. Mocked normal/source reads
alter only the low-level read result for the bound fixture/source path; do not
mock away the hash verifier itself or modify actual frozen files.

The 13 semantic members per origin listed as GENUINE_REPLAY_MISMATCH must first
pass real preflight and then fail genuine replay's corresponding checkpoint
comparison. This includes native identity, full residual/Jacobian, four Schur
fields, coherent translation/proper rotation, adapter deformation and three
deferred adapter hashes. Change one deterministic finite component or hash;
repair all dependent native/final/journal hashes without undoing the target.
Pose mutations must update graph/native copies and affected adapter pose hashes;
proper rotations remain proper. A mere inconsistent nested copy does not satisfy
these semantic probes. Earlier unavailable payloads must not be fabricated.

The initial-hash mutation repairs the first origin and journal hashes, passes
preflight, then fails comparison with a freshly constructed virgin owner.
Other known-link attacks deliberately leave their target link inconsistent,
while repairing outer hashes. The target field must remain demonstrably changed.

For omitted_preparation at NONE, claim a properly rebound CM3 definition while
retaining LOAD-only history: required preparation is absent. At CM3, remove the
first preparation, repair epochs and links, and require rejection of the wrong
first preparation step. Force-scale-switch changes the final load relative to
its predecessor. Wrong-order reverses the two native rows, never a singleton.

Group preflight probes into two origin-scoped children. Run the two virgin and
two review probes in a separate wave. Run each of the 26 expensive replay
negatives in its own child, in sequential waves of 6,6,6,6,2, with at most three
active. Positive controls precede all negatives. No automatic retries or reuse
of prior consumed execution authority. Review must validate exact exception
allowlists and mutation construction before these numerical waves are admitted.

## Acceptance boundary

This gate can establish only complete rehearsal histories and authentic
checkpoint rejection. An implementation/source/resource defect blocks the wave;
scientific contradictions remain genuine failures. MO16 physical recovery,
remaining transport/state failures and full formal G3c confirmation remain open.
G4, G5, full legacy parity and activation remain unauthorized. Q4/S3 mechanics,
defaults, main, releases and concurrent ANYmesh work are unchanged.
