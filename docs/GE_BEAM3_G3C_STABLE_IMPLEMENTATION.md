# G3c stable private owner implementation

Base: 2b6a488fe5807250c4005efbf296e5ebd43a614a, accepted source-map closeout.
All thirteen copied paths must match their independently reviewed generated
hashes. The six additional paths are the inert initializer, fixed authority
wrapper, runner, static implementation tests, runtime guard tests and this note.
All inherited paths remain immutable. The design-only absence test is historical
and runs at its original design commit, not at this implementation successor.

The runner requires a separate canonical five-key independent implementation
review, supplied externally with its exact SHA-256. Required decision:
ACCEPTED_G3C_STABLE_IMPLEMENTATION_FOR_BOUNDED_DEVELOPMENT. Its scope binds the
candidate tree, source_map_sha256 and inputs_sha256 (canonical complete runner
inputs); subject_commit binds the clean candidate. Findings must be empty.
This external review avoids self-referential source hashes and cannot grant
public routing or full graph qualification.

Before numerical imports, the runner verifies original base/tree, clean exact
nineteen-added-path extent, original blobs, generated copies, design acceptance,
implementation acceptance, complete input hashes and the preserved environment.
Worker and finalization repeat complete checks. Runtime capture is one-shot,
binds review/commit/tree/inputs, and captures an immutable source-row tuple.
Each full graph authority check revalidates leased source bytes and mapped
dependencies. Runtime dispatch includes the native/chart/load/joint/helper
chain and immutable coefficient identity/value checks. Changes poison the owner.

Scientific bodies, h values, tolerances, state schemas, exact classes and
authoritative native rotation matrices remain unchanged. Private element and
operator identities distinguish predecessor states. No foreign-state admission,
restart API or manufactured acceptance is added.

## Separate inventories

- Static implementation: six standard-library tests, no mechanics, including
  mocked failed-drain/wait/diagnostic cleanup cases.
- Runtime routing/authority: ten tests, including coefficient and helper
  mutations, authentic shared types, foreign-state rejection and nonzero curved
  load-work derivatives. These are implementation diagnostics, not added graph
  qualification cases.
- Component diagnostic: one unchanged nonclassifying test.
- Bridge: eighteen unchanged tests, including the previously failing KKT test.

The static suite may run before review:

    C:\Python\Python314\python.exe -I -S -B tests/test_ge_beam3_g3c_stable_implementation.py

After independent implementation acceptance only:

    C:\Python\Python314\python.exe -I -S -B scripts/run_ge_beam3_g3c_stable.py --lane guard --review PATH --review-sha256 SHA

Then use the same command with lane diagnostic and bridge, keeping inventories
separate. No automatic retry. Failed or resource-blocked records are retained;
each invocation uses a fresh exclusive external directory. Children are limited
to 600 seconds, 24 GiB/tree and one numerical thread; no progress for 120 seconds
terminates the full tree. Terminal zero must be proved before process.json is
written; failed cleanup is recorded separately, and the owned job handle closes
even if waiting or diagnostic writing fails. This sequence is serial (below the allowed concurrency
of three). Each lane is a separate wave, below the 1800-second wave bound.
No scientific aggregate or partial qualification certificate is emitted.

After an author pass, independently confirm the frozen implementation and compare
corresponding deterministic diagnostics. Only then continue the broader G3c
25-variant histories/restart/rehearsal/formal gates. G4/G5, full legacy-domain
parity and conventional finite Q4/B2-clamp recovery remain open. No publication,
default, B2/B3, Q4/S3 mechanics or prior qualification evidence changes.
