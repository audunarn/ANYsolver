# Corrected G3c rehearsal consumers

Implementation from accepted design closeout
`3334574546007e835c37eaebf3e1438514517d9d`, tree
`8e10f224514c1aefbc4058a42c50cffe16460458`. Exactly two modified consumer
fixtures and four additive implementation paths, as the frozen design permits.

The mutator now reads the five source-hash-bound AST schema literals. All other
attacks, diagnostics, cases and tolerances are unchanged. The sixth predecessor
schema remains hypothetical. Its static regression independently reads the
source assignments and verifies emitted attacks at both synthetic origins.

The read-only bridge verifies the pinned complete permanent archive, its exact
172-file inventory, all raw fingerprints, original wave ordering/path mapping,
ten original leases/reviews/completions/resource records and all eighty packets.
One bounded Git batch checks every original input against original tree blobs.
Current input bytes must match except the two named consumer fixture corrections.
Original runtime/environment and source-map bindings remain exact. No original
record, path, lease or reference is rewritten. Original inputs are not passed
off as new consumer inputs; original producer scope is preserved.

The additive consumer runner has its own exact clean extent, full input list,
commit/tree, review and scope. Its review and every lease bind the bridge
contract and original archive identity. Only eight ordered consumer waves are
admitted: positive, preflight, virgin_review, negative-0 through negative-4.
There are 34 child assignments, two true positive resumes and 142 probes.
Every new predecessor must share the new consumer freeze. The original
producer role is separately validated and supplies only original packet bytes.
The unchanged scientific test/owner performs real resumes, not state injection.
Exact attack reconstruction rejects rehashed wrong bytes/reasons/schema labels.

Child scheduling and Windows Job lifecycle are copied without alteration from
the reviewed bounded rehearsal runner, not dynamically patched. Each child
uses one numerical thread, 24 GiB, 600 seconds and 120 seconds without progress.
At most three run together. One 1800-second coordinator deadline covers authority,
prerequisites, workers, final validation and exclusive same-volume publication.
Failed waves stop unlaunched work and drain launched trees; nothing retries.
All generated records remain external diagnostics, not full G3c qualification.

Before execution, independently review the clean freeze using compatible
private-runtime decision `ACCEPTED_G3C_STABLE_IMPLEMENTATION_FOR_BOUNDED_DEVELOPMENT`
with scope `G3C_R06_CORRECTED_REHEARSAL_CONSUMER_V1`, new full input digest,
source-map hash, bridge contract hash and exact producer_bridge object.
No review created by the implementation is acceptance authority.

Example after acceptance (isolated frozen interpreter):

    python -I -S -B scripts/run_ge_beam3_g3c_rehearsal_consumer.py --wave positive --review PATH --review-sha256 SHA

Later waves additionally require the exact preceding wave path and SHA via
`--previous-wave` and `--previous-sha256`. No producer command is available.

Tests are inert fixture/guard checks, not numerical evidence. Full G3c matrix,
MO16 physical recovery, two formal cycles and G4/G5 remain open. No mechanics,
production recovery, aliases, defaults, release or main change is authorized.
Retain `NO_GO_PRODUCTION_RESTRICTION_UNCHANGED`.
