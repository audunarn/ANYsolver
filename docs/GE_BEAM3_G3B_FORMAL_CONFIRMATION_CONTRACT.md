# G3b formal confirmation preparation V1

Base: correction commit `f67aed07ed877aec679837e0a44e2db5cb398f74`, tree
`e1f812c162307cb2153e1bdd0c5f7eed504efc6c`.

This preparation is NOT execution authority, an independent review, or formal
confirmation. It freezes concrete inventories and inert authority validators.
The coordinator, isolated environment, review and two formal cycles are still
required. The implementation author's passing tests do not substitute for them.

## Scope and exact inventories

Only G3b/S16 and the mixed reference-linear part of S18 are eligible. Preserve
the five concrete families M_B2, M_B3, M_Q4, M_S3 V2D and M_Q4_WEIGHTED;
translation-only ties and independent rotations; 24 native internal variables;
complete stationary/KKT versus Schur comparisons, work, physical recovery,
transport, reference snapshots, cache isolation, atomic publication and replay.
All S17 positive adapters, finite mixed response, history-bearing sections,
public integration, defaults and whole-domain parity remain unqualified here.

The canonical inventory binds exact ordered node IDs from preserved passing
development logs. Lanes stay separate: mb2=25, mb3=40, mq4=42, ms3=45,
weighted=54, transport=39, owner=60, owner_corrections=170. No combined count.
The future recorder must match collection and every setup/call/teardown phase
exactly. Skips, missing or duplicate phases, failures and unexpected tests block.
Do not rerun G1/G2/G3a formal campaigns or change historical test bodies.

## Authority and environment prerequisites

Freeze a clean exact candidate commit/tree containing the reviewed implementation,
runner, tests and inventory. Bind the full transitive source/fixture/contract DAG,
Git launcher/engine, Python executable/version and the actual imported dependency
file graph, including native extension/DLL files. A package name/version listing
or the owner's source-only runtime digest is insufficient environment authority.
Every input path must be a regular file without symlink/reparse substitution.

A read-only import discovery on 2026-09-12 found editable imports from ANYmaterial,
ANYfileIO, ANYgeometry and ANYmesh in the normal Python environment. In particular
ANYmesh has concurrent development: do not freeze or edit its live checkout, and
do not pretend that its installed 0.4.0 metadata identifies its imported 0.5.0
source. Prepare an isolated immutable import graph from explicitly hash-bound
sources/artifacts before formal execution. Record actual import origins, reject
unbound imports, disable automatic pytest plugins and ambient Python path hooks.
Preserve all sibling working copies unchanged. This preparation installs nothing.

Require a genuine external or separately delegated independent review, with
exactly decision/findings/reviewer/scope/subject_commit as canonical JSON keys.
It must accept the exact implementation AND runner, have no unresolved findings,
and bind candidate tree, input DAG, environment manifest, inventory and the
reference-only scope. A JSON claiming independence is not sufficient: preserve
the actual review provenance. Author-generated disposable validator fixtures
must never be used as real review records.

## Required coordinator behavior (not yet implemented by this preparation)

Validate all authority before mechanics/imports or output creation. Use an exact
hash-bound worker lease and closed lane assignment. Each cycle runs all eight
lanes in separate fresh processes/directories; at most three children concurrently,
one numerical-library thread each, 24 GiB per complete process tree, 600 seconds
per child, 120 seconds without CPU/output progress, 1800 seconds per entire wave.
Timeout or failure terminates/reaps the complete tree and preserves diagnostics.
No automatic retries and no reused consumed output or execution authority.

Observe real completed capture, local solve, assembly, recovery, native/other
preparation, acceptance, restart and output checkpoints. Instrumentation must not
change computations or bypass captured dispatch. Record test setup/call/teardown
phases, exact output inventory, import origins, stdout/stderr hashes, return codes
and zero remaining processes. Missing checkpoints cannot be replaced by intention.

Rehearse the complete frozen candidate before formal execution. Then perform
two fresh cycles against identical inputs, revalidating the full DAG/environment
at capture and finalization. Preserve the existing packet schemas and their
qualification=false labels. Compare scientific bytes separately per lane; retain
raw timings/progress externally. Correction tests must also emit case-level
scientific records for all 170 cases, not merely a passing aggregate count.

Only after all lanes/processes/phases/packets pass and both cycles agree may a
complete schema-valid aggregate be atomically published with exclusive creation.
Stage on the same volume, validate before promotion, never overwrite a canonical
path. Interrupted or malformed evidence leaves diagnostics only, not partial GO.
The closeout binds the accepted aggregate, source/environment authority, reviews
and external raw manifests; static closeout only, no mechanics rerun.

Terminal precedence: authority/schema; process/evidence; state/restart;
constraint/junction/work; graph/assembly/regression; finally
`PROVISIONAL_GO_GE_BEAM3_G3B_REFERENCE_MIXED_ELASTIC_STATIC_ONLY`.
Fail closed when evidence cannot support a more specific scientific classification.
Every result retains unchanged production restrictions, no defaults change and
general_static_parity=false. Actual confirmation remains outstanding.

## Preparation verification and handoff, 2026-09-12

The inert-validator inventory passed 53 tests. After adding actual collection
checks for each of the eight lanes and the no-runtime-delta guard, the separate
infrastructure inventory passed 62 tests in 17.90 seconds (18.234-second bounded
process, zero remaining children). Collection only: no G3b mechanics campaign
or formal execution occurred in this preparation. No source, package metadata
or workflow changed. Historical test bodies remain unchanged; successor extent
allowlists and the development runner's infrastructure-file allowlist are extended.

Exact inventory SHA-256:
`fe9223b033947df739623c92f602e4975243aed443f10f27fba73af91cf42b6d`.
External test archive:
`C:/Users/AudunArnesenNyhus/AppData/Local/ANYrelease/ge-beam3-g3b-confirmation-preparation-20260912-f67aed0`.
Its manifest binds six raw log/process files, SHA-256
`523453c1aa05adbfa9d82106ea7b5f468d2235c734f4d68f5acf48fa2efdc3b1`.
Original temporary logs remain preserved. No actual review is created by the
disposable review-validation fixtures in the test suite.

Remaining required work: implement and test the coordinator/worker lease,
prepare the isolated import environment, emit correction case-level records,
complete rehearsal, obtain genuine independent implementation/runner review,
then execute two formal cycles and independently adjudicate/close out. The
request to use a separate review agent is still unanswered; do not impersonate
an independent reviewer or silently bypass the requirement.
