# Independent G3c mixed-owner design review

Reviewer: `/root/g3b_independent_review`, separately delegated reviewer. Subject
`5385ebd21db406621627e632ecb3cc231cf02384`, tree
`20742d5ea1638e3723c6b9b23cd1c2abe6934c2b`. This review accepts only the private
registered-fixture owner design for implementation and bounded testing.

## Scope and result

Read the full contract, inert auditor/layout definitions and tests, together
with the bound native element/operator/material context, rotation store,
NonlinearStateStore construction/trial/commit paths, graph chart pullback and
joint adapter. Previous independent local beam/shell source reviews remain
scoped local evidence; they do not establish graph state safety.

No unresolved correctness finding prevents implementing this frozen design.
The seven inert tests passed independently in 0.551 seconds. They test contract
integrity, not an implemented owner. The separate read-only audit verified all
28 inherited source blobs through one Git batch, three payload hashes, exact
four-added-path extent, subject commit/tree, clean checkout and diff check.
Contract canonical bytes: 11,446; SHA-256
`471d05d2db6c604c39e30320e279e3a8ccd47186cb549969a596a00c62d967e5`.

## State, chart and replay assessment

Fresh native sandbox construction is feasible: source material epochs are
validated from committed payloads, independently of the fresh rotation-store
transaction generation. Authentic validators and contexts must be issued by
that sandbox, with each element's load binding pointing to the same store.
The captured graph generation remains authoritative. This is not permission
to fabricate a context or relabel a virgin source diagnostic as committed.

Native commit may modify its private sandbox before a later injected failure.
Containing the entire sandbox below a single immutable graph generation avoids
publishing such partial work. The no-fallible-operation-after-swap rule is a
mandatory implementation invariant, not established by this static review.

The contract correctly distinguishes already-charted element output, spatial
Jacobian pullback and spatial-row/chart-column output. Joint evaluate_ports
already supplies complete multiplier/constraint blocks. Implementations must
assemble those blocks once, not add the multiplier Hessian again through a
second constraint path. Fixed physical port frames and nonidentity C survive
numbering and passive transformations.

The draft's missing authority for large common motions was corrected before
freeze. Four actual zero-load preparation commits, each below the existing
increment bound, now create authentic accepted origins. Known rigid poses are
predictors only. Preparation failures cannot be skipped; every prefix is
included in restart replay. The passive translation conjugation is correct.

Closed checkpoint layouts, deterministic fixture expansion, externally bound
runtime and expected digest, plus actual every-prefix replay provide a sound
design against rehashed or foreign snapshots. The native source validator
recomputes the full stationary response but should not be mistaken for a
standalone proof of every condensed response field. MO03, MO14 and MO15 must
independently enforce Schur consistency and exact replay of nested condensed
fields. Native/source payload hashes alone are insufficient.

Select one authoritative binary64 graph pose calculation and check the native
subset exactly. Independent Exp/Jet/port arithmetic may differ by last bits;
the adapter record must bind the actual captured pose and actual original-chart
candidate, without overwriting it with a rebased diagnostic. The frozen
original-chart replay rule correctly distinguishes this from numeric same-pose
covariance tests.

## Mandatory remaining boundaries

No graph mechanics were executed. All eighteen named implementation categories
still require concrete collected fixtures/tests and independent implementation
review, followed by bounded rehearsal and two formal cycles. In particular,
late sandbox failure, last-family failure, callback mutation, cancellation,
reentry, token identity, all 25 expansions, all histories/common motions and
every-prefix restart cannot be inferred from the inert suite.

Conventional finite Q4 PHYSICAL_RECOVERY and B2 shear-clamp full-domain recovery
remain OPEN exactly as stated. Signed source-work channels do not waive either
obligation. No full G3c/parity, public route, release, default, G4 or G5 authority
is granted. Historical mechanics and qualification records were not modified
or rerun.

## Diagnostic transparency

The inert suite emitted Git global-ignore ACL warnings but completed all seven
tests successfully. The independent read-only audit initially stopped before
its first check because disabled ambient Git configuration omitted the exact
safe.directory exception. Its scratch-only helper was corrected to pass that
single worktree exception per command; no global configuration or candidate
file changed. The final audit passed in 0.305 seconds. No numerical process or
historical campaign was retried.
