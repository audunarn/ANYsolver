# E4-0 open-core and drill-route report

## Outcome

E4-0 closed its bounded source, algebraic, and flat-affine feasibility wave.
It registered no element candidate and changed no production code.

The component results are:

- open WG core: `GO_E4_OPEN_CORE_IDENTITY`;
- zero-energy local weak-symmetry route:
  `NO_GO_E4_WS_LOCAL_CONDENSATION_AND_RANK`;
- dimensionally reduced perturbed-Lagrange route:
  `PROVISIONAL_GO_E4_PL_LINEAR_QUALIFICATION_PLAN`.

The direct-additive WS identity stops here, and the only immediately
executable successor is the separately reviewed conditional plan
`S4_E4_PL_LINEAR_QUALIFICATION_PLAN.md`. The provisional result is not a
candidate GO, implementation GO, or production authorization. A broader
jointly coupled energetic/dual weak-symmetry architecture remains an
unclassified new identity requiring its own governing plan; E4-0 neither
disproves nor authorizes it.

## Shared core result

The flat-affine WG2020 `n=7`, `k=0`, source-MITC, `2 x 2` physical core is
closed for this study. The exact coordinate selectors form an orthogonal and
complete `20 + 4` split of the 24 external coordinates. The physical core has
rank 14 and exactly six rigid modes; its drill-free embedding retains rank 14
and adds exactly four coordinate drill null directions. The exact certificate
assembles the printed `N_sigma`, `N_epsilon`, `F`, `H`, and `Gq` operators
rather than a surrogate stiffness. It proves the actual 35-by-35 mixed block
invertible, its condensed material operator PSD with rank 14, and its kernel
equal to the six physical rigid modes. Stationary mixed/condensed energy,
residual, tangent, virtual work, load work, and physical-resultant recovery
agree. Direct drill moments are excluded.

The canonical core contract is 2,284 bytes with SHA-256
`8768ABDC5D77B5FCC1643AF69920EFEB83F27DDFBDE17525F06EE2B53A5C1678`.
Its canonical output is 2,832 bytes with SHA-256
`F9C39E4E92D690F6FDECB756E114A30B579E4526FBDB84E73A260938E069BC14`.

## WS necessary theorem

For `Pi(q,lambda)=Pi0(q)+lambda^T Cq`, multiplier stationarity is `Cq=0`
and contains no multiplier. The zero multiplier block therefore cannot be
Schur-condensed at fixed 24-coordinate `q`. Every algebraic exit either
retains a saddle unknown, reduces the external coordinate space, adds a
multiplier compliance/regularization, or adds primal energy. The zero
constraint leaves core rank 14. Consequently no branch satisfying all five
frozen WS requirements can produce a finite rank-18 PSD 24-by-24 stiffness.

This conclusion is limited to the direct-additive post-core identity above.
It is not a theorem against a newly named jointly coupled Hu-Washizu system.

The canonical WS contract is 3,033 bytes with SHA-256
`AAB8E8ECC846FE652F9FAC1A2F02FC50B0AB8E179D808132AB3AF758BC7F82B8`.
Its canonical output is 1,327 bytes with SHA-256
`9BBAF85E85734A36DA22B4780F450A5957CCF4ABE358BB24AEDAAB977C77057E`.
No WS space, macroelement, inf-sup, or benchmark campaign was run.

## PL variational closure

The PL branch uses the centre-linear part of
`c=theta_D-(v_,x-u_,y)/2`, the unnormalized local basis `[1,r,s]`, and the
constant-thickness volume-to-surface reduction

```text
int_A h [T_h c_1 - T_h^2/(2G)] dA.
```

This keeps `G` dimensionally paired with the explicit thickness measure. The
shell-scalar normalization is independently anchored by the open MITC9i
equations (18)--(19), while the 2011 chapter supplies the printed constraint,
deletion, multiplier, and hourglass grammar.
Exact elimination produces the positive Gram form
`G B^T M^-1 B`. The separately derived affine residual-mode energy uses the
frozen printed scale `10^-3`, acts only on the fourth modal drill row, and is
kept outside physical recovery.

The exact oracle gives retained constraint rank three and an independent
fourth residual row. Together they lift exactly four open-core drill
directions: pure common drill and translation-only spin are energetic, their
matched rigid combination is null, and alternating drill is controlled only
by the residual term. The final stiffness has rank 18, nullity six, and no
negative-energy mode on both registered affine witnesses. Exact stationary
38-field parity, rigid images, affine/D4/frame/reversal/origin/unit behavior,
patch separation, diagnostics separation, and hostile historical terminals
also close.

The canonical PL contract is 3,165 bytes with SHA-256
`9B3B2C151B2A910862F2D61ADBFACC8AEB72E7214E6A096B0C53BF2CF447A547`.
Its canonical output is 4,920 bytes with SHA-256
`D6E0DA7E3300BCF691C87875B1FD3F215A6C70F611C52BB74A6579F20772E62D`.

## Reproducibility and boundary

The historical tiers were run only at their closed-world authorities: E0
94/94, E1 16/16, E2-A 8/8, and E3 15/15 (14 scientific plus its closeout
leaf). They are not represented as one live successor suite. The eleven
component tests pass, and each of the three caller-bound oracles produced
byte-identical canonical output in two fresh processes.

The route contract is 2,374 bytes with SHA-256
`BD8F7C0FF49377224E9B2E9BE804B3423D6299898F66B5C117B0A744FC07A453`;
the route output is 1,371 bytes with SHA-256
`796A33AC0C01645A72E28A94C43688126B2D14EBAAB2A0C40ABB3CAC05582461`.

No mass, geometric stiffness, finite rotation, non-affine/warped geometry,
locking, mesh-uniform stability, nonlinear response, buckling, performance,
coupling, selector, serialization, API, dispatch, package, workflow, or
production claim is made. Numerical multiplier/hourglass quantities remain
separate diagnostics and never enter physical `N/M/Q`, stress, yielding,
fatigue, or code checks. Production remains
`NO_GO_PRODUCTION_RESTRICTION_UNCHANGED`, with legacy `ShellElement` as the
default.
