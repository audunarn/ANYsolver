# G1 elastic static integration: scoped confirmation complete

Candidate: `f4c1093f23609d9891a02f2a93e70b440304f952`.
Tree: `44295f73a5bb3af004052a42d58994265d9e9ec4`.
Terminal: **PROVISIONAL_GO_GE_BEAM3_G1_ELASTIC_STATIC_ONLY**.

The independent implementation review and subsequent independent evidence
inspection accepted this scope with no findings. This closes S01-S08 for the
private one/two-element exact-elastic analysis owner. It does not close general
static parity, qualify all legacy-beam domains, enable a public selector, or
authorize release/default changes.

## Corrections

- Free DOFs are immutable bytes-backed indices authenticated against frozen
  support/DOF authority before evaluation.
- Graph authority is revalidated after callbacks, before commit, and at the
  end of native material validation inside actual store preparation.
- Unsupported model-level load cases and DOF-manager constraints fail closed.
- Regression rejection preserves committed bytes, state/rotation generations,
  accepted history and leaves no active trial, including nonempty prefixes.

The independent reviewer reproduced and helped close the later preparation
gap and unsupported manager-constraint channel before candidate freeze.
Original candidate `635888a`, blocked review `9ab7889`, both original P1
findings and all earlier packets remain preserved. A successor acceptance
does not rewrite the historical failed implementation review.

## Separate verification inventories

- Regression smoke: 16 passed before the later guard extensions.
- Intermediate development suite: 45 passed.
- Final rehearsal: 49 passed; final runner includes authenticated-replay progress.
- Confirmation infrastructure: 22 passed.
- Inherited planning/admission: 39 passed, with 54 subtests.
- Formal cycle 1: 49 passed, 37.496 seconds.
- Formal cycle 2: 49 passed, 37.901 seconds.
- Independent frozen-candidate reproduction: 13 grouped checks passed.

Formal test-result records and deterministic packets are byte-identical.
Each packet is 14,968 bytes, SHA-256
`29519c05031ee3da3e05f7337f02e3fb1f462eed6d51d4defb7c39a60be01394`.
The packet retains its development diagnostic schema; it is not relabeled as
a certificate. The separate formal aggregate binds both cycles, the complete
test inventory, accepted review, source/environment identity and scoped terminal.

Both formal workers had zero exit status and zero active processes at completion,
empty stderr, one numerical-library thread and peak tree memory below 194 MiB.
No formal retry occurred. Times are diagnostics, not performance qualification.

## Evidence and preservation

Durable archive:
`C:\Users\AudunArnesenNyhus\AppData\Local\ANYrelease\ge-beam3-g1-confirmation-20260910-f4c1093`.

The archive manifest records byte counts and SHA-256 for 42 retained files,
including smoke/rehearsal outputs, the implementation review, frozen inputs,
formal packets, per-phase test results, checkpoints, process records and logs.
A Windows permission issue copying temporary pytest folders was resolved by
transferring only evidence files through a byte-verified workspace stage.
The stage's duplicate copies were removed only after verification. Original
temporary evidence remains; no historical evidence was removed.

All old runtime files are unchanged relative to the G1a baseline. Only the new
private G1 owner and its validation hook were corrected. Beam/shell mechanics,
Q4/S3 qualification, public interfaces, selectors, defaults and versions are
unchanged. Main and the unrelated ANYmesher compatibility work are untouched.
No push, merge, release or publication was performed.

## Next step

Implement **G2 under the already frozen general-static contract**: nonzero
prescribed translations, affine MPCs, partial/full orientation supports,
relative-pose constraints, and independent multiplier/work comparisons.
First freeze G2's concrete candidate/test/fixture manifest; do not infer mixed
graph or history-bearing material parity from G1. G2 has not been started here.
