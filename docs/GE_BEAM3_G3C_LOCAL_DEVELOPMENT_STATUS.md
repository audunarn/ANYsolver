# G3c local trial development checkpoint

Private direct-anchor equations accepted at f2ddfbe332ba0f87c7aa503a74ac31addbfe5a06;
the exact independent equation review is copied byte-for-byte in this successor.
Public legacy principal-log finding G3C-DR-01 is preserved, not fixed/reclassified.

## Completed development, not formal qualification

The new private B2/B3 local trial implements R=Q_A, all-pair relative guards,
complete analytic second-order deformation and variational pullback, actual
unchanged scalar local operators, and spatial/chart derivative conversion.
It uses fresh local objects per call; no borrowed material/history/cache,
graph owner, state commit, checkpoint or public selector is exposed.
Explicit physical orientation is normalized without legacy fallback, with
actual section-frame validation. Definition data is write-once; entry and
exit snapshots detect coherent replacement. Computed nonfinite values reject.
The runner validates the accepted equation's full source/payload DAG before
mechanics imports and captures the development inputs in an external lease.

Separate inventories:
- Accepted equation design: eight inert methods (at equation candidate).
- Local development: 18 pytest nodes pass; pytest time3.80seconds.
- No graph, formal G3c, G4, G5 or historical mechanics tests were run here.

Latest child:15.7201249seconds including isolated startup and input verification;
peak193355776bytes; exit0; active process count0. These are diagnostics,
not speed or scientific qualification claims. The runner enforces600seconds,
24GiB/tree, one numerical thread and120seconds of inactivity; no automatic retry.
The preceding12-node and13-node passing development runs are separately retained.
An earlier coordinator preflight stopped on Git ownership before a child launch;
its fix was a worktree-specific safe.directory option, not global configuration.

The reviewer identified orientation-scale fallback, immutable-definition,
nonfinite-output and frozen-DAG guard gaps before this checkpoint. Corrections
and targeted regressions are included. Independent review of this exact
implementation commit remains required; do not borrow equation acceptance.

## Still required

1. Native station strains/resultants, physical recovery/work and independent
   local energy checks. Current returned recovery_complete is deliberately false.
2. Complete finite local evidence, independent implementation review and any
   required corrections before the positive graph owner.
3. Native/B2/B3/Q4/S3 common-pose assembly, all five graphs and25variants,
   transactions, accepted histories, cache guards and authenticated restart.
4. Reviewed rehearsal, two deterministic formal G3c cycles and static G3
   composition. Then G4 histories and G5 installed integration.

No existing native/legacy/Q4/S3 mechanics, recovery, defaults, package metadata
or old evidence changed. Existing main remains untouched. This foundation is
not positive G3c acceptance, full parity, default or release authority.
