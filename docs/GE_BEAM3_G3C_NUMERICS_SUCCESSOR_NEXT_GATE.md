# G3c diagnostic closeout and next numerics gate

## Current outcome

G3c mixed-owner confirmation remains BLOCKED, not qualified. The historical
d90aa3e development inventory has 17 passes and one failed full-KKT directional
node. The corrected one-node smoke passed. Each of the two later one-node
diagnostic inventories completed separately; neither is a passing mechanics
gate. All raw runs and independent reviews are retained with byte/hash records.

External archive: C:/Users/AudunArnesenNyhus/AppData/Local/ANYrelease/
ge-beam3-g3c-owner-diagnosis-20260912-096ec0f. Its manifest is copied byte-for-byte
as docs/reference_cases/ge_beam3_g3c_diagnosis_archive_v1.json: 4852 bytes,
SHA-256 bc08ad53f640375656a600564bc64101e9864ded5fb2a16b5857cc596b289557.
All 27 listed files were verified after archival. A standard-library static
audit checked canonical JSON, separate inventories, source commits, terminal
states, lease links and both five-key reviews. Original Temp outputs remain.
All five recorded process trees are terminal with zero active processes.

The canonical independent diagnosis review accepts diagnosis only, findings
empty, and is copied unchanged as ge_beam3_g3c_elementary_diagnosis_review_v1.json
(992 bytes; SHA-256 bb8248e305825854efb7e7edaacb64e1d10cec3dcf27f6547a38e6838063665c).
The d90aa3e blocked implementation review is also preserved unchanged.

The component diagnostic at 8132e745333e572b07e2dd56b9df9c1ca1527270 completed
in 18.444574200024363 seconds. Its h=1e-6 full error exactly reproduces the
2.767173042642239e-7 failure against 1e-7. The error is not confined to one
family; native condensation reconstruction is exact at the tested centre.

The elementary diagnostic at 096ec0f00afa4c5eaaecdb4dedc8982f450aee84, tree
82f1b11c4ea245c77f89b7e991d4d423ff6f9c6a, completed in 14.615772500023013
seconds, peak 141701120 bytes, zero remaining processes. Actual frozen Jet
evaluation disagrees severely with an independent 90-digit Decimal series:

| Quantity | Frozen binary64 evaluator | Independent series |
| --- | ---: | ---: |
| Log factor second derivative, c=0.9999999 | -1498.359375 | 0.266666700952384 |
| Exp cosc second derivative, x=1e-8 | -52441378 | 0.0027777777762896826 |

These scalar discrepancies demonstrate cancellation in shared elementary
evaluation; they do not yet prove that correcting it resolves every assembled
failure. The underlying Exp/Log equations are unchanged. The shared file has
23 source importers, including existing public and retained beam routes.
Do not describe a shared-file edit as a private owner-only change.

## Next: isolated elementary numerics successor

First prepare and independently review a private numerics-only contract. Bind
the current failed evidence and exact original source, author/reviewer oracles,
the admitted chart and derivative conventions, and the new isolated path set.
No source/mechanics import, default, qualification record or historical hash
may be silently replaced. This document recommends the next scope; it is not
an accepted replacement source authority or a graph execution authorization.

1. Add a private successor coefficient/Jet module without editing the shared
   `_ge_beam3_mixed_ad.py` or injecting global replacements. Public routes keep
   using their current source. Reuse mathematical definitions, not unstable
   closed-form evaluations near zero.
2. Derive convergent series, derivative recurrences, remainder bounds and
   floating-point error bounds for coefficient values and their first two
   derivatives. Choose numerical branch points and truncation from these
   bounds, not by fitting the failed graph test. Keep the physical chart guard,
   mechanics, quadrature and every existing tolerance unchanged.
3. Test the whole admitted scalar interval, zero/small/subnormal arguments,
   both sides of old and proposed numerical branch points, and points near
   the unchanged relative-rotation boundary. Validate all Jet gradient/Hessian
   terms and non-axis-aligned SO(3) identities against an independently authored
   high-precision oracle. A scalar coefficient pass alone is insufficient.
4. Review that frozen isolated kernel before defining its graph integration.
   Enumerate the exact transitive native/local/joint dependency graph, state
   fingerprints and tests. Any private clones or explicitly injected immutable
   dependencies must be transparent and hash-bound. Exact-class/material-state
   validators cannot be bypassed merely to admit the successor.
5. Only after that successor integration is independently reviewed, run the
   unchanged component probe at all three h values, then the complete bridge
   inventory. Preserve failures; do not retry the old candidate or waive a step.

Each child remains one thread, 24 GiB/tree, 600 seconds and 120 seconds without
CPU/output progress; at most three workers and 1800 seconds per wave, fresh
exclusive output, no automatic retries. No full historical campaign is needed
for the isolated diagnostic/kernel gate. Applicable integration regression
coverage must be frozen before reconnecting the private graph.

## Still open after that gate

Complete G3c requires the remaining B3/Q4/S3/loop and 25-variant histories,
authentic common-motion and restart-prefix coverage, state safety, independent
implementation review, rehearsal and two deterministic formal cycles.
Conventional finite Q4 physical recovery and B2 shear-clamp recovery remain
open. G4 history-bearing sections and G5 general integration are not complete.
No qualification, release, selector/default change or full legacy parity is
claimed by these diagnostics. Q4/S3 mechanics and historical evidence remain
unchanged. Root main and concurrent mesher work are outside this change.
