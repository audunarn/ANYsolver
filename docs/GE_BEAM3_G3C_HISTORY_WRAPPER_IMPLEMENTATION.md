# Private G3c history wrapper implementation

Successor implementation from f4cabebffa6507b98b74bbc3ca35e3d32646b1ab,
tree 1858bebd483869edc2f60fe8cc464ee91b301c4b. Exact six-added-path extent is
frozen by scripts/run_ge_beam3_g3c_history.py. No inherited file changes.
This is smoke/restart development, NOT full G3c qualification.

The standard-library script front end imports no numerical library. Creation
still requires the accepted inner owner's reviewed lease. Resume performs full
inert nested preflight before first numerical construction, constructs a real
virgin owner, then calls its genuine solve/commit path for every command. It
compares each full journal prefix and final state hash, then all checkpoint
bytes. There is no injected state, accepted-generation copy or numerical cache.

Composition owns one additional exclusion lock, not an additional accepted
state. The inner owner remains the sole publisher. Checkpoint capture holds
both owned locks and full guards. Wrapper solve has no fallible post-publication
callback or secondary commit. Captured guard code/default identities are checked
outside the callable before and after every guard invocation; this closes the
independent mutable-bound-method finding. Runtime identity includes all runtime
Python sources, wrapper/preflight/runner scripts and the unchanged environment.

Accepted-pose diagnostics accept no caller pose/origin/load/callback. They
validate the last accepted command against its actual journal prefix, evaluate
the actual published pose under the owner lock with a fresh genuine nonce and
source sandbox, then discard. Accepted publication is never overwritten. Virgin
owners have no last accepted command and reject this diagnostic. Diagnostic
candidate bytes are explicitly noncommitted and cannot be imported as state.

## Inventories and execution boundary

- Inert wrapper inventory: five unittest nodes, including a synthetic callback
  mutation probe. These are orchestration tests, not numerical evidence.
- Inert runner inventory: five unittest nodes. Mock workers test scheduling
  only; they do not prove actual numerical-process overlap.
- Smoke inventory: five concrete parameterized family nodes, one per child.
  Each genuinely commits zero and first finite stage, verifies native full/
  Schur records and accepted links, and evaluates a discarded same-pose trial.
- Initial restart inventory: three B2 prefix nodes, one per child. Each creates
  actual checkpoints, genuinely replays its selected prefix, continues to the
  second accepted stage and demands exact checkpoint equality.

The two numerical inventories are NOT executed yet. A frozen independent
implementation review with scope G3C_HISTORY_WRAPPER_SMOKE_AND_PREFIX_V1 and
empty findings is mandatory. Its decision string retains the accepted inner
lease's existing private-development decision; the successor runner additionally
requires this exact narrower scope and all current input hashes/commit/tree.
This does not manufacture or modify the historical review.

The runner admits only these two bounded waves, not complete histories or full
qualification. Each worker has 600 seconds, 24 GiB/tree, one numerical thread,
and a 120-second CPU/output inactivity guard. A wave has at most three children
concurrently and an 1800-second deadline. Exclusive fresh directories, full
terminal-zero drain and always-close handling are retained from the accepted
runner. Failures stop unlaunched work; already launched work reaches bounded
terminal handling. Nothing retries automatically. Logs and wave/process status
remain external diagnostics, never canonical scientific GO evidence.

The restart wave additionally requires all five same-candidate smoke receipts.
Independent pre-execution review of freeze 44cfddc found that the first receipt
validator did not bind raw logs or actual worker pass counts. The correction
adds exact stdout/stderr/completion byte counts and hashes to terminal receipts,
plus a canonical worker completion record binding the actual collected/passed
count, nonpassing flag, inventory, candidate, input hash and lease hash. Missing,
replaced or truncated evidence and rehashed nonpassing completion records block
the restart prerequisite. The pre-correction freeze and review remain preserved;
no numerical execution occurred there.
After smoke, report observed duration before further campaigns. Ten full
rehearsal histories, all 375 histories/3450 prefixes, complete mutation and
transport/recovery obligations, MO01-MO18 and two deterministic formal cycles
remain required for full G3c. G4/G5 and full legacy parity remain later gates.
No aliases/defaults, production mechanics, shell qualification evidence,
versions, release or main-branch changes are authorized by this implementation.
