# G3c history/restart implementation progress

Base: accepted design closeout 23842b7962ac09abd64c7a89436b2a1253a0101e.
Branch: codex/ge-beam3-g3c-history-owner-v1.
Status: INCOMPLETE_IMPLEMENTATION_NO_MECHANICS_AUTHORITY.

## Implemented first increment

scripts/ge_beam3_g3c_restart_preflight.py implements bounded strict canonical
JSON, exact frozen nested layouts, binary64/int distinctions, proper rotations,
fixture-derived dimensions and element order, policy/runtime/definition checks,
available journal/native/adapter hash links and graph/native pose consistency.
It loads only the fixed hash-bound standard-library definition source bytes,
without importing anysolver or consulting cached bytecode. All constructor-
derived identities and unavailable predecessor payload hashes remain untrusted
until genuine replay. The returned immutable packet is NOT an accepted owner.

Eight inert unittest nodes exercise all 25 graph/variant schema expansions and
rehash mutations. Their invented numerical arrays are syntax fixtures only;
they are not scientific evidence, owner states, restart proof or smoke results.
The initial six-node suite passed in 0.612 seconds before independent code
review. That review found HRP-01 (Python equality hid signed-zero differences
in native/graph pose copies) and HRP-02 (consistent journal links could contain
cyclic state commitments). Canonical-byte pose comparison and explicit
cross-epoch cycle rejection correct those findings. Two regression nodes cover
signed-zero displacement/rotation copies and self/multi-entry cycles with
otherwise consistent final hashes. All eight tests pass in 0.663 seconds.
Independent correction verification is pending; this is not execution authority.

The complete source/environment lease remains the future reviewed runner's
responsibility before preflight; supplied runtime hashes are independent runner
authority, never trusted claims extracted from the imported packet itself.

## Work still required

1. Resolve independent preflight review findings and add regressions.
2. Implement the private wrapper by composition: one accepted generation,
   owner-lock capture, wrapper/runtime guards, genuine replay and exact prefix
   comparisons, with no source/owner patching or caller-origin bypass.
3. Implement fixed nonpublishing accepted-pose diagnostics under the real
   owner's lock, nonce and command validation, always discarding its sandbox.
4. Add authentic restart/replay, mutation, ownership/cancellation and continuation
   tests. Synthetic syntax coverage cannot replace those tests.
5. Extend the bounded reviewed runner on this successor, binding every input,
   final implementation review, concrete node inventory and runtime capsule.
6. Independently review the frozen implementation before numerical execution.
   Run five bounded family smoke nodes, then ten complete rehearsal histories
   and checkpoint replay/mutation checks. Report measured campaign cost before
   scheduling the complete 375-history/3450-prefix matrix.
7. Retain MO01-MO18, including unresolved physical recovery/MO16. No full G3c
   confirmation while any original obligation remains open; G4/G5 and complete
   legacy parity remain later gates.

No mechanics, coefficients, qualified shell evidence, aliases/defaults, public
APIs, versions, main branches or concurrent ANYmesh work are changed here.
No numerical campaign has run and no accepted qualification is claimed.
