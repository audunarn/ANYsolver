# R06 correction and role-separated rehearsal consumer bridge

Design only from e818ec9541d01c4de1d83e59ca096769aba79f43, tree
74f981544ed201d056c5dcb8e415570e1a5eb354. Preserve the genuine ten producer
histories at e22be49 and every raw output, review and RI-03 incident. This
contract does not authorize numerical execution; freeze and independently
review the narrow implementation next. No new producer run is allowed.

## Exact correction

Replace only the five incorrect R06 envelope strings with the literal constants
hash-bound in the JSON contract. Read their fixed source using AST, without
importing production modules. Verify full source bytes and the unique literal,
so an invented constant cannot pass merely by being repeated in the tests.
Retain the sixth predecessor-G3c fixture as explicitly hypothetical. Do not
rename it an actual historical import format. Keep all 71 members at both
origins: 142 negative probes, including all twelve R06 member/origin probes.

All other mutation construction, exact rejection diagnostics, case definitions,
commands, scales, tolerances, schema, numerical sources and runtime stay fixed.
The only two existing paths that may change are the mutator and its static test.
The new authority/runner/test/document paths are enumerated separately. Never
modify or dynamically patch the old runner, owner, preflight, scientific test,
packet serializer or archived records. Full producer input lists are preserved;
they must not be disguised as the different new consumer input list.

## Original producer role

The pinned archive manifest binds 172 original/review files, including exact
NONE and CM3 wave, lease, completion, stdout/stderr, packet-manifest and packet
hashes. Verify every file against that manifest using regular non-reparse paths
within the single fixed archive root. Reject missing/extra/mismatched files;
the archive-manifest itself is separately hash-bound. No caller-selected stale
archive or general fallback is permitted.

Validate both original producer leases against their own e22be49 commit/tree,
implementation review and 3389-row input digest. Require the independent
producer-only acceptance, exact terminal-zero process records, resource limits,
all ten original test completions and eighty ordered packets. Bind original
serialized predecessor references exactly against the archive's explicit
original-root mapping. Read their unchanged corresponding archived bytes; do
not rewrite a reference, lease, digest, packet or receipt to a new root or commit.

Prove every original input unchanged except the two named correction files;
the allowed corrected files are consumer-only harness inputs, not mechanics.
Require exact original history runtime and environment digests. The five
historical schema sources, registered histories, packet preflight, owner/state/
rotation/store and all production sources remain byte-identical. Compare old
Git tree blobs and new checked-out bytes as well as source-map/lease hashes.
Never suppress a changed runtime fingerprint to make replay pass.

## New consumer role

The new independent review binds its own clean commit/tree, complete input
digest, this bridge contract hash, source-map hash and new consumer scope ID.
Every new lease binds those values plus the original archive-manifest identity.
The new coordinator admits only positive, preflight, virgin_review and the five
six-or-fewer-child negative waves, in that order. Old producer validation is a
separate role, never a relaxation of new-consumer predecessor checks.

The first new wave genuinely resumes both frozen origin packets using digests
from the validated old manifests. It must reproduce complete original bytes.
All later waves require those positive results and all preceding new waves,
under the same new consumer freeze. A missing, failed or partial predecessor
blocks, as does a changed role, producer commit/tree/review/input/runtime/packet,
wrong consumer extent or stale bridge. Test each of these mutations before
execution, including rehashed incorrect R06 constants and role substitution.

Preserve exact inherited negative-test expectations and genuinely reconstruct
expected attacks from verified origin/source/review bytes. A wrong phase/reason,
no-op, arbitrary exception, timeout or malformed semantic packet cannot pass.
Every semantic replay still runs the unchanged owner; no candidate-state
injection, saved numerical matrix or fabricated positive state is permitted.

## Bounds and acceptance

Use the unchanged one-thread, 24-GiB tree, 600-second child, three-worker,
120-second inactivity and 1800-second complete-wave bounds. One coordinator
deadline covers initial/final authority, prerequisite validation and atomic
publication. Exclusive pending files and same-volume promotion prevent partial
canonical evidence. Drain complete process trees; preserve failures; retry none.

Successful consumers close only these two origin replays and 142 rejection
probes. They do not replace the full 375/3450 history/prefix matrix, MO16 physical
recovery, remaining transport/state-failure tests, two formal cycles or G4/G5.
No production/default/release/main change, shell mechanics alteration or full
legacy-parity claim is authorized. Preserve NO_GO_PRODUCTION_RESTRICTION_UNCHANGED.
