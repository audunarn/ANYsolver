# ANYsolver 0.4.3: scoped native beam delivery

This release integrates the accepted delivery at
`0f3c60cae8ca7aa098245fee1b7f3c963ea1c10e`, preserving its complete ancestry and
all historical qualification/incident records. See
[native workflows](../GE_BEAM3_NATIVE_WORKFLOWS.md) and the
[independent acceptance](../reference_cases/ge_beam3_final_delivery_independent_review.json).

Use `ge-beam3` for the earlier admitted straight mixed element or the separate
`anysolver.ge_beam3_native` public workflow factories with `ge-beam3-native` for
the admitted straight/regular-curved native workflows. The latter include the
reviewed generalized/physical-fibre histories and objective generalized-beam
connection to one owned elastic Q4 or S3 V2D. These are explicit opt-ins.

Generic mixed FEModel integration, plastic/fibre-shell coupling, arbitrary
section laws, finite-velocity rotational dynamics and stable postbuckling
paths from rest are not newly qualified. Existing B2/B3, Q4/S3 mechanics and
defaults are unchanged. Qualification is not broadened by publication.

## Version-only artifact bridge

The original unpublished qualification wheel remains immutable at SHA-256
`699ae0dbaef06be61efa7b9a8f923c2ab5607061c00f7ab9f4066217cc340ef4`.
It must not be confused with the earlier PyPI 0.4.2 release.

Release preparation `a891c1020db8e2280241983279b2e2743936e578` was built from a
clean Git export. Its local wheel has 1,785,713 bytes and SHA-256
`3695df8b4f8f21b7a8301042669f6f24743f5f869d032365da78d0785c2d4979`.
All 317 normalized runtime files match the accepted wheel except the exact
`__version__` assignment. Git-export CRLF/LF differences are normalized, not
misreported as raw-byte equality.

The installed release reproduced all 20 accepted scientific files byte-for-byte
using the same eight hash-verified dependency wheels. The successful isolated
probe took about 17.29 seconds and its process tree was empty at completion.
Its first attempt failed before mechanics because the preserved temporary
dependency directory was access-denied; that receipt remains preserved. A
fresh dependency installation corrected the environment, not the element.

External build, dependency, bridge and probe records are retained under
`C:/Users/AudunArnesenNyhus/AppData/Local/ANYrelease/anysolver-0.4.3-a891c10-20260909`.
Publication builds may have different ZIP metadata and hashes. The trusted
publisher therefore verifies every runtime file against the committed accepted
manifest before uploading any distribution and retains its artifact receipt.
The final PyPI hashes must match the distributions uploaded by that workflow;
the local wheel hash is not asserted to be the published hash.

## Release regression boundary

The explicit inventory retains 13 current GE runtime/gate modules in portable
CI and 388 development/protocol/diagnostic modules in the extended lane. New GE
test files without classification fail closed. This does not delete tests,
reclassify scientific results, or rerun prior frozen campaigns. Only the P3
test node binding original package metadata remains historical within its
otherwise active routing/state module.

The local focused run recorded 442 passes, one pre-existing conditional skip,
one historical-node deselection and one workflow-contract failure after the
new publication receipt upload was added. Its expected action sequence was
corrected; the subsequent targeted 15-test release suite passed. An additional
CI failure exposed a current version assertion still set to 0.4.2; that is
updated to 0.4.3 without changing mechanics. Full normal GitHub CI, not these
partial local counts, gates merging. No admin override is authorized.

The preserved qualification artifact and independent review remain the source
of mechanics/performance acceptance. This version bridge makes no new speedup
claim and does not repeat the measured performance campaign.
