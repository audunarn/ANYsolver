# E4-PL-Q1R implementation-identity blocked closeout

## Outcome

E4-PL-Q1R stops at
`BLOCKED_E4_PL_Q1R_IMPLEMENTATION_IDENTITY`. The numbered-frame identity was
successfully preregistered, but the independent implementation review found
remaining priority-one defects before any registered mechanics execution.
This is an implementation-authority block, not a scientific GO or NO-GO for
the frozen formulation.

Candidate
`candidate_e4_pl_q1r.wg2020_numbered_frame_surface_pl_planar_linear_iso_v1`
remains `DORMANT_UNQUALIFIED`. Q1B is not authorized. Production remains
`NO_GO_PRODUCTION_RESTRICTION_UNCHANGED`, and legacy `ShellElement` remains the
default.

The canonical blocked status is 4,139 bytes with SHA-256
`398A895358F84A58667A6C21BE1C4800990D570F4C3AD1AF881FE511077A6025` at
`docs/reference_cases/e4_pl_q1r_status.json`.

## Accepted authority

The accepted preregistration is local commit
`97edc4265a7ce5ca9763f66875d1336e419bcef4`, tree
`e511c461b59162029eaf3e8ceb93f144d94bf910`, with parent
`ad90068a7ee78c3390dfe1b651f28be035094f41`. Its subject is
`docs: preregister E4 PL Q1R numbered-frame qualification` and its exact
16-path plan stage includes the independent verdict
`ACCEPT_Q1R_PREREGISTRATION_NO_P0_P1`.

That acceptance authorizes only static independent implementation work. It
does not authorize mechanics until an accepted implementation review, an
implementation-freeze commit, and a separately reviewed caller-bound
execution contract exist.

## Why implementation identity did not close

The final static implementation review found four unresolved defects:

1. The oracle did not independently reconstruct physical resultants and
   source mixed fields over all 56 numbered cases and their corresponding
   Gauss stations.
2. Its proper-global `R_star` and translation certificate covered stiffness,
   residual, and energy but omitted the registered `T5`, `QD`, projector,
   load, support, reaction, and recovery transports.
3. Its support certificate rebuilt local support rows instead of transporting
   the physically identical boundary problem, and it did not prove the actual
   total-reaction physical/drill decomposition.
4. Its Commit-3 guard did not require the exact three-path authorization
   commit extent, while both implementations accepted review verdicts more
   loosely than the sole frozen exact token.

These are authority and identity defects in draft implementations. They are
not mechanics outcomes. No accepted implementation-review artifact or
implementation-freeze commit was created, and no correction beyond the
single permitted static cycle is authorized under Q1R.

## Preserved draft evidence

The two draft implementations, draft manifest, and five draft tests remain
untracked and byte-identical. They are recorded only as
`NONAUTHORITATIVE_SUCCESSOR_SCAFFOLDING`. They were not staged, imported into
production, or executed as registered mechanics. No contract, raw output,
combined output, scientific review, or Q1B plan exists.

The canonical status binds all eight draft paths, raw byte sizes, and SHA-256
identities. A successor may copy them only after a new independent
preregistration, and must record both the original and corrected identities.

## Repository and production boundary

The closeout changes only the five outcome paths already frozen in the Q1R
allowlist. The six historical evidence roots remain untracked and untouched.
There is no `.gitattributes`, `src/`, package, workflow, API, selector,
serialization, dispatch, recovery, production-test, or default change. No
push, merge, publication, cleanup, or historical rewrite is authorized.
